# 48ステップ実装計画のステップ9をさらに小さなステップに分解

## ステップ9: 修正 - テスト環境エラー修正（Huey SqliteStorage パス）
### 元の目的: `unable to open database file` エラーを解消する

### さらに細分化されたサブステップ（各ステップ3-5分で完了）

#### ステップ9-1: 対象ファイルを開く
- **アクション**: `/home/herbmatsui/autonovel/src/backend/worker_config.py` を開く
- **確認**: ファイルが正常に開けること
- **出力**: ファイル内容が表示される

#### ステップ9-2: DB_PATH 定義の場所を特定する
- **アクション**: ファイル内で `DB_PATH` を検索する
- **確認**: 定義の場所を見つける（行番号をメモ）
- **ツール**: `grep -n "DB_PATH" /home/herbmatsui/autonovel/src/backend/worker_config.py`

#### ステップ9-3: 現在の DB_PATH 定義を確認する
- **アクション**: 特定した行周辺のコードを読む
- **確認**: 現在のパス設定がどのようになっているか確認する
- **判定**: 相対パスが問題になっているか確認する（例: `"./huey.db"` や `"huey.db"` など）

#### ステップ9-4: テスト環境での一時ディレクトリ使用方法を調べる
- **アクション**: テストで一時ディレクトリを使う方法を確認する
- **確認**: `tmp_path` フィクスチャや環境変数の使用方法を把握する
- **参考**: pytest のドキュメントや既存のテストファイルを参照する

#### ステップ9-5: テスト用のフィクスチャを確認するか作成する準備をする
- **アクション**: テストコンフィグファイル（conftest.py）を確認して、既に一時ディレクトリ用のフィクスチャがあるか確認する
- **確認**: `/home/herbmatsui/autonovel/tests/conftest.py` が存在するか確認する
- **ツール**: `ls /home/herbmatsui/autonovel/tests/conftest.py`

#### ステップ9-6: conftest.py が存在する場合はその中身を確認する
- **アクション**: ファイルを開いて内容を読む
- **確認: 一時ディレクトリ関連のフィクスチャがあるか確認する
- **ツール**: `grep -n "tmp_path\|tempdir" /home/herbmatsui/autonovel/tests/conftest.py`

#### ステップ9-7: 一時ディレクトリ用のフィクスチャが必要か判断する
- **アクション**: 既存のフィクスチャがあるか、または作成する必要があるかを判断する
- **確判**: 必要なら作成する準備をする

#### ステップ9-8: 一時ディレクトリ用のフィクスチャを作成する準備をする（conftest.py が存在する場合）
- **アクション**: 必要なら、conftest.py に一時ディレクトリ用のフィクスチャを追加する準備をする
- **確認**: フィクスチャの内容を準備する
- **作業内容**の例:
  ```python
  import pytest
  import tempfile
  import os
  
  @pytest.fixture
  def temp_db_path():
      """テスト用の一時ディレクトリパスを提供するフィクスチャ"""
      with tempfile.TemporaryDirectory() as tmpdir:
          yield os.path.join(tmpdir, "test_huey.db")
  ```

#### ステップ9-9: conftest.py が存在しない場合は作成する準備をする
- **アクション**: conftest.py が存在しない場合は、新規作成する準備をする
- **確認**: 必要な内容を準備する
- **作業内容**の例:
  ```python
  import pytest
  import tempfile
  import os
  
  @pytest.fixture
  def temp_db_path():
      """テスト用の一時ディレクトリパスを提供するフィクスチャ"""
      with tempfile.TemporaryDirectory() as tmpdir:
          yield os.path.join(tmpdir, "test_huey.db")
  ```

#### ステップ9-10: worker_config.py で DB_PATH を環境変数またはフィクスチャから取得するように修正する準備をする
- **アクション**: worker_config.py で DB_PATH の定義を、環境変数から取得するか、またはテストでは一時パスを使うように変更する準備をする
- **確認**: 修正後のコード構造を考える
- **作業内容**の例:
  ```python
  import os
  from typing import Optional
  
  def get_db_path() -> str:
      """データベースファイルのパスを取得する"""
      # テスト環境では一時パスを使う
      test_path = os.environ.get("HUEY_TEST_DB_PATH")
      if test_path:
          return test_path
      # 通常はデフォルトパスを使う
      return "./huey.db"  # または元のパス
  
  DB_PATH = get_db_path()
  ```

#### ステップ9-11: より簡単なアプローチ：環境変数で上書き可能にする
- **アクション**: 既存の DB_PATH 定義を、環境変数で上書き可能にするように変更する準備をする
- **確認**: よりシンプルな変更で対応できるか検討する
- **作業内容**の例:
  ```python
  DB_PATH = os.environ.get("HUEY_DB_PATH", "./huey.db")
  ```

#### ステップ9-12: 変更するコードを最終決定する
- **アクション**: ステップ9-10 と ステップ9-11 を比較して、どちらのアプローチを採用するか決める
- **確認**: 簡潔さと効果を考慮して選択する
- **判定**: 環境変数で上書き可能にするアプローチ（ステップ9-11）を採用する

#### ステップ9-13: 変更するコードを準備する
- **アクション**: `os` モジュールのインポートと、環境変数で上書き可能な DB_PATH 定義を準備する
- **確認**: 変更前後のコード差分を明確にする
- **作業内容**:
  - 追加: `import os` (ファイルの上部に)
  - 変更: `DB_PATH = os.environ.get("HUEY_DB_PATH", "./huey.db")`

#### ステップ9-14: os インポートが必要か確認する
- **アクション**: ファイルの上部に `os` モジュールが既にインポートされているか確確認する
- **確認**: インポートが必要なら追加する準備をする
- **ツール**: `head -10 /home/herbmatsui/autonovel/src/backend/worker_config.py | grep -n "import os"`

#### ステップ9-15: os インポートを追加する（必要な場合）
- **アクション**: `os` インポートが必要なら、ファイルの上部のインポートブロックに追加する
- **確認**: インポートが正しく追加されていること
- **ツール**: `edit` ツールを使ってインポートブロックに追加する

#### ステップ9-16: DB_PATH 定義を変更する
- **アクション**: 特定した DB_PATH 定義の行を、ステップ9-13 で準備した新しい定義に置換する
- **確認**: 置換が正しく行われたこと
- **ツール**: `edit` �ツールを使って正確に置換する

#### ステップ9-17: 変更後のファイルを読んで確認する
- **アクション**: 変更後の `/home/herbmatsui/autonovel/src/backend/worker_config.py` を読む
- **確認**: 新しい DB_PATH 定義が正しく配置されていること
- **判定**: 置換が意図通りに行われたか確認する

#### ステップ9-18: 変更後のファイル全体の構文を確認する準備をする
- **アクション**: Python の構文チェックコマンドを準備する
- **確認**: コマンドが実行可能であることを確認する
- **ツール**: `python -m py_compile /home/herbmatsui/autonovel/src/backend/worker_config.py`

#### ステップ9-19: 構文チェックを実行する
- **アクション**: 準備した構文チェックコマンドを実行する
- **確認**: エラーが出ないこと（何も表示されないのが正常）
- **判定**: 構文エラーがないか確認する

#### ステップ9-20: テスト環境変数を設定してテストを実行する準備をする
- **アクション**: HUEY_DB_PATH 環境変数を設定して、テストを実行する準備をする
- **確認**: テストコマンドが実行可能であることを確認する
- **ツール**: 一時ディレクトリを指定して環境変数を設定するコマンドを準備する

#### ステップ9-21: テストを実行する準備をする（具体的なコマンド）
- **アクション**: テスト環境変数を設定して、問題になっていたテストを実行する準備をする
- **確認**: 実行するコマンドを明確にする
- **作業内容**の例:
  ```bash
  HUEY_DB_PATH=$(mktemp) pytest /home/herbmatsui/autonovel/tests/test_background_worker.py -v
  ```

#### ステップ9-22: テストを実行する
- **アクション**: 準備したコマンドを実行する
- **確認**: テストがエラーなく実行できること
- **判定**: `unable to open database file` エラーが解消されているか確認する

#### ステップ9-23: もしまだエラーが出る場合は一時ディレクトリを使うアプローチを試す
- **アクション**: ステップ9-22 でまだエラーが出る場合は、より明示的に一時ディレクトリを使うアプローチを試す
- **確認**: TMPDIR 環境変数や一時ファイルを使った方法を試す
- **対応**: 必要ならステップ9-10 のような関数ベースのアプローチに切り替える

#### ステップ9-24: 作業の完了を宣言する
- **アクション**: ステップ9のすべてのマイクロステップが完了したことを記録する
- **確認**: 次のステップに進む準備ができていること

## 完了基準
- [ ] `src/backend/worker_config.py` に `import os` が追加されている（必要な場合）
- [ ] `DB_PATH` 定義が `os.environ.get("HUEY_DB_PATH", "./huey.db")` に変更されている
- [ ] 変更後のファイルで Python 構文チェックがエラーなく通る
- [ ] 環境変数 `HUEY_DB_PATH` を設定してテストを実行すると、`unable to open database file` エラーが解消されている
- [ ] 具体的には、`pytest tests/test_background_worker.py` などのテストがエラーなく実行できるか、または環境変数を指定して実行するとエラーが解消される
- [ ] 元のデフォルト動作（環境変数未設定時）は変更されておらず、通常通り "./huey.db" を使っている