# mypy strict モード達成のための48ステップ計画

このドキュメントは、`mypy strict` モードを完全にクリアするための48ステップの詳細な実装計画です。
低性能LLMでも実装可能なように、各ステップを15-30分で完了できる小さなタスクに分解しています。

## 使い方

1. `MYPY_STRICT_MODE_PLAN_48STEPS.md` ファイルを参照してください
2. ステップ1から順に実施してください
3. 各ステップ完了後、`mypy <対象ファイル> --no-error-summary` でエラー減少を確認してください
4. 週末に `pytest tests/unit/ -q` を実施して後退がないことを確認してください

## フェーズ構成

**フェーズ1: 基本的な型注釈追加（ステップ1-16）**
- Streamlitアプリケーション関連ファイルの型注釈を追加
- 主に `no-untyped-def`, `no-untyped-def (パラメータ)`, `var-annotated` エラーを対象

**フェーズ2: 戻り値とコール修正（ステップ17-32）**
- エージェントとサービスの関数戻り値型と未型付き関数呼び出しを修正
- 主に `no-any-return`, `no-untyped-call`, `return-value`, `arg-type` エラーを対象

**フェーズ3: 属性とジェネリクス修正（ステップ33-40）**
- データベースリポジトリとコアモジュールの属性エラーとジェネリクス型を修正
- 主に `attr-defined`, `type-arg`, `assignment` エラーを対象

**フェーズ4: 雑項目と最終調整（ステップ41-48）**
- 残りの雑多なエラーを修正し、最終検証を実施
- 残りの `misc`, `name-defined`, `call-arg`, `method-assign` 等を対象

## 進捗確認

### 現在の状況
- mypyエラー: 1561件
- 対象ファイル数: 284ファイル

### 目標状態
- mypyエラー: 0件
- mypy strict モード: 通過

## 原則

1. **小さなステップ**: 各コミットは15-30分の作業量
2. **テスト駆動**: 変更後に関連テストを実行
3. **後退防止**: 頻繁なコミットと週次のテスト実行
4. **一貫性**:  constancy**: 既存のコードスタイルに従う

## 最初の数ステップの例

**ステップ1**: streamlit_app/state.py 基本関数
- ファイル: streamlit_app/state.py
- タスク: 関数定義に型注釈を追加（最初の10関数）
- 具体例: `def get_runtime(): → def get_runtime() -> UIStateStore:`
- 検証: `mypy streamlit_app/state.py --no-error-summary | head -20`

## 期待される効果

1. **リファクタリング安全性の向上**: 型エラーをコンパイル時に検出
2. **IDEサポートの改善**: 自動補完とリファクタリング支援が向上
3. **バグの早期発見**: 実行前に型ミスマッチを検出
4. **ドキュメントとしての型**: コードの意図が型から読み取れる
5. **チーム開発の効率化**: インターフェースが明確になる

この計画に従って着実に進めれば、約80時間（2週間程度の作業）でmypy strictモードを完全クリアできます。