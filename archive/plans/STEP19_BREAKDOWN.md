# ステップ19: 実装 - `pipeline.py` のリファクタリング（オーケストレーション専用化）

## 目的
`pipeline.py` を純粋な制御フローのみに簡素化

## 作業内容
- `src/easy_mode/pipeline.py` を全面書き換え
- 各サブモジュールを DI として受け取り、`run()` メソッドで順次呼び出す
- 進捗報告・キャンセル・リトライ制御に集中
- 200 行以下を目標（現在 593 行 → 60% 削減）

## 完了基準
`pipeline.py` が 200 行以下、すべての Phase 1-3 統合テストが PASS

## マイクロステップ

#### ステップ19-1: バックアップを作成する
- **アクション**: `src/easy_mode/pipeline.py` のバックアップを取る
- **確認**: バックアップファイルが存在すること
- **ツール**: `cp` コマンド
- **出力**: バックアップファイルのパスを表示
- **完了": ✅

#### ステップ19-2: 現在の行数を測定する
- **アクション**: `pipeline.py` の行数を測定する
- **確認**: 行数が取得できること
- **ツール**: `wc -l` コマンド
- **出力": 行数を表示
- **完了": ✅

#### ステップ19-3: サブモジュールの依存関係を確認する
- **アクション": 現在の `pipeline.py` がどのサブモジュールに依存しているかをリストアップ
- **確認": 依存関係のリストが作成できること
- **ツール": `grep` または `read` コマンド
- **出力": 依存関係リストを一時ファイルに保存
- **完了": ✅

#### ステップ19-4: 新しい `pipeline.py` の骨組みを書く
- **アクション": 新しい `pipeline.py` の内容を考え、骨組みを書く
- **確認": ファイルが作成され、クラスとメソッドの骨組みが存在すること
- **ツール": `write` コマンド
- **出力": 新しいファイルの内容を表示
- **完了": ✅

#### ステップ19-5: `__init__` メソッドを移植する（DI 専用に調整）
- **アクション": 現在の `__init__` メソッドを新しいファイルに移植し、すべての依存をコンストラクタで受け取る形に調整
- **確認": `__init__` がすべての依存を受け取り、サブモジュールを保持すること
- **ツール": `read`、`edit` コマンド
- **出力": 移植後の `__init__` 内容を表示
- **完了": ✅

#### ステップ19-6: `run` メソッドを実装する（オーケストレーションロジック）
- **アクション": `run` メソッドを実装し、サブモジュールを順に呼び出すオーケストレーションロジックを書く
- **確認": `run` メソッドがサブモジュールの適切なメソッドを呼び出すこと
- **ツール": `write`、`edit` コマンド
- **出力": `run` メソッドの内容を表示
- **完了": ✅

#### ステップ19-7: キャンセル機能を実装する
- **アクション": キャンセルフラグの設定と確認ロジックを実装する
- **確認": キャンセルが正しく機能すること
- **ツール": `edit` コマンド
- **出力": キャンセル関連の内容を表示
- **完了": ✅

#### ステップ19-8: リトライ制御を実装する
- **アクション": リトライ設定を読み取り、必要に応じてリトライロジックを実装する
- **確認": リトライ制御が正しく機能すること
- **ツール": `edit` コマンド
- **出力": リトライ関連の内容を表示
- **完了": ✅

#### ステップ19-9: 進捗報告機能を実装する
- **アクション": 進捗報告コールバックを呼び出すロジックを実装する
- **確認": 進捗報告が正しく機能すること
- **ツール": `edit` コマンド
- **出力": 進捗報告関連の内容を表示
- **完了": ✅

#### ステップ19-10: ラッパーメソッドを保持する（後方互換性のため）
- **アクション": `_extract_spice`, `_inject_spice_markers`, `_build_rewrite_prompt`, `_generate_bible`, `_generate_plot_outline` などの後方互換メソッドを保持する
- **確認": これらのメソッドが存在し、サブモジュールに委譲すること
- **ツール": `edit` コマンド
- **出力": ラッパーメソッドの内容を表示
- **完了": ✅

#### ステップ19-11: `create_series` ファクトリ関数を保持する
- **アクション": `create_series` 関数を保持し、新しい `pipeline.py` に合わせて調整する
- **確認": `create_series` が正しく動作すること
- **ツール": `edit` コマンド
- **出力": `create_series` 関数の内容を表示
- **完了": ✅

#### ステップ19-12: 行数を測定し、目標を達成しているか確認する
- **アクション": `pipeline.py` の行数を測定し、200 行以下か確認する
- **確認": 行数が 200 行以下であること
- **ツール": `wc -l` コマンド
- **出力": 行数を表示
- **完了": ✅

#### ステップ19-13: 単体テストを実行し、パスすることを確認する
- **アクション": 関連する単体テストを実行し、 regressions がないことを確認する
- **確認": テストがすべてパスすること
- **ツール": `pytest` コマンド
- **出力": テスト結果を表示
- **完了": ✅

#### ステップ19-14: 統合テストを実行し、パスすることを確認する
- **アクション": `tests/phase2/test_phase2_pipeline_integration.py` を実行し、すべてのテストがパスすることを確認する
- **確認": テストがすべてパスすること
- **ツール": `pytest` コマンド
- "出力": テスト結果を表示
- "完了": ✅
