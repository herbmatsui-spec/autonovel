# 48ステップ実装計画のステップ2をさらに小さなステップに分解

## ステップ2: 現状把握 - `AppContainer2` での `engine` プロバイダ定義の確認
### 元の目的: DI コンテナでの engine 注入経路を理解する

### さらに細分化されたサブステップ（各ステップ3-5分で完了）

#### ステップ2-1: ファイルを開いて内容を確認する
- **アクション**: `/home/herbmatsui/autonovel/src/core/container/app.py` を開く
- **確認**: ファイルが正常に開けること
- **出力**: エディタでファイル内容が表示される

#### ステップ2-2: `engine` プロバイダ定義の場所を特定する
- **アクション**: ファイル内で `engine =` または `.engine` を検索する
- **確認**: プロバイダ定義の場所を見つける（行番号をメモ）
- **ツール**: `grep -n "engine" /home/herbmatsui/autonovel/src/core/container/app.py | head -10`

#### ステップ2-3: `engine` プロバイダ定義の詳細を確認する
- **アクション**: 特定した行周辺のコードを読む
- **確認**: `providers.Factory` または `providers.Singleton` での定義を確認する
- **出力**: 現在の engine プロバイダの設定方法を把握する

#### ステップ2-4: `engine_facade` プロバイダの場所を特定する
- **アクション**: ファイル内で `engine_facade` を検索する
- **確認**: プロバイダ定義の場所を見つける（行番号をメモ）
- **ツール**: `grep -n "engine_facade" /home/herbmatsui/autonovel/src/core/container/app.py`

#### ステップ2-5: `engine_facade` プロバイダ定義の詳細を確認する
- **アクション**: 特定した行周辺のコードを読む
- **確認**: `engine_facade` がどのように定義されているか確認する
- **出力**: engine_facade の設定方法を把握する

#### ステップ2-6: `engine` と `engine_facade` の関係を整理する
- **アクション**: 両方のプロバイダ定義を比較する
- **確認**: 一方がもう一方を参照しているか、または同じものを指しているか確認する
- **出力**: 両者の関係を箇条書きでまとめる

#### ステップ2-7: 現状の依存注入経路を調査する（_legacy 経由か直接か）
- **アクション**: engine プロバイダの定義内部を読む
- **確認**: `_legacy_dep` や `UltimateHegemonyEngine` の呼び出しがあるか確認する
- **判定**: `_legacy` 経由なのか直接注入なのかを決定する

#### ステップ2-8: UltimateHegemonyEngine のインポート箇所を確認する
- **アクション**: ファイルの上部のインポート文を確認する
- **確認**: `UltimateHegemonyEngine` がインポートされているか確認する
- **ツール**: `grep -n "UltimateHegemonyEngine" /home/herbmatsui/autonovel/src/core/container/app.py`

#### ステップ2-9: コンテナテストファイルを開いて内容を確認する
- **アクション**: `/home/herbmatsui/autonovel/tests/unit/test_container.py` を開く
- **確認**: ファイルが正常に開けること
- **出力**: エディタでファイル内容が表示される

#### ステップ2-10: engine プロバイダのテストを探す
- **アクション**: ファイル内で `engine` または `UltimateHegemonyEngine` を検索する
- **確認**: エンジンに関するテストケースがあるか確認する
- **ツール**: `grep -n "engine" /home/herbmatsui/autonovel/tests/unit/test_container.py`

#### ステップ2-11: 既存の engine テスト内容を確認する
- **アクション**: 特定したテストケースの内容を読む
- **確認**: 何をテストしているか、どのようにテストしているかを理解する
- **出力**: 現在のテスト内容を箇条書きでまとめる

#### ステップ2-12: 注入経路図（ASCII 図）の構想を始める
- **アクション**: これまでの調査結果に基づいて、依存注入の流れを考える
- **確認**: コンテナからエンジンまでの経路を頭の中で整理する
- **出力**: 経路図のラフスケッチをメモに書き留める

#### ステップ2-13: 注入経路図（ASCII 図）を作成する準備をする
- **アクション**: 出力先のファイルパスを確認する
- **確認**: メモとしてどこに保存するか決定する（一時ファイルでも可）
- **判定**: どこに書き出すかを決定する

#### ステップ2-14: 注入経路図（ASCII 図）を作成する（上半分）
- **アクション**: 経路図の上半分（コンテナ側）を描き始める
- **確認**: 図の構造が見えてくること
- **出力**: 
  ```
  AppContainer2
       |
       V
  [engineプロバイダ]
  ```

#### ステップ2-15: 注入経路図（ASCII 図）を作成する（中間部分）
- **アクション**: 経路図の中間部分（依存受け渡し）を描き続ける
- **確認**: 依存がどのように渡されているか示せること
- **出力**: 
  ```
       |
       V
  [依存受け渡し: repo, db, llm, ...]
  ```

#### ステップ2-16: 注入経路図（ASCII 図）を作成する（下半分）
- **アクション**: 経路図の下半分（エンジン側）を描き終える
- **確認**: UltimateHegemonyEngine への到達を示せること
- **出力**: 
  ```
       |
       V
  UltimateHegemonyEngine(dependencies)
  ```

#### ステップ2-17: 注入経路図（ASCII 図）に詳細を追加する
- **アクション**: 図に具体的な依存名やラベルを追加する
- **確認**: 図がより具体的で理解しやすくなること
- **出力**: 依存名（repo, db, llm, planner, writer, etc.）を追加

#### ステップ2-18: 注入経路図をファイルに保存する準備をする
- **アクション**: 保存先のファイルを決定する
- **確認**: 一時ファイルでも良いが、後で参照できる場所に保存する
- **判定**: 保存場所とファイル名を決定する

#### ステップ2-19: 注入経路図をファイルに書き出す
- **アクション**: 作成した ASCII 図をファイルに書き込む
- **確認**: ファイルに正しく記録されること
- **出力**: 完成した注入経路図がファイルに保存される

#### ステップ2-20: 結果ファイルの内容を確認する
- **アクション**: 作成したファイルの内容を読み返す
- **確認**: 必要な情報がすべて含まれていること
- **判定**: 調査目的が達成されているか確認する

#### ステップ2-21: 作業の完了を宣言する
- **アクション**: ステップ2の作業が完了したことを記録する
- **確認**: 次のステップに進む準備ができていること