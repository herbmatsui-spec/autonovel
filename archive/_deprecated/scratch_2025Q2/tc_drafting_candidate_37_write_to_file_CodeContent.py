"# Phase 1 実装計画：開発効率とUXの劇的向上\n\n## 概要\n\n現在のコードベース調査に基づく、3つの最優先改善施策の詳細実装計画です。\n\n### 現状スナップショット\n| 領域 | 現状 |\n|------|------|\n| プロンプト管理 | `engine_prompts.py`（53KB, 752行）に全プロンプトがハードコード。一部は `prompts/` 以下に `.j2` ファイルとして外部化済みだが不完全 |\n| DBトランザクション層 | `UnitOfWork` + `DatabaseManager` + `DataRepository` の3層構造。Outboxパターンを内蔵 |\n| リアルタイム通知 | Hueyタスクの進捗を `ProgressState._save_to_db()` → Redis/SQLite → フロントポーリングで取得（`/api/tasks/{task_id}/status` を定期呼び出し） |\n| フロントエンド | React + TypeScript (Vite)。`api.ts` にREST呼び出しを集約 |\n\n---\n\n## 施策 1: プロンプト完全外部化 + Langfuse連携\n\n### 背景と設計方針\n\n`engine_prompts.py` 内には約20のプロンプトビルダーメソッドが混在し、以下の問題があります。\n- テキストの微調整のたびにPythonコードの変更・デプロイが必要\n- A/Bテストの対象バージョン切り替えがコードレベルで困難\n- プロンプトのバージョン管理ができない\n\n**既存の `prompts/` ディレクトリには一部 `.j2` ファイルが存在します**（`final_writing_prompt.j2`、`ai_producer_audit.j2` など）が、`engine_prompts.py` のメソッドのうち約15本はまだPython内でf-string連結されています。\n\n#### 外部化の対象優先順位\n\n| 優先度 | メソッド | 理由 |\n|--------|---------|------|\n| ★★★ | `build_drafting_prompt` | 最頻呼出・品質直結 |\n| ★★★ | `build_polishing_prompt` | 最頻呼出・品質直結 |\n| ★★★ | `build_final_writing_prompt` | 最頻呼出・品質直結 |\n| ★★ | `build_apc_system_prompt` | システムプロンプト・スタイル指示 |\n| ★★ |
<truncated 15798 bytes>

