# 覇権小説エンジン デモ改善計画書 v3 → v4

## 1. 現状の厳しい評価（v3）

前2回の改善（v1→v2, v2→v3）を踏まえ、v3 に**まだ残る**課題を特定した。

| # | 課題 | 証拠 |
|---|------|------|
| 1 | 実アーキテクチャ（SSE/Redis/Huey）を再現していない | 同期進行のみ。READMEはバックエンドSSE＋ジョブキュー |
| 2 | SpiceGuardは静的リスト（実抽出なし） | ラベル固定3件、本文から検出していない |
| 3 | 監査リトライループが見えない | README「95点未満ならリライト」を演舞せず |
| 4 | EPUB/PDF/MOBI出力が偽物（.md出力） | EPUBはzipであるべき |
| 5 | 結果表示時のフォーカス移動欠如 | SR利用者が結果出現に気づかない |
| 6 | 多章で文章が明らかに繰り返す | 全章同じ10文プール |
| 7 | 監査スコアがランダム（入力と無相関） | Math.random で 80–99 |
| 8 | テーマ（ダークモード）不在 | 視認性選択肢が無い |
| 9 | 実バックエンド構成が示されていない | デモ↔READMEアーキテクチャの対応不明 |

## 2. 改善点 9 つと実装方針

### 改善① ジョブ管理・SSE風ログの再現
- 生成開始時にジョブID（ランダム16進）を発行し、バックエンドの「ジョブ投入→進行通知(SSE)→完了」を模したタイムスタンプ付きログフィードを表示
- 各ステップテキストを `job#xxxx STEPn: ...` として追記。エラー時もログに記録

### 改善② SpiceGuard の実抽出デモ
- 各ジャンルに「尖り語彙＋感情語」を定義し、生成本文から実際に該当語を検出
- 検出件数を表示し、本文中の該当箇所を `<<<SPICE:type>>>` マーカー＋黄色ハイライトで囲む本物の抽出ビューを提供

### 改善③ 監査リトライループの可視化
- ステップ3で「監査スコア < 95 → 自動リライト」を演舞
- 計算スコアに応じて「監査スコア93 → リライト(1/3)…」のようにリトライ回数を表示

### 改善④ 本物の EPUB 書き出し
- 純JSで最小 EPUB（zip: store方式＋CRC32）を生成しダウンロード（mimetype/container.xml/content.opf/×.xhtml）
- PDF は「印刷用HTML」ウィンドウを開く（日本語PDFはフォント埋め込みが必要なため誠実に実装）、MOBI は明確に「非対応（デモ）」と注記

### 改善⑤ フォーカス移動・SR体裁の強化
- 結果見出しに `tabindex=-1` を付与し `showResult` で `.focus()`
- 進行中はコンテナに `aria-busy="true"`、完了で `false`
- 完了を live-region で通知（既存）を維持

### 改善⑥ 多章での文章バリエーション拡充
- 各ジャンルに章オープニング文・バリエーションプールを拡張（10→複数グループ）
- 章ごとに異なるオープニングを挿入し、5章でも単調にならないよう構成

### 改善⑦ 決定論的監査スコア
- スコアを入力（創造性・検出尖り数・文字数・話数）から算出し、再現可能かつ意味のある値に
- `score = clamp(78 + creativity*0.15 + spices*1.5 + len/2000*3 + (ch-1)*1, 80, 99)`

### 改善⑧ テーマ（ダークモード）切替
- ヘッダにライト/ダーク切替ボタン。`[data-theme="dark"]` で CSS変数を上書き
- `prefers-color-scheme: dark` にも初期追従

### 改善⑨ アーキテクチャフロー図の追加
- デモ下部に実バックエンド構成（Frontend→FastAPI/SSE→LangGraphパイプライン→Redis/Huey→ChromaDB）を簡易図で表示
- 各デモステップと README アーキテクチャのマッピングを明示

## 3. 実装スケジュール
- フェーズA（構造・アーキ）: 改善⑧⑨（テーマ・フロー図）
- フェーズB（バックエンド再現）: 改善①③（SSEログ・監査リトライ）
- フェーズC（SpiceGuard本物化）: 改善②⑦（実抽出・決定論スコア）
- フェーズD（出力・多章・SR）: 改善④⑤⑥（EPUB・フォーカス・多章バリエーション）

## 4. 成功指標
1. SSE風ログにジョブID付きで進行が表示される
2. 本文から実際に尖りが抽出・マーカー表示される
3. スコア<95 時にリトライが可視化される
4. 本物の .epub（zip）が開ける
5. 結果表示で見出しへフォーカス移動する
6. 5章でも文が目に見えて変化する
7. 同じ入力で監査スコアが再現する
8. ダークモードが切り替わる
9. アーキテクチャ図でデモ↔実装の対応がわかる

## 5. リスクと対応
- EPUB zip 実装の脆弱性 → CRC32/ローカル・セントラル・エンドレコードを正確に実装し、展開テスト
- 多章バリエーションの膨張 → 章オープニング＋既存プールの交互利用で抑える
- テーマ切替の抜け → CSS変数で一括管理
