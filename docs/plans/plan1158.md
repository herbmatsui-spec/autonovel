# GraphRAGインスペクター・文体DNA比較・縦書き装丁プレビュー 実装計画書 (plan1158.md)

本計画書は、UI/UX「発展」項目に分類された3大機能：
1. **GraphRAG相関図のインスペクター化**（世界観設定の把握から編集・本文引用までのシームレス化）
2. **文体DNAのBefore/After比較UI**（作家性チューニングの納得感と楽しさの最大化）
3. **縦書き装丁プレビュー & 宣伝カード**（執筆完了時の達成感・カタルシスと出版・Web投稿導線の強化）
を、低性能なLLMでも確実に1つずつ迷わず実装できるよう、**全48の独立した小さなマイクロステップ**に分割・定義した詳細手順書です。

---

## 全体構成とフェーズ分け

| フェーズ | 対象機能 | ステップ数 | 主な作成・変更対象ファイル |
| :--- | :--- | :--- | :--- |
| **Phase 1** | GraphRAG相関図のインスペクター化 | Step 1 〜 16 | `types/graphInspector.ts`, `api/graph.ts`, `NodeInspector.tsx`, `GraphVisualization.tsx` |
| **Phase 2** | 文体DNAのBefore/After比較UI | Step 17 〜 32 | `types/styleComparison.ts`, `styleSamples.ts`, `StyleComparisonModal.tsx`, `StyleTuningSliders.tsx`, `GeneratePanel.tsx` |
| **Phase 3** | 縦書き装丁プレビュー & 宣伝カード | Step 33 〜 48 | `types/marketingShowcase.ts`, `VerticalReader.tsx`, `BookCoverPreview.tsx`, `PromoCardGenerator.tsx`, `ExportPanel.tsx` |

---

## 詳細マイクロステップ（Micro-Steps 1〜48）

---

### 【Phase 1: GraphRAG相関図のインスペクター化（ステップ 1 〜 16）】
閲覧専用だった相関図を、ノードをクリックして設定の加筆修正・プロパティ追加・本文への引用が可能な「世界観設定ワークベンチ」へ進化させます。

* **Step 1: グラフインスペクターの型定義を作成**  
  - 対象ファイル: `frontend/src/types/graphInspector.ts` [新規作成]  
  - 変更内容: `GraphNodeDetail`, `NodePropertyItem`, `InspectorMode`, `EdgeCreationPayload` の型定義を作成。  
    ```typescript
    export interface NodePropertyItem {
      key: string;
      value: string;
    }
    export interface GraphNodeDetail {
      id: string;
      label: string; // Character, Item, Location, Faction, Event, Concept
      properties: Record<string, string>;
    }
    ```  
  - 検証: TypeScriptの構文エラーがないこと。

* **Step 2: 型インデックスファイルからのエクスポートを追加**  
  - 対象ファイル: `frontend/src/types/index.ts`  
  - 変更内容: `export * from "./graphInspector";` を末尾に追加。  
  - 検証: `import { GraphNodeDetail } from "../types"` で参照できること。

* **Step 3: Graph API クライアントに編集エンドポイントを追加**  
  - 対象ファイル: `frontend/src/api/graph.ts`  
  - 変更内容: `upsertNode(payload)`, `upsertEdge(payload)`, `deleteNode(nodeName)` の3関数を追加し、バックエンドの `POST /api/graph/nodes`, `POST /api/graph/edges` と接続。SQLite環境用のクライアントモックフォールバックも組み込む。  
  - 検証: 各関数の引数・戻り値の型付けが正確であること。

* **Step 4: NodeInspector コンポーネントの骨組みを作成**  
  - 対象ファイル: `frontend/src/components/graph/NodeInspector.tsx` [新規作成]  
  - 変更内容: 幅 360px、右側からスライドインするダークグラスモーフィズムパネルのレイアウトを作成。ヘッダー（ノード名・閉じるボタン）を配置。  
  - 検証: 単体で開閉状態を切り替えられること。

* **Step 5: NodeInspector にエンティティ種別（ラベル）セレクターを実装**  
  - 対象ファイル: `frontend/src/components/graph/NodeInspector.tsx`  
  - 変更内容: ラベル（Character, Location, Item, Faction, Event, Concept）のドロップダウンを配置。選択時にテーマカラー（水色/緑/黄/紫/赤/グレー）のバッジを表示。  
  - 検証: ラベル変更時に即座にバッジ色が連動すること。

* **Step 6: NodeInspector にプロパティ一覧表示テーブルを実装**  
  - 対象ファイル: `frontend/src/components/graph/NodeInspector.tsx`  
  - 変更内容: ノードが保持するプロパティ（例: `role: "主人公"`, `ability: "古代魔導剣術"`, `secret: "王家の血統"`）をキー・バリュー形式の編集行として一覧表示。  
  - 検証: 各値の `<input>` または `<textarea>` でテキストを直接編集できること。

* **Step 7: NodeInspector に「＋ 属性を追加」行の動的追加機能を実装**  
  - 対象ファイル: `frontend/src/components/graph/NodeInspector.tsx`  
  - 変更内容: 「＋ 属性を追加」ボタンをクリックすると空のプロパティ入力欄が1行追加され、新しい属性（例: 弱点、所属ギルド等）を登録可能にする。行の削除（🗑️）ボタンも併設。  
  - 検証: 任意の属性を追加・削除できること。

* **Step 8: NodeInspector に接続エッジ一覧（人間関係・所有関係）を表示**  
  - 対象ファイル: `frontend/src/components/graph/NodeInspector.tsx`  
  - 変更内容: 当該ノードに接続している他のエンティティと関係性（例: `アルト ──[EQUIPPED_WITH]──> 古代魔導剣`）をリスト表示。  
  - 検証: 相関関係がテキストでも一目で確認できること。

* **Step 9: NodeInspector に「＋ 新しい関係を追加」ダイアログを実装**  
  - 対象ファイル: `frontend/src/components/graph/NodeInspector.tsx`  
  - 変更内容: 接続先のノード（ドロップダウン選択）と関係タイプ（FRIEND_OF, ENEMY_OF, BELONGS_TO, OWNS 等）を指定して新しいエッジを引くUIを追加。  
  - 検証: ノード同士の関係性を画面上で手軽に結べること。

* **Step 10: NodeInspector に「💾 設定を保存」処理とトースト通知を実装**  
  - 対象ファイル: `frontend/src/components/graph/NodeInspector.tsx`  
  - 変更内容: 「💾 設定を保存」ボタンで `upsertNode` を呼び出し、保存成功時に「✨ ○○ の設定を更新しました」とトースト通知。  
  - 検証: 保存処理がエラーなく完了すること。

* **Step 11: NodeInspector に「📝 本文へ設定を引用」ボタンを実装**  
  - 対象ファイル: `frontend/src/components/graph/NodeInspector.tsx`  
  - 変更内容: クリック時にノード名とプロパティをまとめたフォーマットテキスト（例: `【設定メモ: アルト】\n役割: 主人公\n能力: 古代魔導剣術`）をクリップボードにコピーし、エディタへ即座に貼り付けられるようにする。  
  - 検証: ワンクリックで設定内容がコピーされること。

* **Step 12: GraphVisualization に NodeInspector を接続**  
  - 対象ファイル: `frontend/src/components/GraphVisualization.tsx`  
  - 変更内容: `onNodeClick` イベントで `setSelectedNode(node)` をセットし、画面右側に `<NodeInspector />` をレンダリング。  
  - 検証: グラフ上のノードをクリックすると右側にインスペクターが開き、選択ノードの情報が正確に流し込まれること。

* **Step 13: GraphVisualization に「＋ 新規エンティティ作成」ボタンを追加**  
  - 対象ファイル: `frontend/src/components/GraphVisualization.tsx`  
  - 変更内容: ツールバーに「＋ キャラ・設定を追加」ボタンを新設。新規空ノードのインスペクターを開き、新しい人物やアイテムをグラフに追加可能にする。  
  - 検証: 画面上から新しいキャラクターを即座に相関図へ登録できること。

* **Step 14: ForceGraph キャンバス領域の自動リサイズ処理を実装**  
  - 対象ファイル: `frontend/src/components/GraphVisualization.tsx`  
  - 変更内容: インスペクターの開閉に合わせて ForceGraph の横幅（`dimensions.width`）が滑らかに調整され、ノードがインスペクターの裏に隠れないようにする。  
  - 検証: インスペクターを開いてもグラフ全体が視界内に収まること。

* **Step 15: インスペクターでの設定保存後にグラフデータをリアルタイム再読み込み**  
  - 対象ファイル: `frontend/src/components/GraphVisualization.tsx`  
  - 変更内容: インスペクター保存コールバックで `fetchGraphData()` を再実行し、グラフ上のノード名やエッジ接続を即座に最新状態にリフレッシュ。  
  - 検証: 画面をリロードすることなく、修正した設定がグラフに即時反映されること。

* **Step 16: Phase 1 結合動作確認**  
  - 対象ファイル: `frontend/src/components/GraphVisualization.tsx`  
  - 変更内容: ノードの選択、プロパティ編集、保存、関係追加、本文引用、新規作成の一連の流れがエラーなく動作することを検証。

---

### 【Phase 2: 文体DNAのBefore/After比較UI（ステップ 17 〜 32）】
抽象的なパラメータ調整に留まっていた文体選択を、同一シーンの「Before（標準） / After（選択文体）」比較とスライダー操作で直感的に行えるようにします。

* **Step 17: 文体比較機能の型定義を作成**  
  - 対象ファイル: `frontend/src/types/styleComparison.ts` [新規作成]  
  - 変更内容: `StyleComparisonScene`, `StyleTuningParams`, `CustomStyleLibraryItem` の型を定義。  
    ```typescript
    export interface StyleComparisonScene {
      id: "action" | "dialogue" | "psychology";
      title: string;
      standardText: string;
      styledTextMap: Record<string, string>; // style_idごとの変換後サンプル
    }
    ```  
  - 検証: TypeScriptの構文エラーがないこと。

* **Step 18: 比較用標準シーンのプリセットデータを作成**  
  - 対象ファイル: `frontend/src/constants/styleSamples.ts` [新規作成]  
  - 変更内容: 3つの標準シーン（⚔️ バトルアクション、💬 仲間との軽快な掛け合い、🌑 息詰まる心理・緊迫）の標準テキストとお手本サンプル文章を定義。  
  - 検証: 各ジャンルの特徴がわかりやすいテキストが定義されていること。

* **Step 19: 文体APIクライアントにプレビュー比較取得関数を追加**  
  - 対象ファイル: `frontend/src/api/styleApi.ts`  
  - 変更内容: `getStyleComparisonPreview(styleId, sceneId, params)` を実装。バックエンドの `GET /api/styles/{style_id}/preview` や音律整形を活用し、比較サンプルを生成・返却。  
  - 検証: APIコールの型定義が整合していること。

* **Step 20: 文体比較モーダルの基本レイアウトを作成**  
  - 対象ファイル: `frontend/src/components/style/StyleComparisonModal.tsx` [新規作成]  
  - 変更内容: 幅 900px、最大高さ 85vh のダークモーダルを作成。ヘッダー（「🎨 作家性DNA・文体Before/Afterプレビュー」）、閉じるボタンを配置。  
  - 検証: モーダルの開閉が正しく動作すること。

* **Step 21: StyleComparisonModal にシーン切り替えタブを実装**  
  - 対象ファイル: `frontend/src/components/style/StyleComparisonModal.tsx`  
  - 変更内容: 「⚔️ バトルアクション」「💬 会話・掛け合い」「🌑 緊迫・心理描写」の3タブを配置し、比較する場面を切り替え可能にする。  
  - 検証: タブをクリックすると表示対象のシーンが切り替わること。

* **Step 22: StyleComparisonModal に横並び（スプリット）Before/Afterカードを実装**  
  - 対象ファイル: `frontend/src/components/style/StyleComparisonModal.tsx`  
  - 変更内容: 左側に「標準文体 (Before)」、右側に「選択文体 (After)」を並列表示。背景や文字色のコントラストを調整し、読み比べしやすくする。  
  - 検証: 2つの文章が左右対称に綺麗にレンダリングされること。

* **Step 23: StyleComparisonModal に文体DNAメトリクスバッジを表示**  
  - 対象ファイル: `frontend/src/components/style/StyleComparisonModal.tsx`  
  - 変更内容: 平均文長（例: `24文字/文`）、体言止め率（例: `35%`）、会話文比率（例: `42%`）、ケレン味強度（例: `Lv.4 圧倒的無双`）をバッジ表示。  
  - 検証: 文体の構造的特徴が数値とバッジで直感的に把握できること。

* **Step 24: インタラクティブ文体チューニングスライダーコンポーネントを作成**  
  - 対象ファイル: `frontend/src/components/style/StyleTuningSliders.tsx` [新規作成]  
  - 変更内容: スライダー調整用の独立したUIコンポーネントを作成。  
  - 検証: 単独で数値入力・スライダー操作を受け取れること。

* **Step 25: StyleTuningSliders に「ケレン味（過剰演出）強度」スライダーを実装**  
  - 対象ファイル: `frontend/src/components/style/StyleTuningSliders.tsx`  
  - 変更内容: スライダー値（1: 抑制・淡々 〜 5: 劇的・過剰ケレン味）を操作可能にし、リアルタイムで値ラベル（例: `「Lv.3: 王道Web小説調」`）を動的表示。  
  - 検証: スライダー操作で数値が即時更新されること。

* **Step 26: StyleTuningSliders に「文末リズム（だ・である vs 体言止め）」スライダーを実装**  
  - 対象ファイル: `frontend/src/components/style/StyleTuningSliders.tsx`  
  - 変更内容: 体言止め・短文連打の比率を 0% 〜 100% で調整可能にするスライダーを配置。  
  - 検証: スライダー位置に応じて文末の勢いパラメータが変化すること。

* **Step 27: StyleTuningSliders に「比喩・五感描写の濃厚さ」スライダーを実装**  
  - 対象ファイル: `frontend/src/components/style/StyleTuningSliders.tsx`  
  - 変更内容: 光影・匂い・心理比喩の修飾語量を調整するスライダーを配置。  
  - 検証: パラメータ変更イベントが親コンポーネントに通知されること。

* **Step 28: スライダー調整に伴うリアルタイムテキスト差分ハイライトを実装**  
  - 対象ファイル: `frontend/src/components/style/StyleComparisonModal.tsx`  
  - 変更内容: スライダーを動かした際、強調された語句や変化した文末に薄紫色のハイライトアニメーション（`.highlight-pulse`）を適用。  
  - 検証: どこがどう変化したのかが視覚的にダイナミックに伝わること。

* **Step 29: 「カスタム文体としてライブラリ保存」機能を実装**  
  - 対象ファイル: `frontend/src/components/style/StyleComparisonModal.tsx`  
  - 変更内容: 調整した文体パラメータに名前（例: 「俺専用・疾走バトル調」）を付けて `localStorage` の文体ライブラリへ保存するダイアログを実装。  
  - 検証: 次回以降、プリセット一覧にカスタム文体として表示・再利用できること。

* **Step 30: 「✨ この文体スタイルを採用」ボタンと適用処理を実装**  
  - 対象ファイル: `frontend/src/components/style/StyleComparisonModal.tsx`  
  - 変更内容: ボタンを押すと `NovelContext` の `character.style_id` および `character.style_profile` にチューニング結果を反映し、「✨ 文体スタイルを適用しました！」の通知と共にモーダルを閉じる。  
  - 検証: 執筆生成エンジンへ即座に選択した文体が適用されること。

* **Step 31: GeneratePanel に「🔍 文体Before/Afterを比較」ボタンを追加**  
  - 対象ファイル: `frontend/src/components/GeneratePanel.tsx`  
  - 変更内容: 文体セレクトボックスの横に「🔍 比較・調整」ボタンを配置し、クリックで `StyleComparisonModal` を起動。  
  - 検証: かんたんモードからワンクリックで文体比較モーダルを開けること。

* **Step 32: StudioWorkspace のサイドバーにも文体比較モーダル起動ボタンを追加**  
  - 対象ファイル: `frontend/src/components/studio/StudioWorkspace.tsx`  
  - 変更内容: 左サイドバーの設定エリアに「🎨 文体DNA比較」ボタンを追加。  
  - 検証: Studioモードでもいつでも文体を比較・微調整できること。

---

### 【Phase 3: 縦書き装丁プレビュー & 宣伝カード（ステップ 33 〜 48）】
無味乾燥なZIPダウンロードで終わっていた執筆完了体験を、実際の電子書籍やWeb小説リーダーと同じ「縦書き見開きリーダー」と「SNS宣伝カード自動生成」で感動のゴールへ導きます。

* **Step 33: 成果物ショーケース機能の型定義を作成**  
  - 対象ファイル: `frontend/src/types/marketingShowcase.ts` [新規作成]  
  - 変更内容: `BookShowcaseData`, `MarketingPromoData`, `ReaderTheme` ('light' | 'sepia' | 'dark') の型を定義。  
  - 検証: TypeScriptの構文エラーがないこと。

* **Step 34: マーケティング生成 API クライアントを作成**  
  - 対象ファイル: `frontend/src/api/marketingApi.ts` [新規作成]  
  - 変更内容: バックエンドの `POST /api/marketing/generate` を呼び出し、あらすじ、煽りキャッチコピー、おすすめタグ、X(Twitter)投稿文を取得する関数を実装。  
  - 検証: APIリクエストとレスポンスの型付けが正確であること。

* **Step 35: BookShowcaseModal の基本コンポーネントを作成**  
  - 対象ファイル: `frontend/src/components/showcase/BookShowcaseModal.tsx` [新規作成]  
  - 変更内容: 幅 1000px、フルスクリーンに近い迫力あるモーダルレイアウトを作成。上部に「📖 縦書き装丁プレビュー」タブと「📢 SNS宣伝カード」タブを配置。  
  - 検証: タブ切り替えとモーダル開閉が正しく動作すること。

* **Step 36: 縦書きリーダーコンポーネントの骨組みを作成**  
  - 対象ファイル: `frontend/src/components/showcase/VerticalReader.tsx` [新規作成]  
  - 変更内容: 見開き2ページ風の枠組み、ページ番号表示、前へ/次へボタンを配置。  
  - 検証: 枠組みが正しくレンダリングされること。

* **Step 37: 縦書き専用のCSSスタイルを定義**  
  - 対象ファイル: `frontend/src/index.css`  
  - 変更内容: 日本語小説専用の縦書きルールを追加：  
    ```css
    .vertical-book-viewport {
      writing-mode: vertical-rl;
      text-orientation: upright;
      font-family: 'Shippori Mincho', 'Noto Serif JP', serif;
      line-height: 2.1;
      letter-spacing: 0.08em;
    }
    ```  
  - 検証: 本文が右から左へ美しい縦書きで流れること。

* **Step 38: VerticalReader にルビ（ふりがな）・約物整形ロジックを実装**  
  - 対象ファイル: `frontend/src/components/showcase/VerticalReader.tsx`  
  - 変更内容: `｜親文字《ルビ》` の縦書きルビ表示 (`<ruby>` タグ)、段落先頭の全角1マス空け、鍵括弧（「」）の縦書き配置を完璧に整形。  
  - 検証: ルビが文字の右側に正確に配置され、小説本そのままの外観になること。

* **Step 39: 見開きページめくりインタラクションを実装**  
  - 対象ファイル: `frontend/src/components/showcase/VerticalReader.tsx`  
  - 変更内容: 左右キー入力（← / →）やクリックで、ページめくりアニメーションと共に前後の章・ページへ移動できるナビゲーションを実装。  
  - 検証: 本物の本をめくるようにスムーズに読み進められること。

* **Step 40: 縦書きリーダー用カスタマイズバーを実装**  
  - 対象ファイル: `frontend/src/components/showcase/VerticalReader.tsx`  
  - 変更内容: リーダー下部に「文字サイズ（標準 / 大）」「背景色（生成り和紙 / ホワイト / ダーク）」の切り替えボタンを配置。  
  - 検証: ワンクリックで読書環境のテーマが切り替わること。

* **Step 41: 仮想書籍カバー（表紙・帯）プレビューコンポーネントを作成**  
  - 対象ファイル: `frontend/src/components/showcase/BookCoverPreview.tsx` [新規作成]  
  - 変更内容: タイトル、著者名、ジャンルバッジ、そして書籍の「帯（キャッチコピー）」がデザインされたグラフィカルな本の表紙カードを作成。  
  - 検証: 商業ライトノベルの単行本のような洗練されたカバーが表示されること。

* **Step 42: SNS宣伝カード自動生成コンポーネントを作成**  
  - 対象ファイル: `frontend/src/components/showcase/PromoCardGenerator.tsx` [新規作成]  
  - 変更内容: 宣伝カード作成パネルの枠組みを作成。  
  - 検証: コンポーネントがエラーなくロードされること。

* **Step 43: PromoCardGenerator に宣伝カードプレビューを実装**  
  - 対象ファイル: `frontend/src/components/showcase/PromoCardGenerator.tsx`  
  - 変更内容: 16:9 比率のグラデーションカード内に「タイトル」「1行あらすじ」「魅力的な煽り文句（カタルシスポイント）」を美しくレイアウトした宣伝画像風プレビューを描画。  
  - 検証: X(Twitter)のOGPカードのような見栄えの良いカードが表示されること。

* **Step 44: PromoCardGenerator に X(Twitter) 投稿文自動生成とコピーボタンを実装**  
  - 対象ファイル: `frontend/src/components/showcase/PromoCardGenerator.tsx`  
  - 変更内容: 「本文から自動生成したあらすじ＋ハッシュタグ（例: #Web小説 #カクヨム #なろう）」をテキストエリアに表示し、「📋 投稿文をコピー」ボタンでクリップボードへ保存。  
  - 検証: ワンクリックでSNS告知用のテキストが手に入ること。

* **Step 45: 宣伝カードの PNG 画像ダウンロード機能を実装**  
  - 対象ファイル: `frontend/src/components/showcase/PromoCardGenerator.tsx`  
  - 変更内容: プレビュー表示された宣伝カード要素を Canvas 経由で PNG 画像としてダウンロードできる「🖼️ 宣伝画像をダウンロード」ボタンを実装。  
  - 検証: ボタンクリックでカードが画像ファイルとして即座にダウンロード保存されること。

* **Step 46: ExportPanel に「📖 縦書き装丁プレビュー & 宣伝カード」ボタンを追加**  
  - 対象ファイル: `frontend/src/components/ExportPanel.tsx`  
  - 変更内容: ZIPエクスポートボタンの隣に、目立つアクセントカラー（ゴールド/パープル）で「📖 縦書き装丁プレビュー」ボタンを配置し、クリックで `BookShowcaseModal` を起動。  
  - 検証: かんたんモードのエクスポートエリアから直感的に成果物プレビューを呼び出せること。

* **Step 47: StudioWorkspace にも成果物プレビューボタンを配置**  
  - 対象ファイル: `frontend/src/components/studio/StudioWorkspace.tsx`  
  - 変更内容: 上部ツールバーに「📖 装丁プレビュー」ボタンを新設。  
  - 検証: Studioモードで執筆中のユーザーも、いつでも完成形の装丁と縦書きプレビューを確認できること。

* **Step 48: 全体結合テスト・リグレッション検証・ビルド確認**  
  - 実行コマンド:  
    - `cd frontend && npx tsc --noEmit` (TypeScript 型チェック)  
    - `cd frontend && npm run test` (Vitest ユニットテスト)  
  - 検証内容:  
    1. 相関図ノードをクリックして設定インスペクターが開き、属性の加筆・保存・本文引用ができるか。  
    2. 文体Before/After比較モーダルが開き、3つの場面で文章が左右比較でき、スライダーでリアルタイム調整できるか。  
    3. 縦書き装丁プレビューが開き、美しい縦書き見開きでページめくり読書ができ、宣伝カードが生成・コピーできるか。  
    4. すべてのコンポーネントでビルドエラー・型エラーがゼロであること。

---

## 本計画書で得られるユーザー価値（UXの革命的進化）

1. **GraphRAG相関図が「本物の制作ツール」へ**  
   単なる鑑賞用の相関図から、世界観やキャラクター設定を直接書き込み、本文に引き出せる「ナレッジ編集デスク」へと変貌します。

2. **文体選びの「納得感」と「手応え」**  
   「どの文体が自分の作品に合うか分からない」という作家の迷いを、同一場面の Before / After 比較とケレン味スライダーによって解消し、直感的な作品作りを支援します。

3. **「書き終えた！」という最高のカタルシスと出版体験**  
   冷たいZIPダウンロードではなく、電子書籍の縦書き装丁で自分の物語が本になった姿を目にし、そのままSNSへ告知できる導線を提供することで、作家体験（Creator UX）を究極の完成度へと引き上げます。
