# ��権小説エンジン v3.2 - ��善実装計画書（24ステップ版）

## ���要
コードレビューで特定された3つの主要改善点を、低性能LLMでも実装可能な24の小さなステップに分割した詳細計画書です。

---

## Phase 1: かんたんモードパイプラインのAPI統合（最優先・ステップ1-12）

### Step 1: 現状確認 - EasyModePipeline クラスの理解
**目的**: `src/easy_mode/pipeline.py` の `EasyModePipeline` クラスのインターフェースを理解する
**作業内容**:
- `EasyModePipeline` クラスの `__init__` メソッドの引数を確認
- `run()` メソッドの��り��（`SeriesResult`）の構造を確認
- `PipelineConfig` データクラスのフィールドを確認
**完了基準**: クラスの使い方をメモにまとめる

### Step 2: 現状確認 - 現在の API エンドポイントの理解
**目的**: `/api/easy_mode/generate` エンドポイントの実装を理解する
**作業内容**:
- `src/backend/server.py` の `generate_easy` 関数を読む
- `full_auto_workflow` が何をしているか確認
- リクエスト/レスポンスのスキーマを確認
**完了基準**: 現在のフローを��解する

### Step 3: 現状確認 - EasyModeRequest スキーマの確認
**目的**: APIリクエストで受け取るパラメータを理解する
**作業内容**:
- `src/models/easy_mode_schemas.py` または関連ファイルを探す
- `EasyModeRequest` クラスのフィールドを確認
- `PipelineConfig` にマッピングできる項目を特定
**完了基準**: パラメータマッピング表を作成

### Step 4: 設計 - PipelineConfig への変��ロジックの設計
**目的**: APIリクエストから `PipelineConfig` を作成する変��関数を設計
**作業内容**:
- `EasyModeRequest` の各フィールドを `PipelineConfig` のフィールドにマッピング
- デフォルト��の設定方��を決める
- �����フィールドと任意フィールドを整理
**完了基準**: 変��関数の仕様書（メモレベルでOK）を作成

### Step 5: 実装 - 変��関数の作成
**目的**: APIリクエストから `PipelineConfig` を作成する関数を実装
**作業内容**:
- `src/backend/routers/easy_mode.py` または新規ユーティリティファイルに関数を追加
- 関数名: `create_pipeline_config_from_request(request: EasyModeRequest) -> PipelineConfig`
- ���ヒントを正しく付ける
**完了基準**: 関数が作成され、インポートエラーがない

### Step 6: 実装 - EasyModePipeline インスタンスの作成と実行
**目的**: APIエンドポイント内で `EasyModePipeline` を使って生成を実行
**作業内容**:
- `generate_easy` 関数内で `EasyModePipeline` をインポート
- エンジンインスタンスを取得（DIコンテナから）
- `PipelineConfig` を作成してパイプラインを実行
- 結果を適切なレスポンス形式に変��
**完了基準**: パイプラインが実行され、結果が返ってくる

### Step 7: 実装 - 進��コールバックの実装（オプション）
**目的**: 生成進��をタスク管理システムに報告する
**作業内容**:
- `PipelineConfig.progress_callback` に渡すコールバック関数を作成
- 既存の `BackgroundReporter` / `ProgressState` を活用
- ���階ごとの進��メッセージを定義
**完了基準**: 進��が正しく報告される

### Step 8: 実装 - 結果のレスポンス変��
**目的**: `SeriesResult` を API レスポンス用の��書に変��
**作業内容**:
- `SeriesResult` のフィールドを確認
- フロントエンドが期待する形式にマッピング
- ���要に応じて `SeriesResult.to_dict()` メソッドを追加
**完了基準**: 適切な JSON レスポンスが返る

### Step 9: 実装 - エラー��ンドリングの追加
**目的**: パイプライン実行時のエラーを適切に��理
**作業内容**:
- `try-except` でパイプライン実行を��む
- 既存の `HTTPException` パターンに従ってエラーを返す
- ログ出力を追加
**完了基準**: エラー時に適切な HTTP ステータスとメッセージが返る

### Step 10: テスト - 単体テストの作成（変��関数）
**目的**: `create_pipeline_config_from_request` のテストを書く
**作業内容**:
- `tests/test_easy_mode_api.py` を作成または既存ファイルに追加
- 正常系・��常系のテストケースを作成
- モックを使ってエンジン依存を排除
**完了基準**: テストがパスする

### Step 11: テスト - 統合テストの作成（エンドポイント）
**目的**: `/api/easy_mode/generate` エンドポイントの統合テストを書く
**作業内容**:
- モックエンジンを使ってパイプライン実行をシミュレート
- レスポンス形式の����
- エラーケースの����
**完了基準**: テストがパスする

### Step 12: ��作確認 - 手動テスト
**目的**: 実際にAPIを��いて動作確認
**作業内容**:
- サーバーを起動
- `curl` または Postman で `/api/easy_mode/generate` を��く
- レスポンスが期待通りか確認
- ログに進��が出力されるか確認
**完了基準**: 手動テストで正常動作を確認

---

## Phase 2: タイプ注��問題の修正（ステップ13-18）

### Step 13: 修正対象の特定 - spice_guard.py の型エラー確認
**目的**: mypy エラーの詳細を確認
**作業内容**:
- `mypy src/easy_mode/spice_guard.py` を実行
- エラー��ッセージをすべて記録
- 各エラーの行番号と内容を整理
**完了基準**: エラー一覧をメモにまとめる

### Step 14: 修正 - metadata フィールドの型修正
**目的**: `metadata: Dict = None` を正しい型に修正
**作業内容**:
- `src/easy_mode/spice_guard.py` 行21を修正
- `from typing import Optional` をインポート（未インポートなら）
- `metadata: Optional[Dict] = None` に変更
- `field(default_factory=dict)` でも可
**完了基準**: mypy でこの行のエラーが消える

### Step 15: 修正 - priority パラメータの型注��修正
**目的**: SpiceElement コンストラクタ呼び出しでの型エラーを修正
**作業内容**:
- エラーが出ている行（189, 200, 217, 228付近）を確認
- `priority` 変数の型が `str` になっているか確認
- ���要なら型キャストやアサーションを追加
**完了基準**: mypy でこれらの行のエラーが消える

### Step 16: 修正 - pipeline.py の型エラー修正
**目的**: `src/easy_mode/pipeline.py` の mypy エラーを修正
**作業内容**:
- `mypy src/easy_mode/pipeline.py` を実行
- 行93: `raise last_error` の問題を修正（`last_error` が `Exception` ���であることを保��）
- 行109: `episodes = []` に型注��を追加
**完了基準**: mypy で pipeline.py のエラーが消える

### Step 17: 全体の型チェック実行
**目的**: プロジェクト全体で mypy エラーが減ったか確認
**作業内容**:
- `mypy src/` を実行
- エラー数を確認
- 新たなエラーが発生していないか確認
**完了基準**: エラー数が減少、またはゼロになる

### Step 18: テスト実行で動作確認
**目的**: ���修正でランタイムエラーが出ていないか確認
**作業内容**:
- `pytest tests/test_phase2_pipeline_integration.py -v` を実行
- SpiceGuard 関連テストがすべてパスするか確認
**完了基準**: 全テストパス

---

## Phase 3: レガシー��ードの整理（ステップ19-24）

### Step 19: ���� - 非推��プロパティの使用��所特定
**目的**: `ai_api`, `llm_client` などがどこで使われているか調べる
**作業内容**:
- `grep -r "ai_api" src/` で��索
- `grep -r "llm_client" src/` で��索
- `grep -r "planner" src/` など、レガシープロパティも含めて��索
**完了基準**: 使用��所の一覧を作成

### Step 20: ���� - _legacy 経由の依存性を特定
**目的**: `UltimateHegemonyEngine._legacy` 経由で注入されている依存性を特定
**作業内容**:
- `engine.py` の `_legacy_dep` メソッドとプロパティ群を確認
- どのキーで何が注入されているか整理
- コンテナ定義（`container.py`）と照合
**完了基準**: 依存性マップを作成

### Step 21: 判断 - ��除可能かどうかの判断
**目的**: 各レガシー��ードが��除可能か判断
**作業内容**:
- Step 19, 20 の結果を見て、使用��所がないものを特定
- 使用されているものは、移行コストを見積もる
- チーム/プロジェクトの方��に従って判断（この計画では「��除しない」前提で進める）
**完了基準**: ��除対象と保持対象のリストを作成

### Step 22: ��善 - 非推��警告の強化（��除しない場合）
**目的**: 非推��プロパティの警告をより明確にする
**作業内容**:
- `engine.py` の `ai_api`, `llm_client` プロパティの警告メッセージを改善
- 移行先（`llm` プロパティ）を明記
- `DeprecationWarning` ではなく `FutureWarning` に変更（デフォルトで表示される）
**完了基準**: ��告メッセージが改善される

### Step 23: ドキュメント化 - レガシー依存性の移行ガイド作成
**目的**: 将来的な移行のためにドキュメントを残す
**作業内容**:
- `docs/guides/legacy_migration.md` を作成
- 非推��プロパティ一覧と移行先を記載
- `_legacy` 経由の依存性注入の仕組みを説明
**完了基準**: ドキュメントが作成される

### Step 24: 最終確認 - 全テスト実行
**目的**: すべての変更で既存機能が��れていないか確認
**作業内容**:
- `pytest tests/ -x` で全テスト実行
- 失敗するテストがないか確認
- カバレッジも確認
**完了基準**: 全テストパス

---

## 実装��序の推��

```
Week 1: Phase 1 (Steps 1-12) - API統合
Week 2: Phase 2 (Steps 13-18) - ���修正
Week 3: Phase 3 (Steps 19-24) - レガシー整理
```

## 各ステップの所要時間目安

| ステップ | 所要時間 | ��易度 |
|---------|---------|--------|
| 1-3 | 30分 each | 低 |
| 4-5 | 1時間 each | 低-中 |
| 6-9 | 1-2時間 each | 中 |
| 10-11 | 1時間 each | 中 |
| 12 | 30分 | 低 |
| 13-16 | 30分 each | 低 |
| 17-18 | 30分 each | 低 |
| 19-21 | 1時間 each | 中 |
| 22-23 | 30分 each | 低 |
| 24 | 1時間 | 低 |

---

## 成功基準（Definition of Done）

1. **Phase 1完了**: `/api/easy_mode/generate` が `EasyModePipeline` を使用し、SpiceGuard保護付きの全自動生成が動作する
2. **Phase 2完了**: `mypy src/` でエラーがゼロ（または大幅減少）になる
3. **Phase 3完了**: レガシー��ードが整理され、ドキュメント化され、警告が改善される
4. **全体**: `pytest tests/` がすべてパスする

---

## 注意事項

- 各ステップは独立してコミット可能な単位とする
- わからないことは即座に質問せず、まずは既存コードを読んで推��する
- テストは実装と同時に書く（TDD風）
- 既存のコードスタイル（ruff, mypy設定）に従う