# AutoNovel 緊急対応実装計画書

## 概要
本ドキュメントは、コードレビューで指摘された緊急に対応が必要なクリティカルおよびハイプライオリティ課題に対する詳細な実装計画をまとめたものです。
重大度順に対応項目を列挙し、具体的な修正手順と見積もりを示します。

## 対応優先順位
1. **Critical**（致命的）: 認証バイパスなど、放置すると直ちにセキュリティ侵害につながる問題
2. **High**: セキュリティリスクまたはサービス可用性に重大な影響を与える問題

## 修正項目一覧

### Critical Issues (即時対応必須)

#### C1: 設定オーバーライド二重呼び出しとグローバル設定書き換え競合
| 項目 | 内容 |
|------|------|
| ファイル | `src/backend/tasks.py` |
| 問題 | (1) 143行目で `_apply_config_overrides(config_dict)` を呼んだ直後、172行目で再度呼び出し（コンテキスト変数の戻り値設定のため重複）。(2) グローバル `Settings` を書き換えるため、Huey タスクの並行実行で他タスクの設定が上書きされる。 |
| 影響 | (1) 無駄な処理、(2) ユーザーAのAPIキーがユーザーBのタスク実行中に漏洩するリスク |
| 修正方針 | 1. `_apply_config_overrides` の呼び出しを1回に統合（143行目のみ使用）。<br>2. 設定オーバーライドをコンテキスト変数ベースに移行し、グローバル変更を完全に排除。<br>3. `finally` 節でコンテキスト変数をリセットする仕組みを確実にする。 |
| 検証 | (a) 2タスク並列実行で `Settings.gemini_api_key` が混ざらないこと、(b) 例外発生時にコンテキスト変数が必ずリセットされるユニットテスト |
| 工数 | 1.5h |
| 依存 | `config/settings.py` のコンテキスト変数ベース実装 |

#### C2: CORS 設定の起動時検証強化
| 項目 | 内容 |
|------|------|
| ファイル | `src/backend/server.py:216-225`, `config/cors_config.py` |
| 問題 | `allow_credentials=True` とワイルドカード/広いオリジンの組み合わせにより認証付きCSRFリスクが発生。現在の実装では起動時に検証ロジックがない。 |
| 影響 | クロスサイトリクエストフォージェリ(CSRF)攻撃の脆弱性 |
| 修正方針 | 1. 起動時にCORS設定を検証し、`allow_credentials=True` の場合、`allowed_origins` がリストで且つ `"*"` または `["*"]` でないことを確認。違反時はアプリ起動を失敗させる。<br>2. `cors_config.py` のデフォルトを空リスト `[]` に変更し、環境変数 `CORS_ALLOWED_ORIGINS` を必須にする（本番環境では明示設定が必要）。<br>3. 開発時の利便性のため、`CORS_ALLOW_ALL_ORIGINS=true` 環境変数でワイルドカード許可を一時的に可能にする（本番では無視）。 |
| 検証 | (a) 異常設定で起動失敗すること、(b) 正常設定で起動成功することの統合テスト |
| 工数 | 1.0h |
| 依存 | なし |

#### C3: 本番環境での test* API キー フォールバックの削除
| 項目 | 内容 |
|------|------|
| ファイル | `src/backend/auth.py:66-71` |
| 問題 | `ENVIRONMENT != "production"` で `test-` プレフィックスのAPIキーが認証を通過する。本番誤設定時に認証が完全にバイパスされるリスク。 |
| 影響 | 本番環境での不正アクセス可能性 |
| 修正方針 | 1. `test-*` フォールバックを完全に削除し、未設定時は常に認証を拒否するように変更。<br>2. 開発用には `ALLOWED_API_KEYS` の明示設定を強制し、未設定なら起動時に `RuntimeError` を送出する。<br>3. 開発時のみ特例を許可する `API_KEY_DEV_BYPASS=true` 環境変数を追加（本番では無視）。 |
| 検証 | (a) `test-xxx` キーで403になること、(b) `API_KEY_DEV_BYPASS=true` + `ENVIRONMENT=development` でのみバイパス可、(c) `ENVIRONMENT=production` でのバイパス不可 |
| 工数 | 0.5h |
| 依存 | なし |

#### H4: SSE の同期 Redis pubsub.get_message() 呼び出しによるイベントループブロック
| 項目 | 内容 |
|------|------|
| ファイル | `src/backend/sse.py:69` |
| 問題 | `pubsub.get_message(timeout=1.0)` が同期呼び出しで最大1秒ブロック。イベントループ上で実行されるため他のリクエスト処理が停止し、スケーラビリティが著しく低下。 |
| 影響 | 高並列アクセス時の応答遅延、リソース枯渇リスク |
| 修正方針 | 1. `redis.asyncio` の非同期 `pubsub.get_message()` を使用し、完全ノンブロッキング実装に変更。<br>2. 代替案として、`pubsub.listen()` の非同期イテレータを使用し、`asyncio.wait_for` でタイムアウト制御。<br>3. ハートビートメカニズムは既存の仕組みを維持。 |
| 検証 | (a) 100接続同時SSE時に他のルータが遅延しないこと、(b) locustで100RPSでもp99<200msを維持 |
| 工数 | 1.5h |
| 依存 | `redis.asyncio` パッケージ（既に依存済みの可能性高） |

### High Issues (1週間以内対応)

#### C4: ヘルスチェックのLLM実呼び出しデフォルト無効化（改善提案）
| 項目 | 内容 |
|------|------|
| ファイル | `src/backend/routers/health.py:62-69` |
| 問題 | `/health` エンドポイントでLLMヘルスチェックが常に実行される。K8s livenessProbeが頻繁に実行されるとAPI課金が増大し、LLM側レート制限でプローブが失敗し無意味な再起動を誘発。 |
| 影響 | 不要なAPIコスト、誤検知によるサービス不安定性 |
| 修正方針 | 1. ヘルスチェックの構造を見直し、`/health/live`（LLMなし基本ヘルス）と`/health/ready`（全項目包括的ヘルス）を分離。<br>2. `/health/live`はDB、Redis、ワーカーのみをチェックし、高速かつ無料で実行可能。<br>3. `/health/ready`は現在の`/health`と同様にすべての依存サービスを含むが、明示的に使用されることを前提とする。 |
| 検証 | (a) `/health/live`でLLMコールが発生しないこと、(b) `/health/ready`でLLMコールが発生すること |
| 工数 | 1.0h |
| 依存 | ルータ分割設計 |

## 実装フェーズとマイルストーン

### フェーズ0: 準備（0.5日）
- 影響範囲調査: 各ファイルの関連コード全検索
- テストカバレッジ確認（特に認証・タスク周り）
- 実装計画のレビューおよび担当者割当

### フェーズ1: Critical対応（1.5日）
- C1: tasks.py 設定オーバーライド二重呼び出し修正
- C2: server.py CORS起動時検証強化 + cors_config.py 修正
- C3: auth.py test*フォールバック削除
- H4: sse.py 非同期Redis実装へ変更
- **マイルストーン**: 認証/設定/健全性関連の単体テスト + 統合テスト green

### フェーズ2: High対応（0.5日）
- C4: ヘルスチェックエンドポイントの分離とLLM呼び出し最適化
- **マイルストーン**: ヘルスチェック機能の検証

### フェーズ3: テスト・ドキュメント・検証（0.5日）
- 各修正に対する単体テスト追加
- 統合テストでの動作確認
- ドキュメント更新（設定方法、環境変数等）
- 最終動作確認と性能ベンチマーク

## リスクと代替案

| リスク | 説明 | 代替案/緩和策 |
|--------|------|----------------|
| 設定変更による後方互換性 | 本番で `ALLOWED_API_KEYS` 未設定かつ `test-*` キーを使っていた場合、認証が切れる | 移行期間は警告ログを出し、猶予期間を設ける（例: 環境変数 `API_KEY_MIGRATION_PERIOD=true` で一時許可） |
| Hueyタスクの設定分離による影響範囲 | 設定オーバーライドのコンテキスト変数化で既存コードに影響 | まずは読み取り専用参照にし、書き込みはタスクコンテキスト経由に徐々に移行。書き込み箇所を特定してから実装 |
| Redis非同期実装の互換性 | `redis.asyncio` が利用できない環境での代替策 | 同期Redis呼び出しを `loop.run_in_executor` でスレッドプールにオフロードするフォールバック実装を準備 |
| ヘルスチェック分離による監視設定変更 | 既存の監視設定が `/health` を参照している場合の変更必要性 | 移行期間中は両エンドポイントを維持し、徐々に `/health/live` へ移行を促す |

## 成果物
- 修正済みソースコード（各ファイル）
- 実装後の単体・結合テスト（既存テストを走らせ、カバレッジを維持/向上）
- 更新後のAPI仕様書（認証必須エンドポイントの明記、ヘルスチェックエンドポイントの分離等）
- 運用マニュアルへの追記（CORS設定方法、認証キーの必須設定、ヘルスチェックの使い分け等）

## 完了条件
- すべての Critical 項目が修正され、本番デプロイ可能な状態であること
- 既存の機能テストが全てパスすること（回帰なし）
- 新たに追加したセキュリティ・非同期に関するテストがパスすること
- コードレビューを経て、残る Medium/Low は改善余地としてバックログに残すこと

---
*作成日: 2026-08-29*
*対象リポジトリ: /home/herbmatsui/autonovel*