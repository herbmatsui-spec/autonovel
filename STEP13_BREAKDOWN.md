# ステップ13: 現状把握 - `pipeline.py` の関数/メソッド・依存関係の完全調査

## 目的
分割設計のための影響範囲調査

## 作業内容
- `src/easy_mode/pipeline.py` の全メソッド（30 個以上）を列挙
- 各メソッドの行数・複雑度・依存関係を表にまとめる
- 外部依存（`engine`, `preset`, `spice_guard`, `llm`）と内部依存を分離
- メソッドをグルーピング（生成系・監査系・リライト系・進捗系・ヘルパー）

## 完了基準
`proposals/pipeline_split_design.md` にグルーピング図を作成

## マイクロステップ

#### ステップ13-1: 対象ファイルを開く
- **アクション**: `/home/herbmatsui/autonovel/src/easy_mode/pipeline.py` を開く
- **確認**: ファイルが正常に開けること
- **ツール**: `read` コマンドで先頭20行を表示
- **出力**: ファイルの先頭内容を確認
- **完了**: ✅

#### ステップ13-2: ファイルの全行数を取得
- **アクション**: ファイルの総行数を数える
- **確認**: 行数が取得できること
- **ツール**: `wc -l /home/herbmatsui/autonovel/src/easy_mode/pipeline.py`
- **出力**: 行数を記録
- **完了**: ✅

#### ステップ13-3: メソッド定義を検索する
- **アクション**: ファイル内で `def ` または `async def ` を検索し、行番号とメソッド名をリストアップ
- **確認**: すべてのメソッド定義の場所を見つける（行番号をメモ）
- **ツール**: `grep -n "^[[:space:]]*def \|^[[:space:]]*async def " /home/herbmatsui/autonovel/src/easy_mode/pipeline.py`
- **出力**: メソッドリストを一時ファイルに保存（例: /tmp/methods.txt）
- **完了**: ✅

#### ステップ13-4: 各メソッドの行数を計算する
- **アクション**: 各メソッドの開始行から次のメソッドの開始行（またはファイル終端）までの行数を計算
- **確認**: 各メソッドの行数が取得できること
- **ツール**: スクリプトを書いて一時ファイルを作成し、実行する（例: awk を使う）
- **出力**: メソッドごとの行数を一時ファイルに保存
- **完了**: ✅

#### ステップ13-5: 各メソッドの複雑度を推定（行数ベースで簡易評価）
- **アクション**: 行数を基準に複雑度を low/medium/high と分類（例: 1-20行: low, 21-40行: medium, 41行以上: high）
- **確認**: 各メソッドに複雑度ラベルを付ける
- **ツール**: 前ステップの出力を読み込み、分類を追加
- **出力**: メソッドごとの複雑度を一時ファイルに追加
- **完了**: ✅

#### ステップ13-6: 各メソッドの外部依存を特定する
- **アクション**: 各メソッド内で `self.engine`, `self.preset`, `self.spice_guard`, `self.llm` などの属性アクセスまたはインポートされたモジュールの使用を検索
- **確認**: 各メソッドがどの外部依存を使用しているかをリスト
- **ツール**: 各メソッドの範囲を抜き出し、`grep -n "self\.\(engine\|preset\|spice_guard\|llm\)"` またはインポートされた名前を検索
- **出力**: メソッドごとの外部依存リストを一時ファイルに追加
- **完了**: ✅

#### ステップ13-7: 各メソッドの内部依存（他メソッドの呼び出し）を特定する
- **アクション**: 各メソッド内で `self.` で始まる他のメソッド呼び出し（属性アクセスではなくメソッド呼び出し）を検索
- **確認**: 各メソッドが呼び出す内部メソッドをリスト
- **ツール**: 各メソッドの範囲を抜き出し、`grep -n "self\.[a-zA-Z_][a-zA-Z0-9_]*("` を使い、メソッド定義リストと照合
- **出力**: メソッドごとの内部依存リストを一時ファイルに追加
- **完了**: ✅

#### ステップ13-8: メソッドを機能別にグルーピングする
- **アクション**: メソッド名や処理内容から、生成系（bible, plot, episode 生成）、監査系（audit）、リライト系（rewrite, spice 注入）、進捗系（progress report）、ヘルパー（汎用ユーティリティ）に分類
- **確認**: 各メソッドがどのグループに属するかを決定
- **ツール**: 手動で判断し、一時ファイルにグループ列を追加
- **出力**: メソッドごとのグループを一時ファイルに追加
- **完了**: ✅

#### ステップ13-9: 結果をマークダウン表にまとめる
- **アクション**: メソッド名, 行数, 複雑度, 外部依存, 内部依存, グループ を含むマークダウン表を作成
- **確認**: 表が正しくフォーマットされていること
- **ツール**: 一時ファイルを読み込み、マークダウン表を生成してファイルに書き込む
- **出力**: `/home/herbmatsui/autonovel/proposals/pipeline_split_design.md` の草稿を作成
- **完了**: ✅

#### ステップ13-10: グルーピング図を作成する
- **アクション**: グループごとにメソッドをリストし、依存関係を矢印で示す簡易図をテキストまたは mermaid 形式で作成
- **確認**: 図が読みやすく、グループ間の依存が示されていること
- **ツール**: マークダウンファイルに mermaid ブロックを追加（例: flowchart または graph）
- **出力**: `/home/herbmatsui/autonovel/proposals/pipeline_split_design.md` に最終版を書き込む
- **完了**: ✅

#### ステップ13-11: 作業ディレクトリをクリーンアップ
- **アクション**: 一時ファイルを削除
- **確認**: 一時ファイルが残っていないこと
- **ツール**: `rm -f /tmp/methods.txt /tmp/*.tmp` など
- **出力**: クリーンアップ完了
- **完了**: ✅
