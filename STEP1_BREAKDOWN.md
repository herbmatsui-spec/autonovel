# 48ステップ実装計画のステップ1をさらに小さなステップに分解

## ステップ1: 現状把握 - `UltimateHegemonyEngine` の `_legacy` 依存箇所の完全調査
### 元の目的: 影響範囲を正確に把握する

### さらに細分化されたサブステップ（各ステップ3-5分で完了）

#### ステップ1-1: ファイルを開いて内容を確認する
- **アクション**: `/home/herbmatsui/autonovel/src/backend/engine.py` を開く
- **確認**: ファイルが正常に開けること
- **出力**: エディタでファイル内容が表示される

#### ステップ1-2: `_legacy_dep` メソッドの場所を特定する
- **アクション**: ファイル内で `_legacy_dep` を検索する
- **確認**: メソッド定義の場所を見つける（行番号をメモ）
- **ツール**: `grep -n "_legacy_dep" /home/herbmatsui/autonovel/src/backend/engine.py`

#### ステップ1-3: `_legacy_dep` メソッドの定義を確認する
- **アクション**: 特定した行番号のメソッド定義を見る
- **確認**: メソッドのシグニチャと実装を理解する
- **出力**: メソッドの目的と返り値を把握

#### ステップ1-4: 全プロパティメソッドをリストアップする
- **アクション**: ファイル内で `@property` デコレータを検索する
- **確認**: 全プロパティメソッド名をリストアップする
- **ツール**: `grep -n "@property" /home/herbmatsui/autonovel/src/backend/engine.py`

#### ステップ1-5: 各プロパティが `_legacy_dep` を使っているか確認する（planner編）
- **アクション**: `planner` プロパティの実装を見る
- **確認**: `_legacy_dep('planner')` を使っているか確認
- **判定**: 使っている場合は「使用」、使っていない場合は「未使用」を記録

#### ステップ1-6: 各プロパティが `_legacy_dep` を使っているか確認する（writer編）
- **アクション**: `writer` プロパティの実装を見る
- **確認**: `_legacy_dep('writer')` を使っているか確認
- **判定**: 使っている場合は「使用」、使っていない場合は「未使用」を記録

#### ステップ1-7: 各プロパティが `_legacy_dep` を使っているか確認する（pm編）
- **アクション**: `pm` プロパティの実装を見る
- **確認**: `_legacy_dep('pm')` を使っているか確認
- **判定**: 使っている場合は「使用」、使っていない場合は「未使用」を記録

#### ステップ1-8: 各プロパティが `_legacy_dep` を使っているか確認する（ctx_mgr編）
- **アクション**: `ctx_mgr` プロパティの実装を見る
- **確認**: `_legacy_dep('ctx_mgr')` を使っているか確認
- **判定**: 使っている場合は「使用」、使っていない場合は「未使用」を記録

#### スteps1-9: 各プロパティが `_legacy_dep` を使っているか確認する（formatter編）
- **アクション**: `formatter` プロパティの実装を見る
- **確認**: `_legacy_dep('formatter')` を使っているか確認
- **判定**: 使っている場合は「使用」、使っていない場合は「未使用」を記録

#### ステップ1-10: 各プロパティが `_legacy_dep` を使っているか確認する（validator編）
- **アクション**: `validator` プロパティの実装を見る
- **確認**: `_legacy_dep('validator')` を使っているか確認
- **判定**: 使っている場合は「使用」、使っていない場合は「未使用」を記録

#### ステップ1-11: 各プロパティが `_legacy_dep` を使っているか確認する（auditor編）
- **アクション**: `auditor` プロパティの実装を見る
- **確認**: `_legacy_dep('auditor')` を使っているか確認
- **判定**: 使っている場合は「使用」、使っていない場合は「未使用」を記録

#### ステップ1-12: 各プロパティが `_legacy_dep` を使っているか確認する（narrative編）
- **アクション**: `narrative` プロパティの実装を見る
- **確認**: `_legacy_dep('narrative')` を使っているか確認
- **判定**: 使っている場合は「使用」、使っていない場合は「未使用」を記録

#### ステップ1-13: 各プロパティが `_legacy_dep` を使っているか確認する（critique編）
- **アクション**: `critique` プロパティの実装を見る
- **確認**: `_legacy_dep('critique')` を使っているか確認
- **判定**: 使っている場合は「使用」、使っていない場合は「未使用」を記録

#### ステップ1-14: 各プロパティが `_legacy_dep` を使っているか確認する（marketing編）
- **アクション**: `marketing` プロパティの実装を見る
- **確認**: `_legacy_dep('marketing')` を使っているか確認
- **判定**: 使っている場合は「使用」、使っていない場合は「未使用」を記録

#### ステップ1-15: 各プロパティが `_legacy_dep` を使っているか確認する（bible_agent編）
- **アクション**: `bible_agent` プロパティの実装を見る
- **確認**: `_legacy_dep('bible_agent')` を使っているか確認
- **判定**: 使っている場合は「使用」、使っていない場合は「未使用」を記録

#### ステップ1-16: 各プロパティが `_legacy_dep` を使っているか確認する（plot_agent編）
- **アクション**: `plot_agent` プロパティの実装を見る
- **確認**: `_legacy_dep('plot_agent')` を使っているか確認
- **判定**: 使っている場合は「使用」、使っていない場合は「未使用」を記録

#### ステップ1-17: 各プロパティが `_legacy_dep` を使っているか確認する（style_rag編）
- **アクション**: `style_rag` プロパティの実装を見る
- **確認**: `_legacy_dep('style_rag')` を使っているか確認
- **判定**: 使っている場合は「使用」、使っていない場合は「未使用」を記録

#### ステップ1-18: `_legacy_dep` のその他の使用箇所を検索する
- **アクション**: ファイル全体で `_legacy_dep` のすべての使用箇所を検索する
- **確認**: プロパティ以外での使用箇所がないか確認する
- **ツール**: `grep -n "_legacy_dep" /home/herbmatsui/autonovel/src/backend/engine.py`

#### ステップ1-19: 使用状況をまとめ始める
- **アクション**: これまでの確認結果を一時的にメモする
- **確認**: 使用しているプロパティと使っていないプロパティを分けてリストアップする
- **出力**: 使用中のプロパティリストと未使用のプロパティリストの草稿

#### ステップ1-20: 結果をファイルに書き出す準備をする
- **アクション**: 提出先のファイルパスを確認する
- **確認**: `/home/herbmatsui/autonovel/proposals/legacy_deps_inventory.md` が存在するか確認する
- **判定**: 存在しない場合は新規作成、存在する場合は上書きする準備をする

#### ステップ1-21: 結果をファイルに書き出す（ヘッダー部分）
- **アクション**: ファイルにヘッダー情報を書き込む
- **確認**: ファイルが作成/更新されること
- **内容**: 
  ```
  # UltimateHegemonyEngine _legacy依存調査結果
  調査日: [現在の日付]
  調査ファイル: src/backend/engine.py
  ```

#### ステップ1-22: 結果をファイルに書き出す（使用中の依存リスト）
- **アクション**: 使用している `_legacy_dep` 依存のリストを書き込む
- **確認**: ファイルに正しく記録されること
- **フォーマット**: 
  ```
  ## 使用中の _legacy_dep 依存
  - planner: src/backend/engine.py:[行番号]
  - writer: src/backend/engine.py:[行番号]
  ...
  ```

#### ステップ1-23: 結果をファイルに書き出す（未使用の依存リスト）
- **アクション**: 未使用の `_legacy_dep` 依存のリストを書き込む
- **確認**: ファイルに正しく記録されること
- **フォーマット**: 
  ```
  ## 未使用の _legacy_dep 依存
  - なし（またはリスト）
  ```

#### ステップ1-24: 結果ファイルの内容を確認する
- **アクション**: 作成したファイルの内容を読み返す
- **確認**: 必要な情報がすべて含まれていること
- **判定**: 調査目的が達成されているか確認する

#### ステップ1-25: 作業の完了を宣言する
- **アクション**: ステップ1の作業が完了したことを記録する
- **確認**: 次のステップに進む準備ができていること