# 詳細な実装計画（v4.0 以降）　約20,000文字

## 1. プロジェクトビジョン
覇権小説エンジンは、AIを活用して小説の企画から執筆、出版・メディアミックスまでを一貫して自動化するプラットフォームである。現在のv3.6ではUI/UXのリファクタリングと状態管理の統一が完了し、次世代v4.0では「マルチメディア出力」「国際化」「コラボレーション」「高度な品質保証」を柱とする機能拡張を計画する。

## 2. 全体スケジュール
| フェーズ | 期間 | 主な目的 |
|----------|------|----------|
| フェーズ0：基盤整備 | 2026年9月〜10月 | 依存アップグレード、CI/CD強化、テスト基盤整備 |
| フェーズ1：マルチメディア出力 | 2026年11月〜2027年1月 | 音声合成（TTS）、動画生成、画像生成の統合 |
| フェーズ2：国際化（i18n） | 2027年2月〜4月 | 多言語UI、翻訳ワークフロー、ローカライズ済みコンテンツ生成 |
| フェーズ3：コラボレーション機能 | 2027年5月〜7月 | リアルタイム共同編集、コメント機能、バージョン管理 |
| フェーズ4：品質保証・分析 | 2027年8月〜10月 | 自動評価メトリクス、読者シミュレーション、フィードバックループ |
| フェーズ5：リリースと運用 | 2027年11月〜12月 | ベータ公開、パフォーマンスチューニング、ドキュメント整備 |

各フェーズは2週間のスプリント単位で詳細タスクに分解し、マイルストーンごとにレビューとデモを行う。

## 3. フェーズ0：基盤整備（詳細）

### 3.1 依存関係のアップグレード
- Python 3.12 → 3.13（サポート確認後移行）
- Node.js 22 → 24（LTS）
- FastAPI 0.110 → 0.115
- React 18 → 18.3（コンフィグモード）
- TailwindCSS 3.4 → 3.5
- アップグレード手順：
  1. `pyproject.toml` と `package.json` を更新
  2. Dependabot プルリクエストを自動生成
  3. 各ライブラリの破壊的変更を調査し、必要に応じてアダプターを実装
  4. フルテストスイートを実行し、後退がないことを確認
  5. セキュリティスキャン（Bandit, npm audit）を実行し、脆弱性を修正

### 3.2 CI/CD パイプラインの強化
- GitHub Actions を用いたマルチプラットフォームビルド（Linux, macOS, Windows）
- ビルドキャッシュの活用（actions/cache）
- 並列テスト実行（pytest-xdist, jest --runInBand）
- アーティファクト保存（Dockerイメージ, ビルド済みフロントエンド）
- デプロイメントステージング環境への自動デプロイ（Vercel/Netlify + Fly.io）
- プルリクエストごとにプレビューURLを自動生成し、UI変更の視認性を向上
- シークレット管理は GitHub Secrets と HashiCorp Vault のハイブリッド運用

### 3.3 テスト基盤の整備
- ユニットテスト：カバレッジ 90% 以上を目標（現状 80%）
- 統合テスト：API エンドポイントとワークフローのエンドツーエンドテスト
- E2E テスト：Playwright を導入し、フロントエンドのユーザーシナリオを自動化
- パフォーマンステスト：k6 を用いたロードテストとスパイクテスト
- テスト結果の可視化：GitHub Actions のジョブサマリーと Codecov レポート
- テストデータのモック：Factory Boy (Python) と factory.ts (TypeScript) を統一

### 3.4 ドキュメントとナレッジマネジメント
- 既存のドキュメントを MkDocs に移行し、検索可能なナレッジベースを構築
- API リファレンスは FastAPI の自動生成 Swagger UI と ReDoc を維持
- アーキテクチャ決定記録 (ADR) を `docs/adr/` ディレクトリに格納し、マークダウンで記録
- チュートリアルビデオを作成し、YouTube プライベートチャンネルにアップロード
- 貢献者ガイド (`CONTRIBUTING.md`) を更新し、プルリクエストテンプレートを改善

## 4. フェーズ1：マルチメディア出力（詳細）

### 4.1 音声合成（TTS）機能
#### 目的
生成された小説のテキストを自然な音声に変換し、オーディオブックやサンプル音声として提供する。

#### 技術選定
- Google Cloud Text-to-Speech（WaveNet）または Amazon Polly
- オープンソース fallback として Coqui TTS または ESPnet-TTS を Docker コンテナで提供
- 言語：日本語、英語、中国語（簡体・繁体）を優先

#### 実装ステップ
1. **サービス抽象層の作成**
   - `src/services/tts/base.py` に `AbstractTTSService` を定義（`synthesize(text, voice, speed) -> bytes`）
   - 各プロバイダーごとに実装クラスを作成 (`GoogleTTSService`, `AmazonTTSService`, `CoquiTTSService`)
   - ファクトリー `src/services/tts/factory.py` で環境変数に基づいてインスタンスを生成
2. **設定と認証**
   - `.env.example` に `TTS_PROVIDER`, `GOOGLE_APPLICATION_CREDENTIALS`, `AWS_ACCESS_KEY_ID` 等を追加
   - Docker Compose に必要なボリュームマウントとシークレット管理を追加
3. **API エンドポイント**
   - `POST /api/tts/synthesize` （テキストとボイスパラメータを受け取り、MP3 または WAV を返す）
   - `GET /api/tts/voices` （利用可能なボイス一覧を返す）
   - レートリミットと認証は既存の API キー機構を流用
4. **ストレージとキャッシュ**
   - 合成結果は一時的に `storage/tts_cache` に保存し、同一テキストの再合成を防止
   - Redis を用いたキー値ストアでキャッシュ管理（TTL 24時間）
5. **フロントエンド統合**
   - 各エピソード詳細ページに「音声再生」ボタンを追加
   - プログレスバーと再生/一時停止コントロールを実装（HTML5 Audio API）
   - ダウンロードボタンでオーディオファイルをローカル保存
6. **テスト**
   - モックプロバイダーを用いた単体テスト（合成成功・失敗シナリオ）
   - 統合テストで実際のプロバイダーへの呼び出し（ステージング環境のみ）
   - アクセシビリティ：音声コンテンツには字幕（テキスト）を同時に表示
7. **ドキュメント**
   - `docs/features/tts.md` に利用方法とカスタマイズ手順を記載

### 4.2 動画生成機能
#### 目的
小説の章またはシーンを短編アニメーションまたはスライドショー形式の動画に変換し、YouTube Shorts や TikTok 向けコンテンツを自動生成。

#### 技術選定
- 画像生成：Stable Diffusion XL（API またはローカルランタイム）
- テキストから画像へのプロンプト生成：LLM を用いたシーン抽出とプロンプト最適化
- 動画合成：FFmpeg を用いた画像シーケンスとナレーションの合成
- 音声ナレーション：前述の TTS サービスを利用

#### 実装ステップ
1. **シーン抽出モジュール**
   - `src/services/video/scene_extractor.py` で章テキストを文単位に分割し、重要シーンをスコアリング（感情ヒートマップ、キャラクター登場頻度など）
   - 上位 N シーン（デフォルト 5）を選出
2. **プロンプトエンジニアリング**
   - `src/services/video/prompt_builder.py` が選出シーンごとに「アニメーション風」「イラスト風」「Cinematic」などのスタイルタグを付与し、Stable Diffusion 用プロンプトを生成
   - ネガティブプロンプトも自動生成（低品質、文字ぼかしなどを除外）
3. **画像生成**
   - `src/services/video/image_generator.py` で Hugging Face Inference API またはローカルの `diffusers` パイプラインを呼び出し、各シーンの画像を PNG で取得
   - 生成結果は `storage/video_cache/<video_id>/scene_XX.png` に保存
   - フェイルセーフとして、生成失敗時はプレースホルダーイラストを使用
4. **動画合成**
   - `src/services/video/video_assembler.py` が FFmpeg をラップし、画像シーケンスにクロスフェード、テキストオーバーレイ（字幕）、ナレーション音声を合成
   - 出力フォーマット：MP4 (H.264/AAC)、解像度 1080x1920 (縦長) または 1920x1080 (横長)、フレームレート 30fps
   - メタデータにタイトル、説明、ハッシュタグを埋め込み
5. **API エンドポイント**
   - `POST /api/video/generate` （章 ID とスタイルパラメータを受け取り、動画生成ジョブをキューに投入）
   - `GET /api/video/status/{job_id}` （ジョブの進捗と完了時のダウンロードURLを返す）
   - バックグラウンドジョブは Huey と Redis を使用し、進捗はステータスファイルで管理
6. **フロントエンド統合**
   - 「動画生成」ボタンをエピソード詳細とプロット一覧に追加
   - モーダルでスタイル選択（アニメ、リアリスティック、モノクロ）と長短の選択肢を提供
   - 生成完了時にプレビュープレイヤーとダウンロードボタンを表示
7. **テストと品質保証**
   - 単体テスト：各モジュールの入出力をモック
   - 統合テスト：ステージング環境で実際の画像生成 API への呼び出し（コスト削減のためにダミー画像を使用）
   - 動画の再生可能性とメタデータの妥当性を自動検証（ffprobe）
8. **ドキュメント**
   - `docs/features/video_generation.md` に手順とカスタマイズ方法を記載

### 4.3 画像生成・イラスト機能（スタンドアロン）
#### 目的
キャラクター立ち絵、挿絵、表紙画像などをオンデマンドで生成し、電子書籍やプロモーション素材に使用。

#### 実装ステップ
- 基本は動画生成の画像生成モジュールを流用し、単体エンドポイント `POST /api/image/generate` を提供
- プロンプトはキャラ属性（性別、髪型、衣装、表情）とシーン情報から自動構築
- 出力形式：PNG（透明背景対応）および JPEG
- サイズバリエーション：サムネイル (256x256)、標準 (1024x1024)、ハイ解像度 (2048x2048)
- キャッシュ機構により同じプロンプトは再利用
- フロントエンドではキャラエディット画面に「イラスト再生成」ボタンを配置
- テストは画像生成モジュールのモックと、実際の API 呼び出し（ステージング）で実施

## 5. フェーズ2：国際化（i18n）詳細

### 5.1 多言語対応方針
- UI 文字列は React-i18next または Format.js を使用し、JSON ファイルで管理
- 言語コードは ISO 639-1（ja, en, zh, ko, es, fr）を段階的に追加
- コンテンツ言語（小説本文）は原文のまま保持し、オプションで機械翻訳版を生成可能にする
- 日付・通貨・数値フォーマットは ICU(MessageFormat) に準拠

### 5.2 実装ステップ
1. **i18n ライブラリ導入**
   - `npm i i18next react-i18next`
   - `src/i18n/` ディレクトリを作成し、`locales/{lang}/common.json` などを配置
2. **言語切替コンポーネント**
   - ヘッダーに言語セレクトドロップダウンを追加し、選択時に `i18n.changeLanguage` を呼び出し、URL に `?lang=xx` パラメータを付与
   - 次回訪問時にローカルストレージから言語設定を復元
3. **既存テキストの抽出**
   - `src/frontend/src/**/*.tsx` と `*.ts` からハードコードされた文字列を抽出し、翻訳キーに置換
   - スクリプト `scripts/extract_i18n.ts` を作成し、自動抽出とプレースホルダー生成を実行
4. **動的コンテンツの翻訳**
   - 小説のタイトル・あらすじは、ユーザーが言語を切替えたときに機械翻訳サービス（DeepL API または Google Translate）を呼び出し、結果をキャッシュ
   - 翻訳品質を向上させるために、用語集（キャラクター名、固有語彙）を事前に提供
5. **ルーティングとSEO**
   - 各言語ごとに `/ja/`, `/en/` などのプレフィックスを付与し、サーバーサイドレンダリング（SSR）または静的サイト生成（SSH）でプリレンダー
   - `next-i18next` （もしくは同様のルーティングラッパー）を導入し、検索エンジンが各言語ページを適切にインデックスできるようにする
6. **テスト**
   - 各言語の文字列が欠けていないかを検証するテスト（`i18next-scanner`）
   - UI スクリーンショットテストで各言語レイアウトが崩れないかを確認（Storybook + Chromatic）
   - 機械翻訳の品質はサンプルテキストで BLEU スコアを測定し、基準を設定
7. **ドキュメント**
   - `docs/i18n/guidelines.md` に翻訳作業の手順と用語集管理方法を記載
   - リリースノートに各言語対応状況を追記

## 6. フェーズ3：コラボレーション機能詳細

### 6.1 リアルタイム共同編集
#### 目的
複数ユーザーが同じプロットや章を同時に編集でき、変更を即座に反映させる。

#### 技術選定
- WebSocket サーバー（FastAPI + WebSocket または Socket.io）
- 操作変換（Operational Transform, OT）または Conflict-free Replicated Data Type (CRDT) を採用
- Yjs ライブラリ（WebSocket プロバイダー）をフロントエンドとバックエンドで使用

#### 実装ステップ
1. **バックエンド WebSocket エンドポイント**
   - `/ws/{document_id}` エンドポイントを作成し、Yjs プロバイダーとして動作
   - ドキュメントの状態は PostgreSQL の JSONB カラムまたは Redis にスナップショットとして保存
   - 認証は既存の API キーまたは JWT を WebSocket ハンドシェイクで検証
2. **フロントエンド統合**
   - `src/components/collab/YjsProvider.tsx` で Yjs を初期化し、WebSocket 接続を確立
   - 既存のエディターコンポーネント（Monaco Editor または Slate）を Yjs バインディングでラップ
   - カーソルの位置と選択範囲を他ユーザーに表示（アバターと色分け）
3. **操作履歴とアンドゥ/リドゥ**
   - Yjs のアンドゥマネージャを利用し、グローバルな履歴を保持
   - ボタンでアンドゥ/リドゥを提供し、サーバー側でも履歴を永続化（監査目的）
4. **コメントとディスカッション**
   - ドキュメントに対してスレッド形式のコメントを追加できる機能を実装
   - コメントは DB に保存し、タイムスタンプと著者情報を記録
   - メンション機能（@ユーザー名）を実装し、通知メールまたはアプリ内通知を送信
5. **通知システム**
   - リアルタイム通知は WebSocket 再利用または別途サーバーサンドイベント (SSE) を使用
   - デスクトップ通知（Notification API）とメール送信（SendGrid または SES）をオプションで提供
6. **テスト**
   - WebSocket 接続の同時接続数をシミュレートし、負荷テスト（Locust または k6）
   - 競合シナリオ（同時に同じ範囲を編集）で収束性を検証
   - セキュリティテスト：不正なドキュメントアクセスやデータ改ざんを防ぐ
7. **ドキュメント**
   - `docs/features/collab_realtime.md` にアーキテクチャ図と利用手順を記載

### 6.2 バージョン管理とブランチング
#### 目的
小説のプロットや章を Git 風にブランチし、変更をマージまたはプルリクエスト形式でレビューできるようにする。

#### 実装ステップ
1. **データモデル拡張**
   - `documents` テーブルに `branch`、`parent_branch`、`is_head` カラムを追加
   - `commits` テーブルで変更セット（diff）を保存し、メタデータ（作者、メッセージ、タイムスタンプ）を記録
2. **ブランチ操作API**
   - `POST /api/documents/{id}/branch` ：新しいブランチを作成
   - `GET /api/documents/{id}/branches` ：ブランチ一覧
   - `POST /api/documents/{id}/commit` ：現在のブランチに変更をコミット
   - `POST /api/documents/{id}/merge` ：ブランチをマージ（3-way マージアルゴリズムを実装）
3. **差分表示**
   - ダイフフアルゴリズム（Googleのdiff-match-patchまたはjsdiff）を用いてインライン差分を表示
   - ビジュアルdiffビュー（サイドバイサイド）を提供
4. **プルリクエスト風フロー**
   - `POST /api/documents/{id}/pull-request` ：ベースブランチとヘッドブランチを指定しPRを作成
   - レビューアを割り当て、コメント機能と承認フローを実装
   - マージ時にCIチェック（自動テストと品質スコア）を必須とする
5. **フロントエンドUI**
   - ブランチセレクターとコミット履歴ビューをサイドバーに配置
   - プルリクエスト一覧ページとレビューモーダルを実装
6. **テスト**
   - ブランチ作成、コミット、マージの単体テスト
   - マージコンフリクト時の自動解決と手動介入フローをシナリオテスト
   - パフォーマンステスト：大規模ドキュメント（10万字以上）でのブランチ操作遅延を測定
7. **ドキュメント**
   - `docs/features/version_control.md` にワークフロー図とベストプラクティスを記載

## 7. フェーズ4：品質保証・分析詳細

### 7.1 自動評価メトリクス
#### 目的
生成された小説の品質を多角的に評価し、改善点をフィードバックループに供給する。

#### 評価項目
- 読みやすさ（文長、語彙難易度、助詞使用頻度）
- 感情の起伏（ヒートマップの変動率）
- キャラクターの立て込み（対話比率、独白比率）
- プロットの緊張感（クライマックスまでの上昇曲線）
- 独創性（n-gram重複率、既存作品との類似度）
- 官能描写の適切度（SpiceGuardによる抽出率とバランス）

#### 実装ステップ
1. **メトリクス抽出モジュール**
   - `src/services/quality/metrics.py` で spaCy または Ginza を用いた自然言語処理を行い、各指標を計算
   - 結果は JSON スキーマに従い、`storage/quality/<document_id>/metrics.json` に保存
2. **API エンドポイント**
   - `GET /api/quality/{document_id}` ：メトリクスJSONを返す
   - `POST /api/quality/batch` ：複数ドキュメントの一括評価（非同期ジョブ）
3. **フィードバックループ**
   - メトリクスがしきい値を下回った場合、自動的にプロンプト調整やリライト指示を生成
   - 品質改善のためのリライトタスクを Huey キューに投入し、再度評価
4. **ダッシュボード**
   - フロントエンドに品質ダッシュボードページを追加し、時系列グラフとヒートマップを可視化（Chart.js または Recharts）
   - アラート機能：品質スコアが急落したら Slack またはメールで通知
5. **テスト**
   - 各メトリクスアルゴリズムの単体テスト（既知のテキストに対する期待値比較）
   - エンドツーエンドテストで品質評価ジョブのフローを検証
6. **ドキュメント**
   - `docs/features/quality_metrics.md` に算出方法と解釈ガイドを記載

### 7.2 読者シミュレーションとA/Bテスト
#### 目的
仮想読者エージェントを用いて物語の受容度を予測し、異なるバージョンのプロットや表現を比較する。

#### 技術選定
- 強化学習エージェント（シンプルなマルチアームバンディットまたはコンテキストバンディット）
- シミュレータは LLM を用いた「読者反応予測」モデル（フィクトーシズムまたは人間フィードバックデータを基にファインチューニング）

#### 実装ステップ
1. **読者モデルの作成**
   - `src/services/simulation/reader_model.py` で、与えられたテキストに対する「好感度」「継続意欲」「離脱ポイント」を出力するモデルを定義
   - モデルは Hugging Face の `text-classification` または `regression` ファインチューニングモデルを使用
   - 学習データは過去のユーザーレビューと読了時間のログから構築（プライバシー匿名化済み）
2. **シミュレーションエンジン**
   - `src/services/simulation/engine.py` が章ごとに読者モデルを適用し、累積スコアと離脱確率を計算
   - 複数のバージョン（A/B）を同時にシミュレートし、期待値の高い方を推奨
3. **API エンドポイント**
   - `POST /api/simulation/run` ：ドキュメントIDとパラメータを受け取り、シミュレーション結果を返す
   - `GET /api/simulation/{simulation_id}` ：結果の取得とダウンロード（CSVまたはJSON）
4. **フロントエンド統合**
   - プロット編集画面に「シミュレーション実行」ボタンを追加し、結果をレーダーチャートで表示
   - ユーザーは異なるプロット案を並べて比較し、シミュレーションスコアに基づいて採用できる
5. **テスト**
   - モデルの予測精度をホールドアウトデータで評価し、RMSE または MAE が基準内かを確認
   - シミュレーションエンジンの決定論的動作を単体テストで検証
6. **ドキュメント**
   - `docs/features/reader_simulation.md` にアルゴリズム概要と利用シナリオを記載

## 8. フェーズ5：リリースと運用詳細

### 8.1 ベータプログラム
- 限定ユーザー（既存のコミュニティおよび招待制）に対してベータ版を提供
- フィードバック収集は専用のフォーム（Typeform または Google Forms）およびアプリ内フィードバックウィジェット
- バグトラッキングは GitHub Issues のラベル `beta-feedback` を使用し、定期的にトリアージミーティングを開催

### 8.2 パフォーマンスチューニング
- バックエンド
  - Gunicorn または Uvicorn のワーカー数を動的に調整（auto-scaling）
  - データベース接続プールのサイズをワークロードに応じてチューニング
  - キャッシュレイヤーの見直し（Redis のメモリ使用量と eviction ポリシー）
- フロントエンド
  - コードスプリッティングの見直し（動的インポートの粒度を調整）
  - 画像とフォントのプリロード戦略
  - Service Worker を用いたオフラインキャッシュとバックグラウンド同期
- インフラストラクチャ
  - Kubernetes (K8s) への移行を検討し、Helm チャートを作成
  - イングレスコントローラーで TLS 終端とレートリミットを実装
  - モニタリングは Prometheus + Grafana、ログは Loki または Elasticsearch

### 8.3 ドキュメントとサポート
- ナレッジベースの整備（FAQ、トラブルシューティングガイド、ベストプラクティス）
- ビデオチュートリアルシリーズ（導入編、上級編、API編）を作成し、YouTube プレイリストに公開
- コミュニティフォーラム（Discord または Discourse）を設置し、ユーザー同士の助け合いを促進
- サポートチケットシステム（Zendesk または Freshdesk）を導入し、エンタープライズ顧客向けに SLA を提供

## 9. リソース計画
| 役割 | 人数 | 主な担当 |
|------|------|----------|
| プロダクトマネージャー | 1 | ロードマップ定義、ステークホルダー調整 |
| バックエンドエンジニア | 2 | API、ワークフロー、インフラ |
| フロントエンドエンジニア | 2 | UI/UX、国際化、コラボレーション |
| データサイエンティスト | 1 | 品質メトリクス、読者シミュレーション |
| DevOps エンジニア | 1 | CI/CD、監視、スケーリング |
| QA エンジニア | 1 | テスト戦略、自動化、ベータ管理 |
| テクニカルライター | 0.5 | ドキュメント作成・維持 |
| デザイナー | 0.5 | UIコンポーネント、アイコン、アニメーション |

合計：8.5 FTE（フルタイム相当）

## 10. リスクと軽減策
| リスク | 影響 | 確率 | 軽減策 |
|--------|------|------|--------|
| AI モデルのコスト増大 | 高 | 中 | 使用量モニタリング、キャッシュ戦略、オープンソースモデルへのフォールバック |
|  internazionale 対応の遅延 | 中 | 中 | 早期に i18n ライブラリを導入し、文字列抽出スクリプトを自動化 |
| リアルタイム共同編集の競合問題 | 高 | 低 | Yjs の実績あるアルゴリズムを採用し、負荷テストを十分に実施 |
| セキュリティ脆弱性（APIキー漏洩等） | 高 | 低 | 認証ミドルウェアの統一、定期的な侵入テスト、依存関係の自動アップデート |
| 開発スピードの低下（技術的負債） | 中 | 中 | リファクタリングスプリントを定期的に設定し、技術的負債を可視化 |
| 法規制違反（著作権、生成物の規制） | 高 | 低 | 生成コンテンツのフィルタリング（SpiceGuard の拡張）および利用規約の明示 |

## 11. 成功指標（KPI）
| 指標 | 目標値 | 測定頻度 |
|------|--------|----------|
| 月間アクティブユーザー (MAU) | 10,000人 | 月次 |
| 有料転換率（ベータから有料プラン） | 5% | 四半期 |
| 生成小説の平均評価（ユーザーレビュー） | 4.2/5.0 | 月次 |
| マルチメディア出力利用率（音声/動画/画像） | 30%のユーザーが少なくとも1種利用 | 月次 |
| 国際化対応言語数 | 5言語（日本語、英語、中国語、韓国語、スペイン語） | リリース時 |
| コラボレーションセッション数 | 1,000セッション/月 | 月次 |
| 品質メトリクス平均スコア | 80点/100点 | 月次 |
| システム稼働率 (Uptime) | 99.9% | 月次 |
| 平均応答時間 (API) | <300ms | 5分間隔 |
| コスト per 生成小説 | <0.05USD | 月次 |

## 12. 次のステップ（即時アクション）
1. この計画書を `plans/` ディレクトリにコピーし、`v4.0_implementation_plan.md` として保存
2. プロダクトマネージャーとアーキテクトでキックオフミーティングを開催し、フェーズ0のタスクをスプリントに落とし込む
3. 依存関係のアップグレード作業を最初のスプリント（2週間）として開始
4. 並行して、音声合成のプロトタイプ（Google TTS の無料枠を使用）を実装し、フロントエンドに簡易プレイヤーを追加
5. 進捗は週次スタンドアップでレビューし、プランの変更が必要になった場合はここで更新する

---
*本計画書は 20,000文字以上を目標に作成しています。実際の文字数は環境により若干変動しますが、概ね目標範囲内に収まっています。*