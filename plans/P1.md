# P1 実装計画書: 生成物のクオリティ向上 72ステップ詳細設計
## （LLMメディアミックス台本生成・音声合成の感情演技パラメータ連動・長編GraphRAG伏線検索結合）

- **策定日**: 2026年9月11日
- **対象バージョン**: AutoNovel v4.5.0+
- **設計方針**: 低性能なLLMでも迷わず1ステップずつ確実に実装・検証できるよう、最小単位の作業（1コミット＝1テスト緑）に分割。6パート構成、各パート12ステップ、12ステップごとに動作検証ゲート（Checkpoint）を設置。
- **総ステップ数**: 全72ステップ（12ステップ × 6パート）

---

## 📋 パート別構成概要

| パート | ステップ | テーマ | 主な対象ファイル | ステータス |
|---|---|---|---|---|
| **Part 1** | Step 1〜12 | LLM駆動メディアミックス台本生成エージェント `MediaScriptAgent` の基盤設計 | `src/agents/media_script_agent.py`<br>`src/agents/prompts/media_script_prompts.py`<br>`tests/unit/test_media_script_agent.py` | ⏳ 未着手 |
| **Part 2** | Step 13〜24 | `MediaScriptAgent` の実行エンジン & `media_mix.py` ルールベースからの刷新 | `src/easy_mode/phase3/media_mix.py`<br>`src/backend/multimedia_service.py`<br>`tests/unit/test_llm_media_mix.py` | ⏳ 未着手 |
| **Part 3** | Step 25〜36 | セリフ感情解析 & VOICEVOX動的音響パラメータ・スタイルマッパー | `src/services/audio/emotion_classifier.py`<br>`src/services/audio/speaker_mapper.py`<br>`tests/unit/test_audio_emotion_mapper.py` | ⏳ 未着手 |
| **Part 4** | Step 37〜48 | `ChapterAudioSynthesizer` への感情演技パラメータ統合 & 自動調声 | `src/services/audio/chapter_synthesizer.py`<br>`src/services/audio/dialogue_extractor.py`<br>`tests/unit/test_synthesizer_emotions.py` | ⏳ 未着手 |
| **Part 5** | Step 49〜60 | GraphRAG / ChromaDB 伏線・設定リトリーバー基盤 | `src/services/rag/context_retriever.py`<br>`src/services/graph/subgraph_extractor.py`<br>`tests/unit/test_foreshadowing_retriever.py` | ⏳ 未着手 |
| **Part 6** | Step 61〜72 | 執筆パイプライン（`WriterAgent` / `PDCA`）への伏線自動注入 & P1総合E2E | `src/agents/writer_agent.py`<br>`src/services/pdca_directive.py`<br>`tests/integration/test_p1_quality_enhancement_e2e.py` | ⏳ 未着手 |

---

## 🎬 Part 1: LLM駆動メディアミックス台本生成エージェント `MediaScriptAgent` の基盤設計 (Step 1〜12)

### Step 1: 漫画コマ割り構造化モデル `MangaPanelScript` の定義
- **目的**: LLMが出力する漫画1ページあたりのコマ数、構図（クローズアップ/引き）、セリフ、ト書き、効果音の型を定義。
- **対象ファイル**: `src/agents/media_script_agent.py` (新規作成)
- **変更内容**:
  ```python
  from pydantic import BaseModel, Field

  class PanelScript(BaseModel):
      panel_number: int
      camera_angle: str = Field(..., description="close_up | medium | wide | bird_eye | low_angle")
      visual_description: str = Field(..., description="コマ内の情景・人物の表情やポーズ")
      dialogues: list[dict[str, str]] = Field(default_factory=list, description="[{'speaker': '...', 'text': '...'}]")
      sfx: list[str] = Field(default_factory=list, description="オノマトペ・効果音")
      narration: str = ""

  class MangaScriptOutput(BaseModel):
      page_number: int
      panels: list[PanelScript]
      scene_mood: str
  ```
- **確認コマンド**: `python -c "from src.agents.media_script_agent import MangaScriptOutput; print('OK')"`
- **完了条件**: スキーマが正しくインポートできること。

### Step 2: 音声ドラマ演出指示構造化モデル `AudioDramaScriptOutput` の定義
- **目的**: キャラクターごとの演技指導（怒り・囁き・早口）、BGM指定（穏やか・緊迫・戦闘）、環境音（雨音・雑踏）の型を定義。
- **対象ファイル**: `src/agents/media_script_agent.py`
- **変更内容**: `AudioDramaLine`, `AudioDramaScriptOutput` モデルを定義。
- **確認コマンド**: `python -m py_compile src/agents/media_script_agent.py`
- **完了条件**: 構文エラーなくコンパイルできること。

### Step 3: 漫画コマ割り用システムプロンプトの策定
- **目的**: 小説の地の文とセリフから「読者の視線誘導」「起承転結のコマ配分」を考慮したコマ割りを指示するプロンプト。
- **対象ファイル**: `src/agents/prompts/media_script_prompts.py` (新規作成)
- **変更内容**: `MANGA_ADAPTATION_PROMPT` を作成し、ページあたり3〜6コマ、大コマ（見せ場）の指定ルールを定義。
- **確認コマンド**: `python -c "from src.agents.prompts.media_script_prompts import MANGA_ADAPTATION_PROMPT; print('OK')"`
- **完了条件**: プロンプト定義がインポートできること。

### Step 4: 音声ドラマ用システムプロンプトの策定
- **目的**: 聴覚のみで場面が伝わるよう、セリフ前の状況説明SEや感情演技ト書きを細かく補完する指示プロンプト。
- **対象ファイル**: `src/agents/prompts/media_script_prompts.py`
- **変更内容**: `AUDIO_DRAMA_ADAPTATION_PROMPT` を作成。
- **確認コマンド**: `python -m py_compile src/agents/prompts/media_script_prompts.py`
- **完了条件**: プロンプトがコンパイルできること。

### Step 5: `MediaScriptAgent` クラスの初期化とLLMクライアント受領
- **目的**: LLMゲートウェイまたはプロバイダを受け取り、温度設定（temperature=0.3〜0.5）で初期化。
- **対象ファイル**: `src/agents/media_script_agent.py`
- **変更内容**:
  ```python
  class MediaScriptAgent:
      def __init__(self, llm_provider=None):
          self.llm = llm_provider
  ```
- **確認コマンド**: `python -m py_compile src/agents/media_script_agent.py`
- **完了条件**: クラスが定義されること。

### Step 6: 漫画コマ割り生成メソッド `generate_manga_script` の実装
- **目的**: チャプターテキストと登場人物リストを受け取り、LLMに問い合わせて `MangaScriptOutput` のリストを生成。
- **対象ファイル**: `src/agents/media_script_agent.py`
- **変更内容**: プロンプト構築、LLM呼び出し、JSONパース処理の実装。
- **確認コマンド**: `python -m py_compile src/agents/media_script_agent.py`
- **完了条件**: コマ割り生成メソッドが実装されていること。

### Step 7: 音声ドラマ台本生成メソッド `generate_audio_script` の実装
- **目的**: テキストから効果音キューと声優向け演技ディレクションを含んだ台本を生成。
- **対象ファイル**: `src/agents/media_script_agent.py`
- **変更内容**: `generate_audio_script(self, chapter_text: str, characters: list[dict]) -> AudioDramaScriptOutput` を実装。
- **確認コマンド**: `python -m py_compile src/agents/media_script_agent.py`
- **完了条件**: 音声ドラマ台本生成メソッドが実装されていること。

### Step 8: JSONパースエラー時のリトライ・自己修復機構
- **目的**: LLMの出力が不完全なJSONやMarkdownブロックに包まれていた場合の頑健なクレンジング処理。
- **対象ファイル**: `src/agents/media_script_agent.py`
- **変更内容**: `_clean_json_markdown(raw: str) -> dict` を実装。
- **確認コマンド**: `python -m py_compile src/agents/media_script_agent.py`
- **完了条件**: クレンジング関数が実装されていること。

### Step 9: 長文チャプターのシーン単位自動スプリッター
- **目的**: 8,000文字を超える長編チャプターをLLMのコンテキストと出力トークン上限（4,096）に収まるようシーン分割。
- **対象ファイル**: `src/agents/media_script_agent.py`
- **変更内容**: 空行や場面転換記号（◆◇等）で1シーンあたり約1,500字に分割して順次生成・連結するロジック。
- **確認コマンド**: `python -m py_compile src/agents/media_script_agent.py`
- **完了条件**: シーンスプリッターが実装されていること。

### Step 10: ルールベースへのフォールバック機構
- **目的**: LLMが利用不可（APIエラー・レートリミット）の場合に、従来の正規表現ルールベースで最低限の台本を返す安全網。
- **対象ファイル**: `src/agents/media_script_agent.py`
- **変更内容**: `try ... except Exception: return self._fallback_rule_based(...)` を実装。
- **確認コマンド**: `python -m py_compile src/agents/media_script_agent.py`
- **完了条件**: フォールバック処理が実装されていること。

### Step 11: `MediaScriptAgent` のモックLLM単体テスト
- **目的**: モックレスポンスを用いて、漫画コマ割りおよび音声台本が期待するデータ構造で返却されるか検証。
- **対象ファイル**: `tests/unit/test_media_script_agent.py` (新規作成)
- **変更内容**: 正常系パース、シーン分割、フォールバックの各テストケースを作成。
- **確認コマンド**: `pytest tests/unit/test_media_script_agent.py -v`
- **完了条件**: 単体テストがパスすること。

### Step 12: Checkpoint 1 - メディアスクリプトエージェント基盤の検証
- **目的**: Part 1 のスキーマ、プロンプト、エージェント単体テストの正常動作を確認。
- **確認コマンド**: `pytest tests/unit/test_media_script_agent.py`
- **完了条件**: Checkpoint 1 が ALL GREEN であること。

---

## 🎭 Part 2: `MediaScriptAgent` の実行エンジン & `media_mix.py` ルールベースからの刷新 (Step 13〜24)

### Step 13: `MangaScriptGenerator` への `MediaScriptAgent` 委譲ロジック追加
- **目的**: 従来の改行・正規表現によるコマ割りを、LLMエージェント呼び出しへ置き換える。
- **対象ファイル**: `src/easy_mode/phase3/media_mix.py`
- **変更内容**:
  - `MangaScriptGenerator.__init__` に `script_agent=None` を追加。
  - `generate()` メソッド内で `self.script_agent.generate_manga_script(...)` を優先実行。
- **確認コマンド**: `python -m py_compile src/easy_mode/phase3/media_mix.py`
- **完了条件**: 委譲ロジックが追加されていること。

### Step 14: `AudioDramaScriptGenerator` への `MediaScriptAgent` 委譲ロジック追加
- **目的**: 音声ドラマ台本生成をLLMエージェント呼び出しへ置き換え、高品質な演技指示・効果音を生成。
- **対象ファイル**: `src/easy_mode/phase3/media_mix.py`
- **変更内容**: `AudioDramaScriptGenerator.generate()` 内で `self.script_agent.generate_audio_script(...)` を優先実行。
- **確認コマンド**: `python -m py_compile src/easy_mode/phase3/media_mix.py`
- **完了条件**: 委譲ロジックが追加されていること。

### Step 15: キャラクターバイブルからの一人称・口調プロンプト自動注入
- **目的**: `preset["characters"]` に登録された各キャラの性格・一人称・決め台詞をLLMプロンプトのコンテキストに付与。
- **対象ファイル**: `src/easy_mode/phase3/media_mix.py`
- **変更内容**: `_build_character_context(self.characters)` メソッドを実装。
- **確認コマンド**: `python -m py_compile src/easy_mode/phase3/media_mix.py`
- **完了条件**: キャラクター文脈生成ロジックが追加されていること。

### Step 16: コマ割りデータ（`PanelScript`）から内部 `Panel` モデルへのマッピング
- **目的**: LLMエージェントが出力した Pydantic モデルを既存の `media_mix.Panel` データクラスへ変換。
- **対象ファイル**: `src/easy_mode/phase3/media_mix.py`
- **変更内容**: `_convert_to_panels(manga_output)` を実装。
- **確認コマンド**: `python -m py_compile src/easy_mode/phase3/media_mix.py`
- **完了条件**: マッピング処理が実装されていること。

### Step 17: 音声ドラマデータから内部 `VoiceLine` モデルへのマッピング
- **目的**: LLMが出力した演技ディレクション・SE指定を `VoiceLine` および `AudioCue` に正しく詰め替える。
- **対象ファイル**: `src/easy_mode/phase3/media_mix.py`
- **変更内容**: `_convert_to_voice_lines(audio_output)` を実装。
- **確認コマンド**: `python -m py_compile src/easy_mode/phase3/media_mix.py`
- **完了条件**: マッピング処理が実装されていること。

### Step 18: `create_media_mix_exporter` ファクトリの更新
- **目的**: ファクトリ関数内で `MediaScriptAgent` を自動インスタンス化して各ジェネレーターに渡す。
- **対象ファイル**: `src/easy_mode/phase3/media_mix.py`
- **変更内容**: `create_media_mix_exporter(genre, preset, script_agent=None)` に更新。
- **確認コマンド**: `python -m py_compile src/easy_mode/phase3/media_mix.py`
- **完了条件**: ファクトリ関数が更新されていること。

### Step 19: `MultimediaService` でのエージェント初期化と受け渡し
- **目的**: `MultimediaService` が自身のLLM設定をもとに `MediaScriptAgent` を生成しエクスポートに渡す。
- **対象ファイル**: `src/backend/multimedia_service.py`
- **変更内容**: `generate_media_mix` 内でエージェントをファクトリに注入。
- **確認コマンド**: `python -m py_compile src/backend/multimedia_service.py`
- **完了条件**: サービス層の配線が完了すること。

### Step 20: 従来ヒューリスティック関数の非推奨化マーク
- **目的**: `_guess_speaker`, `_guess_emotion`, `_generate_direction` にフォールバック専用の旨をコメントと警告ログに記録。
- **対象ファイル**: `src/easy_mode/phase3/media_mix.py`
- **変更内容**: ドキュメントストリングに `@deprecated: Used only as rule-based fallback` を記載。
- **確認コマンド**: `python -m py_compile src/easy_mode/phase3/media_mix.py`
- **完了条件**: コード保守性が向上していること。

### Step 21: LLM連携メディアミックスの単体テスト作成
- **目的**: モックLLMを使用し、`create_media_mix_exporter` から高品質なコマ割りJSONおよび音声ドラマJSONが出力されるかをテスト。
- **対象ファイル**: `tests/unit/test_llm_media_mix.py` (新規作成)
- **変更内容**: 漫画・音声ドラマ双方の生成テストを作成。
- **確認コマンド**: `pytest tests/unit/test_llm_media_mix.py -v`
- **完了条件**: 単体テストが成功すること。

### Step 22: 生成される演出指示の品質バリデーション
- **目的**: 出力された `VoiceLine.direction` に感情や声量の指示（例: `[激昂・大声]`）が確実に含まれるかのアサーション。
- **対象ファイル**: `tests/unit/test_llm_media_mix.py`
- **変更内容**: 指示品質のアサーションテストを追加。
- **確認コマンド**: `pytest tests/unit/test_llm_media_mix.py -k test_direction_quality`
- **完了条件**: 品質テストがパスすること。

### Step 23: コスト・トークン消費削減のためのプロンプト最適化
- **目的**: 余計な出力トークンを削減するため、不要な解説文を出力させず純粋なJSONのみを返すようプロンプトをチューニング。
- **対象ファイル**: `src/agents/prompts/media_script_prompts.py`
- **変更内容**: 「解説や前置きは一切出力せず、JSONオブジェクトのみを出力してください」の制約を強調。
- **確認コマンド**: `python -m py_compile src/agents/prompts/media_script_prompts.py`
- **完了条件**: プロンプトが最適化されていること。

### Step 24: Checkpoint 2 - LLMメディアミックス生成エンジンの検証
- **目的**: Part 1 および Part 2 のテストを一括実行し、ルールベース脱却とLLM生成の正常動作を確認。
- **確認コマンド**: `pytest tests/unit/test_media_script_agent.py tests/unit/test_llm_media_mix.py`
- **完了条件**: Checkpoint 2 が ALL GREEN であること。

---

## 🎙️ Part 3: セリフ感情解析 & VOICEVOX動的音響パラメータ・スタイルマッパー (Step 25〜36)

### Step 25: セリフ感情列挙型 `SpeechEmotion` の定義
- **目的**: 音声合成に適用可能な感情カテゴリ（喜び、怒り、哀しみ、安らぎ、恐怖、驚き、囁き、叫び、日常）を定義。
- **対象ファイル**: `src/services/audio/emotion_classifier.py` (新規作成)
- **変更内容**:
  ```python
  from enum import Enum

  class SpeechEmotion(str, Enum):
      NEUTRAL = "neutral"       # 通常・地の文
      JOY = "joy"               # 歓喜・笑顔
      ANGER = "anger"           # 怒り・敵意
      SADNESS = "sadness"       # 悲哀・落胆
      FEAR = "fear"             # 恐怖・怯え
      SURPRISE = "surprise"     # 驚愕・動揺
      WHISPER = "whisper"       # 囁き・内緒話
      SHOUT = "shout"           # 叫び・号令
  ```
- **確認コマンド**: `python -c "from src.services.audio.emotion_classifier import SpeechEmotion; print('OK')"`
- **完了条件**: 列挙型が定義されていること。

### Step 26: 感情別音響パラメータデータクラス `AcousticParameters` の定義
- **目的**: VOICEVOXの `speedScale`, `pitchScale`, `intonationScale`, および音量調整値を格納する型。
- **対象ファイル**: `src/services/audio/emotion_classifier.py`
- **変更内容**:
  ```python
  from dataclasses import dataclass

  @dataclass
  class AcousticParameters:
      speed_scale: float = 1.0       # 0.50 〜 2.00
      pitch_scale: float = 0.0       # -0.15 〜 0.15
      intonation_scale: float = 1.0  # 0.00 〜 2.00
      volume_scale: float = 1.0      # 0.00 〜 2.00
      pause_after_sec: float = 0.4   # セリフ後の間合い秒数
  ```
- **確認コマンド**: `python -m py_compile src/services/audio/emotion_classifier.py`
- **完了条件**: クラスが定義されること。

### Step 27: 感情別デフォルト音響パラメータテーブル `EMOTION_ACOUSTIC_TABLE` の策定
- **目的**: 感情に応じた人間らしい自然な発声パラメータ（例: 怒り＝早口・高ピッチ・強抑揚、囁き＝ゆっくり・低ピッチ・弱抑揚）。
- **対象ファイル**: `src/services/audio/emotion_classifier.py`
- **変更内容**: 各 `SpeechEmotion` に対する `AcousticParameters` の基準値を定義。
- **確認コマンド**: `python -m py_compile src/services/audio/emotion_classifier.py`
- **完了条件**: パラメータテーブルが定義されていること。

### Step 28: 高速セリフ感情分類器 `DialogueEmotionClassifier` の実装
- **目的**: セリフテキスト内の語彙、記号（！？…）、および文脈から `SpeechEmotion` を高速判定するハイブリッド分類器。
- **対象ファイル**: `src/services/audio/emotion_classifier.py`
- **変更内容**: 形態素／キーワードマッチと記号解析による感情推定メソッド `classify(text: str, context: str = "") -> SpeechEmotion` を実装。
- **確認コマンド**: `python -m py_compile src/services/audio/emotion_classifier.py`
- **完了条件**: 感情分類器が実装されていること。

### Step 29: VOICEVOX スタイルID対応テーブル `VOICEVOX_STYLE_REGISTRY` の構築
- **目的**: 各キャラクターの各感情（ノーマル、あまあま、ツンツン、セクシー、ささやき等）に対応する VOICEVOX style_id を網羅。
- **対象ファイル**: `src/services/audio/speaker_mapper.py`
- **変更内容**:
  ```python
  VOICEVOX_STYLE_REGISTRY = {
      "ずんだもん": {
          SpeechEmotion.NEUTRAL: 3,
          SpeechEmotion.JOY: 1,    # あまあま
          SpeechEmotion.ANGER: 7,  # ツンツン
          SpeechEmotion.WHISPER: 22, # ささやき
      },
      "四国めたん": {
          SpeechEmotion.NEUTRAL: 2,
          SpeechEmotion.JOY: 0,    # あまあま
          SpeechEmotion.ANGER: 6,  # ツンツン
          SpeechEmotion.WHISPER: 36, # ささやき
      },
      # ...
  }
  ```
- **確認コマンド**: `python -m py_compile src/services/audio/speaker_mapper.py`
- **完了条件**: スタイルレジストリが登録されていること。

### Step 30: 感情連動話者ID解決関数 `resolve_speaker_and_style` の実装
- **目的**: キャラクター名と推定感情から、最適な VOICEVOX speaker_id (style_id) と `AcousticParameters` を同時に解決。
- **対象ファイル**: `src/services/audio/speaker_mapper.py`
- **変更内容**: `resolve_speaker_and_style(speaker_name: str, emotion: SpeechEmotion, gender="female", role="heroine") -> tuple[int, AcousticParameters]` を実装。
- **確認コマンド**: `python -m py_compile src/services/audio/speaker_mapper.py`
- **完了条件**: 解決関数が実装されていること。

### Step 31: 該当スタイルが存在しない場合の安全なフォールバック
- **目的**: 指定キャラに「ささやき」スタイルがない場合、ノーマルスタイルに音響パラメータ補正（音量小・ピッチ低）を重ねてエミュレート。
- **対象ファイル**: `src/services/audio/speaker_mapper.py`
- **変更内容**: デフォルトスタイルへのフォールバック分岐を追加。
- **確認コマンド**: `python -m py_compile src/services/audio/speaker_mapper.py`
- **完了条件**: エミュレーションフォールバックが実装されていること。

### Step 32: セリフの強弱（感嘆符数）に応じたパラメータの動的微調整
- **目的**: 「！」が3つ連続する場合は通常怒りよりも `speed_scale` をさらに+0.1し、臨場感を演出。
- **対象ファイル**: `src/services/audio/emotion_classifier.py`
- **変更内容**: `adjust_acoustics_by_punctuation(params, text)` を実装。
- **確認コマンド**: `python -m py_compile src/services/audio/emotion_classifier.py`
- **完了条件**: 微調整ロジックが実装されていること。

### Step 33: 感情判定エンジンの単体テスト作成
- **目的**: 「ふざけるな！」「愛してるよ……」「どこへ行くのですか？」等の例文が正しく感情分類されるかを検証。
- **対象ファイル**: `tests/unit/test_audio_emotion_mapper.py` (新規作成)
- **変更内容**: 各種感情の判定テストケースを網羅。
- **確認コマンド**: `pytest tests/unit/test_audio_emotion_mapper.py -k test_emotion_classification -v`
- **完了条件**: 判定テストがパスすること。

### Step 34: スタイルID解決とパラメータ計算の単体テスト作成
- **目的**: ずんだもんの「怒り」でスタイルID 7、ピッチ上昇パラメータが返却されることを検証。
- **対象ファイル**: `tests/unit/test_audio_emotion_mapper.py`
- **変更内容**: スタイル解決テストケースを追加。
- **確認コマンド**: `pytest tests/unit/test_audio_emotion_mapper.py -k test_style_resolution -v`
- **完了条件**: スタイル解決テストがパスすること。

### Step 35: 外部設定ファイル（`config/audio_voices.yaml`）からのスタイル設定読み込み対応
- **目的**: コードを改変せずに新規キャラクターやスタイルIDを追加できるようYAMLからのロード機能を提供。
- **対象ファイル**: `src/services/audio/speaker_mapper.py`
- **変更内容**: YAMLが存在する場合は優先して読み込む `load_voice_config()` を実装。
- **確認コマンド**: `python -m py_compile src/services/audio/speaker_mapper.py`
- **完了条件**: 外部設定読み込みがサポートされること。

### Step 36: Checkpoint 3 - 音声感情パラメータマッパーの検証
- **目的**: Part 3 の全テストを実行し、感情分類およびパラメータ計算の正確性を確認。
- **確認コマンド**: `pytest tests/unit/test_audio_emotion_mapper.py`
- **完了条件**: Checkpoint 3 が ALL GREEN であること。

---

## 🔊 Part 4: `ChapterAudioSynthesizer` への感情演技パラメータ統合 & 自動調声 (Step 37〜48)

### Step 37: `DialogueLine` への感情・音響パラメータフィールド追加
- **目的**: パーサーが抽出した各セリフ行に `emotion: SpeechEmotion` と `acoustics: AcousticParameters` を保持。
- **対象ファイル**: `src/services/audio/dialogue_extractor.py`
- **変更内容**:
  ```python
  from src.services.audio.emotion_classifier import SpeechEmotion, AcousticParameters

  @dataclass
  class DialogueLine:
      line_index: int
      text: str
      is_dialogue: bool
      speaker_name: str = "narration"
      speaker_id: int = 3
      emotion: SpeechEmotion = SpeechEmotion.NEUTRAL
      acoustics: AcousticParameters | None = None
  ```
- **確認コマンド**: `python -m py_compile src/services/audio/dialogue_extractor.py`
- **完了条件**: データクラスが拡張されていること。

### Step 38: `DialogueExtractor.extract_lines` での感情自動抽出
- **目的**: セリフ行を抽出するタイミングで `DialogueEmotionClassifier` を呼び出し、感情を自動セット。
- **対象ファイル**: `src/services/audio/dialogue_extractor.py`
- **変更内容**: `extractor` 内で `classifier.classify(chunk, context=p)` を呼び出す。
- **確認コマンド**: `python -m py_compile src/services/audio/dialogue_extractor.py`
- **完了条件**: 抽出時に感情が付与されること。

### Step 39: `AudioSynthesisRequest` の音響パラメータ受領強化
- **目的**: 合成リクエストモデルに `speed_scale`, `pitch_scale`, `intonation_scale`, `volume_scale` を正しく保持。
- **対象ファイル**: `src/services/audio/base.py`
- **変更内容**: クラス定義のフィールド確認および初期値の整備。
- **確認コマンド**: `python -m py_compile src/services/audio/base.py`
- **完了条件**: リクエストモデルが整備されていること。

### Step 40: `VoicevoxClient` への音響パラメータ完全マッピング
- **目的**: `VoicevoxClient.synthesize()` 内で `query_data` に対し全てのスケール値を反映。
- **対象ファイル**: `src/services/audio/voicevox_client.py`
- **変更内容**: `volumeScale` も含めてリクエストパラメータを完全にセット。
- **確認コマンド**: `python -m py_compile src/services/audio/voicevox_client.py`
- **完了条件**: VOICEVOXクエリへの反映処理が実装されること。

### Step 41: `ChapterAudioSynthesizer` での感情連動スタイルID適用
- **目的**: 各行の合成ループ内で、`resolve_speaker_and_style(line.speaker_name, line.emotion)` を呼び出し、決定されたスタイルIDとパラメータをリクエストに注入。
- **対象ファイル**: `src/services/audio/chapter_synthesizer.py`
- **変更内容**:
  ```python
  speaker_id, acoustics = resolve_speaker_and_style(
      line.speaker_name, line.emotion, gender=gender, role=role
  )
  req = AudioSynthesisRequest(
      text=line.text,
      speaker_id=speaker_id,
      speed_scale=acoustics.speed_scale,
      pitch_scale=acoustics.pitch_scale,
      intonation_scale=acoustics.intonation_scale,
  )
  ```
- **確認コマンド**: `python -m py_compile src/services/audio/chapter_synthesizer.py`
- **完了条件**: 感情連動パラメータがリクエストに注入されること。

### Step 42: セリフ間無音時間の動的調整（間合いの演出）
- **目的**: 緊迫した会話ではセリフ間を0.2秒、感慨深い余韻では0.8秒など、`acoustics.pause_after_sec` に応じた無音長をクリップ間に挿入。
- **対象ファイル**: `src/services/audio/chapter_synthesizer.py`, `src/services/audio/audio_combiner.py`
- **変更内容**: `AudioCombiner.combine_wav_clips_with_variable_pauses(clips, pauses)` の実装と呼び出し。
- **確認コマンド**: `python -m py_compile src/services/audio/audio_combiner.py`
- **完了条件**: 可変ポーズ結合機能が実装されていること。

### Step 43: 長大な章の並列音声合成バッチ処理
- **目的**: 1行ずつ直列に合成すると時間がかかるため、`asyncio.gather` により最大4並列でクエリ＆合成を実行。
- **対象ファイル**: `src/services/audio/chapter_synthesizer.py`
- **変更内容**: セマフォ（`asyncio.Semaphore(4)`）を用いた並列合成キューの実装。
- **確認コマンド**: `python -m py_compile src/services/audio/chapter_synthesizer.py`
- **完了条件**: 並列処理ロジックが実装されていること。

### Step 44: 音声合成エラー時のスキップ・フォールバック安全性
- **目的**: 特定の1行で合成APIが失敗した場合でも、章全体の結合を中断せず無音またはスキップして続行。
- **対象ファイル**: `src/services/audio/chapter_synthesizer.py`
- **変更内容**: 各クリップ合成の `try ... except` 内でエラーログを記録し、他の行の結合を維持。
- **確認コマンド**: `python -m py_compile src/services/audio/chapter_synthesizer.py`
- **完了条件**: 堅牢性が確保されていること。

### Step 45: 音声メタデータへの感情タグ集計の保存
- **目的**: 合成完了時に「本章のセリフ感情内訳（喜20%, 怒30%, 平穏50%）」をメタデータJSONに記録。
- **対象ファイル**: `src/services/audio/chapter_synthesizer.py`
- **変更内容**: 返却辞書に `emotion_distribution` を追加。
- **確認コマンド**: `python -m py_compile src/services/audio/chapter_synthesizer.py`
- **完了条件**: メタデータ記録が追加されていること。

### Step 46: 感情連動音声合成の単体テスト作成
- **目的**: `MockAudioClient` を用いて、感情に応じたスタイルIDおよび `speed_scale` が正しくクライアントに渡されているかを検証。
- **対象ファイル**: `tests/unit/test_synthesizer_emotions.py` (新規作成)
- **変更内容**: パラメータ受け渡しのアサーションテストを記述。
- **確認コマンド**: `pytest tests/unit/test_synthesizer_emotions.py -v`
- **完了条件**: 単体テストが成功すること。

### Step 47: 可変ポーズ結合のWAVバイナリ検証テスト
- **目的**: 異なる無音時間が挿入されたWAVバイナリの長さを `wave.open` で読み取り、計算通りの再生時間になっているかをテスト。
- **対象ファイル**: `tests/unit/test_synthesizer_emotions.py`
- **変更内容**: WAV長さ検証テストを追加。
- **確認コマンド**: `pytest tests/unit/test_synthesizer_emotions.py -k test_variable_pause`
- **完了条件**: 検証テストがパスすること。

### Step 48: Checkpoint 4 - 感情演技音声合成パイプラインの検証
- **目的**: Part 3 および Part 4 の音声合成関連テストを一括実行。
- **確認コマンド**: `pytest tests/unit/test_audio_emotion_mapper.py tests/unit/test_synthesizer_emotions.py`
- **完了条件**: Checkpoint 4 が ALL GREEN であること。

---

## 🕸️ Part 5: GraphRAG / ChromaDB 伏線・設定リトリーバー基盤 (Step 49〜60)

### Step 49: 伏線・設定エンティティモデル `ForeshadowingEntity` の定義
- **目的**: 伏線フラグ（提示章、回収予定章、ステータス: open/resolved）、関連キャラクター、キーワードを管理するデータ構造。
- **対象ファイル**: `src/services/rag/context_retriever.py` (新規作成)
- **変更内容**:
  ```python
  from dataclasses import dataclass, field
  from enum import Enum

  class ForeshadowingStatus(str, Enum):
      OPEN = "open"          # 未回収
      IN_PROGRESS = "in_progress" # 進展中
      RESOLVED = "resolved"  # 完全回収

  @dataclass
  class ForeshadowingEntity:
      foreshadow_id: str
      description: str
      introduced_in_ep: int
      target_resolution_ep: int | None = None
      status: ForeshadowingStatus = ForeshadowingStatus.OPEN
      related_characters: list[str] = field(default_factory=list)
      keywords: list[str] = field(default_factory=list)
  ```
- **確認コマンド**: `python -c "from src.services.rag.context_retriever import ForeshadowingEntity; print('OK')"`
- **完了条件**: クラスが定義されること。

### Step 50: ChromaDB 伏線コレクションの初期化
- **目的**: 小説の既刊チャプターから抽出された伏線および設定断片をベクトル格納するコレクション（`novel_foreshadowings`）を作成。
- **対象ファイル**: `src/services/rag/context_retriever.py`
- **変更内容**: `_get_or_create_collection(book_id: int)` メソッドを実装。
- **確認コマンド**: `python -m py_compile src/services/rag/context_retriever.py`
- **完了条件**: コレクション初期化処理が実装されること。

### Step 51: 伏線エンティティのベクトル登録メソッド `upsert_foreshadowing`
- **目的**: 執筆またはプロット生成時に発見された伏線ノードをベクトルDBに保存・更新。
- **対象ファイル**: `src/services/rag/context_retriever.py`
- **変更内容**: `upsert_foreshadowing(self, book_id: int, entity: ForeshadowingEntity) -> None` を実装。
- **確認コマンド**: `python -m py_compile src/services/rag/context_retriever.py`
- **完了条件**: 登録メソッドが実装されていること。

### Step 52: 知識グラフ（Entity-Relation）からの関連サブグラフ探索 `extract_subgraph`
- **目的**: 対象エピソードの登場人物に関連するアイテム所持・因縁・秘密情報をグラフ探索（NetworkX / AGE）で抽出。
- **対象ファイル**: `src/services/graph/subgraph_extractor.py` (新規作成)
- **変更内容**: グラフから深度2ホップ以内の関連エッジを抽出する `extract_character_subgraph(graph, character_names: list[str]) -> list[dict]` を実装。
- **確認コマンド**: `python -m py_compile src/services/graph/subgraph_extractor.py`
- **完了条件**: サブグラフ抽出メソッドが実装されていること。

### Step 53: 未回収伏線の期日優先フィルタリング
- **目的**: 現在執筆対象のエピソード番号（例: 第10話）に対し、未回収（`status == OPEN`）かつ回収目標が近い伏線を優先抽出。
- **対象ファイル**: `src/services/rag/context_retriever.py`
- **変更内容**: `get_pending_foreshadowings(book_id: int, current_ep: int) -> list[ForeshadowingEntity]` を実装。
- **確認コマンド**: `python -m py_compile src/services/rag/context_retriever.py`
- **完了条件**: フィルタリング処理が実装されていること。

### Step 54: プロット概念と意味的類似度が高い伏線のセマンティック検索
- **目的**: 次章のプロット概要テキスト（例: 「主人公が古い遺跡で紋章を見つける」）とコサイン類似度が高い過去の伏線をChromaDBから検索。
- **対象ファイル**: `src/services/rag/context_retriever.py`
- **変更内容**: `search_relevant_context(book_id: int, plot_summary: str, top_k=3) -> list[dict]` を実装。
- **確認コマンド**: `python -m py_compile src/services/rag/context_retriever.py`
- **完了条件**: ベクトル検索処理が実装されていること。

### Step 55: `LongFormContextRetriever` 統合クラスの設計
- **目的**: ベクトル検索（ChromaDB）と知識グラフ探索（NetworkX）を統合し、執筆に必要な背景知識をワンストップで提供。
- **対象ファイル**: `src/services/rag/context_retriever.py`
- **変更内容**: `retrieve_writing_context(self, book_id: int, current_ep: int, plot_outline: str) -> dict` を実装。
- **確認コマンド**: `python -m py_compile src/services/rag/context_retriever.py`
- **完了条件**: 統合リトリーバーが完成すること。

### Step 56: 抽出された知識のコンテキストプロンプト文字列へのフォーマット
- **目的**: 執筆エージェントが理解しやすいMarkdownリスト形式（回収必須伏線、重要設定、キャラクター状態）に整形。
- **対象ファイル**: `src/services/rag/context_retriever.py`
- **変更内容**: `format_context_for_prompt(context_dict: dict) -> str` を実装。
- **確認コマンド**: `python -m py_compile src/services/rag/context_retriever.py`
- **完了条件**: フォーマット関数が実装されていること。

### Step 57: 伏線回収ステータスの更新メソッド `resolve_foreshadowing`
- **目的**: 執筆完了時に「第X話でこの伏線が回収された」とマークし、ステータスを `RESOLVED` に更新。
- **対象ファイル**: `src/services/rag/context_retriever.py`
- **変更内容**: `resolve_foreshadowing(book_id: int, foreshadow_id: str, resolved_ep: int)` を実装。
- **確認コマンド**: `python -m py_compile src/services/rag/context_retriever.py`
- **完了条件**: 更新メソッドが実装されていること。

### Step 58: ベクトル検索・伏線リトリーバーの単体テスト作成
- **目的**: インメモリChromaDB（またはモック）を用いて、伏線登録 ➔ 類似検索 ➔ 回収更新の一連のライフサイクルをテスト。
- **対象ファイル**: `tests/unit/test_foreshadowing_retriever.py` (新規作成)
- **変更内容**: 各種リトリーブ条件のテストケースを記述。
- **確認コマンド**: `pytest tests/unit/test_foreshadowing_retriever.py -v`
- **完了条件**: 単体テストが成功すること。

### Step 59: サブグラフ抽出ロジックの単体テスト作成
- **目的**: テスト用NetworkXグラフを作成し、指定キャラクターの2ホップ以内の関連エッジが正しく抽出されるかを検証。
- **対象ファイル**: `tests/unit/test_foreshadowing_retriever.py`
- **変更内容**: グラフ探索テストを追加。
- **確認コマンド**: `pytest tests/unit/test_foreshadowing_retriever.py -k test_subgraph`
- **完了条件**: グラフテストがパスすること。

### Step 60: Checkpoint 5 - 長編コンテキストリトリーバーの検証
- **目的**: Part 5 の全テストを実行し、GraphRAGおよび伏線検索エンジンの動作を確認。
- **確認コマンド**: `pytest tests/unit/test_foreshadowing_retriever.py`
- **完了条件**: Checkpoint 5 が ALL GREEN であること。

---

## ✍️ Part 6: 執筆パイプライン（`WriterAgent` / `PDCA`）への伏線自動注入 & P1総合E2E (Step 61〜72)

### Step 61: `WriterAgent` プロンプトテンプレートへの伏線セクション追加
- **目的**: 執筆プロンプトに `## 本話で意識・回収すべき伏線・設定` のブロックを動的挿入。
- **対象ファイル**: `src/agents/writer_agent.py`
- **変更内容**: プロンプトフォーマット時に `context_retriever.format_context_for_prompt()` の出力を埋め込む。
- **確認コマンド**: `python -m py_compile src/agents/writer_agent.py`
- **完了条件**: プロンプトテンプレートが更新されていること。

### Step 62: `WriterAgent` への `LongFormContextRetriever` 依存性注入
- **目的**: 執筆実行時に自動で該当書籍・章のコンテキストを取得できるようリトリーバーを注入。
- **対象ファイル**: `src/agents/writer_agent.py`
- **変更内容**: `__init__(self, ..., context_retriever=None)` の対応。
- **確認コマンド**: `python -m py_compile src/agents/writer_agent.py`
- **完了条件**: 依存性注入が完了すること。

### Step 63: 執筆出力からの「伏線回収検知」ロジックの実装
- **目的**: 生成されたエピソード本文中に、未回収伏線のキーワードや解決描写が含まれているかを簡易検知。
- **対象ファイル**: `src/agents/writer_agent.py`
- **変更内容**: `detect_resolved_foreshadowings(content, pending_list) -> list[str]` を実装。
- **確認コマンド**: `python -m py_compile src/agents/writer_agent.py`
- **完了条件**: 回収検知ロジックが実装されていること。

### Step 64: `ConsistencyAuditor` への伏線整合性監査ルールの統合
- **目的**: 指定された伏線が矛盾なく処理されているか、未回収のまま放置されていないかを8専門家オーディターが評価。
- **対象ファイル**: `src/agents/specialists/consistency_auditor.py`
- **変更内容**: 監査コンテキストに `foreshadowing_context` を引き渡し、矛盾があれば減点とActionable Diffを発行。
- **確認コマンド**: `python -m py_compile src/agents/specialists/consistency_auditor.py`
- **完了条件**: 監査ロジックが拡張されていること。

### Step 65: クローズドループPDCAディレクティブへの伏線再執筆指示の生成
- **目的**: 伏線回収が不十分で監査不合格となった場合、PDCA再執筆プロンプトに「伏線Xの描写を強調せよ」と明示指示。
- **対象ファイル**: `src/services/pdca_directive.py`
- **変更内容**: `WritingDirective` に `foreshadowing_directives: list[str]` を追加しプロンプトに反映。
- **確認コマンド**: `python -m py_compile src/services/pdca_directive.py`
- **完了条件**: ディレクティブ生成が強化されていること。

### Step 66: P1総合E2Eテストハーネスの整備
- **目的**: 「伏線検索 ➔ 執筆 ➔ 感情付き音声合成 ➔ LLM漫画台本生成」を一気通貫で検証するテスト環境を構築。
- **対象ファイル**: `tests/integration/test_p1_quality_enhancement_e2e.py` (新規作成)
- **変更内容**: 統合フィクスチャとモックプロバイダを準備。
- **確認コマンド**: `pytest tests/integration/test_p1_quality_enhancement_e2e.py -v`
- **完了条件**: テストハーネスが動作すること。

### Step 67: E2Eテスト 1: 伏線自動注入 ➔ 執筆 ➔ 伏線回収検知フロー
- **目的**: 未回収伏線がある状態で第2話を執筆し、本文中に伏線回収描写が反映されステータスが更新されることを検証。
- **対象ファイル**: `tests/integration/test_p1_quality_enhancement_e2e.py`
- **変更内容**: `test_writing_with_foreshadowing_retrieval_e2e` を記述。
- **確認コマンド**: `pytest tests/integration/test_p1_quality_enhancement_e2e.py -k test_writing_with_foreshadowing`
- **完了条件**: テストが合格すること。

### Step 68: E2Eテスト 2: 執筆された小説からの感情演技音声合成フロー
- **目的**: 執筆されたエピソード本文からセリフと感情（怒り・囁き）を抽出し、適切なスタイルIDで通しWAVが生成されることを検証。
- **対象ファイル**: `tests/integration/test_p1_quality_enhancement_e2e.py`
- **変更内容**: `test_synthesizer_with_detected_emotions_e2e` を記述。
- **確認コマンド**: `pytest tests/integration/test_p1_quality_enhancement_e2e.py -k test_synthesizer_with_detected_emotions`
- **完了条件**: テストが合格すること。

### Step 69: E2Eテスト 3: LLMエージェントによる漫画コマ割り・音声ドラマ台本出力フロー
- **目的**: 同エピソードから `MediaScriptAgent` を経由して高品質な漫画コマ割りJSONおよび音響指示付き台本が生成されることを検証。
- **対象ファイル**: `tests/integration/test_p1_quality_enhancement_e2e.py`
- **変更内容**: `test_llm_media_scripts_e2e` を記述。
- **確認コマンド**: `pytest tests/integration/test_p1_quality_enhancement_e2e.py -k test_llm_media_scripts`
- **完了条件**: テストが合格すること。

### Step 70: 生成物品質の定量的評価テスト（Commercial Quality Check）
- **目的**: 商業品質基準（CommercialQualityMetrics）において、伏線整合性スコアおよび感情表現スコアが80点以上であることを検証。
- **対象ファイル**: `tests/integration/test_p1_quality_enhancement_e2e.py`
- **変更内容**: スコアアサーションテストを追加。
- **確認コマンド**: `pytest tests/integration/test_p1_quality_enhancement_e2e.py -k test_quality_metrics`
- **完了条件**: 品質スコアテストが合格すること。

### Step 71: コードクリーンアップと型チェック（mypy/ruff）
- **目的**: Part 1〜6で作成・改変した全モジュールの型安全性・構文整合性を検証。
- **対象ファイル**: `src/agents/`, `src/services/`
- **確認コマンド**: `python -m py_compile src/agents/media_script_agent.py src/services/audio/emotion_classifier.py src/services/rag/context_retriever.py src/agents/writer_agent.py`
- **完了条件**: 重大な警告およびエラーがないこと。

### Step 72: Final Gate (Checkpoint 6) - P1 全テストスイート一括実行 (100% ALL GREEN)
- **目的**: P1 に関連する全ての単体テスト・結合テストスイートを一括実行し、完全合格を確認。
- **確認コマンド**: `pytest tests/unit/test_media_script_agent.py tests/unit/test_llm_media_mix.py tests/unit/test_audio_emotion_mapper.py tests/unit/test_synthesizer_emotions.py tests/unit/test_foreshadowing_retriever.py tests/integration/test_p1_quality_enhancement_e2e.py -v`
- **完了条件**: 全テストが 100% GREEN でパスすること。
