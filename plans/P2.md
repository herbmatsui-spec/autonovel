# P2 実装計画書: アーキテクチャ・UXの洗練 72ステップ詳細設計
## （統一LLMインターフェース一本化・リアルタイムPDCA/DAG進捗可視化・小説投稿サイト最適化フォーマッター）

- **策定日**: 2026年9月11日
- **対象バージョン**: AutoNovel v4.5.0+
- **設計方針**: 低性能なLLMでも迷わず1ステップずつ確実に実装・検証できるよう、最小単位の作業（1コミット＝1テスト緑）に分割。6パート構成、各パート12ステップ、12ステップごとに動作検証ゲート（Checkpoint）を設置。
- **総ステップ数**: 全72ステップ（12ステップ × 6パート）

---

## 📋 パート別構成概要

| パート | ステップ | テーマ | 主な対象ファイル | ステータス |
|---|---|---|---|---|
| **Part 1** | Step 1〜12 | 統一LLMインターフェース規格 `IUnifiedLLMClient` の策定 | `src/core/llm/unified_interface.py`<br>`src/core/llm/types.py`<br>`tests/unit/test_unified_llm_interface.py` | ⏳ 未着手 |
| **Part 2** | Step 13〜24 | プロバイダアダプタ移行 & `SpecialistAuditor` / `WriterAgent` のリフレクション撤廃 | `src/core/llm/adapters/*.py`<br>`src/agents/specialist_auditor_base.py`<br>`tests/unit/test_unified_auditor_llm.py` | ⏳ 未着手 |
| **Part 3** | Step 25〜36 | 執筆・PDCA・DAGイベントバス & WebSocket/SSEストリーミングサーバー | `src/backend/websocket/pipeline_hub.py`<br>`src/backend/routers/pipeline_stream.py`<br>`tests/unit/test_pipeline_websocket.py` | ⏳ 未着手 |
| **Part 4** | Step 37〜48 | フロントエンドリアルタイム進捗コンポーネント & スコア改善可視化 | `frontend/src/hooks/usePipelineWebSocket.ts`<br>`frontend/src/components/studio/PDCALiveMonitor.tsx`<br>`frontend/src/components/studio/DAGLiveTracker.tsx` | ⏳ 未着手 |
| **Part 5** | Step 49〜60 | プラットフォーム別フォーマッター（なろう・カクヨム・KDP）設計 | `src/services/formatters/platform_formatter.py`<br>`src/services/formatters/ruby_transpiler.py`<br>`tests/unit/test_platform_formatter.py` | ⏳ 未着手 |
| **Part 6** | Step 61〜72 | パブリッシングUI・ワンクリックエクスポート & P2総合E2Eテスト | `src/backend/routers/publishing.py`<br>`frontend/src/components/ExportPanel.tsx`<br>`tests/integration/test_p2_architecture_ux_e2e.py` | ⏳ 未着手 |

---

## 🏛️ Part 1: 統一LLMインターフェース規格 `IUnifiedLLMClient` の策定 (Step 1〜12)

### Step 1: 共通LLMリクエストモデル `LLMRequest` の定義
- **目的**: プロンプト、システムプロンプト、温度（temperature）、最大トークン数、モデル指定、ストリーミング要否を統一表現するデータ型。
- **対象ファイル**: `src/core/llm/types.py` (新規作成)
- **変更内容**:
  ```python
  from dataclasses import dataclass, field
  from typing import Any

  @dataclass
  class LLMRequest:
      prompt: str
      system_prompt: str = ""
      temperature: float = 0.7
      max_tokens: int | None = None
      model: str | None = None
      json_mode: bool = False
      response_schema: type | None = None
      extra_params: dict[str, Any] = field(default_factory=dict)
  ```
- **確認コマンド**: `python -c "from src.core.llm.types import LLMRequest; print('OK')"`
- **完了条件**: クラスが定義されインポートできること。

### Step 2: 共通LLMレスポンスモデル `LLMResponse` の定義
- **目的**: 生成テキスト、使用トークン数、モデル名、終了理由、レイテンシを統一表現するデータ型。
- **対象ファイル**: `src/core/llm/types.py`
- **変更内容**:
  ```python
  @dataclass
  class LLMUsage:
      prompt_tokens: int = 0
      completion_tokens: int = 0
      total_tokens: int = 0

  @dataclass
  class LLMResponse:
      content: str
      model: str
      usage: LLMUsage = field(default_factory=LLMUsage)
      finish_reason: str = "stop"
      latency_ms: float = 0.0
      parsed_json: dict | None = None
  ```
- **確認コマンド**: `python -m py_compile src/core/llm/types.py`
- **完了条件**: レスポンスモデルがコンパイルできること。

### Step 3: ストリーミングチャンクモデル `StreamChunk` の定義
- **目的**: リアルタイム出力用トークンチャンクと終了フラグを保持するデータ型。
- **対象ファイル**: `src/core/llm/types.py`
- **変更内容**: `StreamChunk(text: str, is_final: bool = False, finish_reason: str | None = None)` を定義。
- **確認コマンド**: `python -m py_compile src/core/llm/types.py`
- **完了条件**: クラスが定義されていること。

### Step 4: 統一インターフェース抽象基底クラス `IUnifiedLLMClient` の定義
- **目的**: 同期／非同期、テキスト生成、ストリーミング、構造化生成の厳格な抽象メソッドを定義。
- **対象ファイル**: `src/core/llm/unified_interface.py` (新規作成)
- **変更内容**:
  ```python
  from abc import ABC, abstractmethod
  from collections.abc import AsyncIterator, Iterator
  from src.core.llm.types import LLMRequest, LLMResponse, StreamChunk

  class IUnifiedLLMClient(ABC):
      @abstractmethod
      def generate(self, req: LLMRequest) -> LLMResponse: ...

      @abstractmethod
      async def agenerate(self, req: LLMRequest) -> LLMResponse: ...

      @abstractmethod
      def stream(self, req: LLMRequest) -> Iterator[StreamChunk]: ...

      @abstractmethod
      async def astream(self, req: LLMRequest) -> AsyncIterator[StreamChunk]: ...
  ```
- **確認コマンド**: `python -c "from src.core.llm.unified_interface import IUnifiedLLMClient; print('OK')"`
- **完了条件**: 抽象基底クラスが定義されていること。

### Step 5: 構造化出力（JSON Mode）ヘルパーメソッドの実装
- **目的**: `Pydantic` モデルを受け取り、スキーマ指示を自動注入してパース結果を返す共通ヘルパー。
- **対象ファイル**: `src/core/llm/unified_interface.py`
- **変更内容**: `generate_structured(self, req: LLMRequest, schema: type[T]) -> T` の基底実装。
- **確認コマンド**: `python -m py_compile src/core/llm/unified_interface.py`
- **完了条件**: 構造化ヘルパーが実装されていること。

### Step 6: 統一モッククライアント `UnifiedMockLLMClient` の作成
- **目的**: 外部APIを呼ばずに全テストを高速実行できる標準モッククライアントの実装。
- **対象ファイル**: `src/core/llm/adapters/mock_unified_client.py` (新規作成)
- **変更内容**: あらかじめ登録された返答や定型文を同期・非同期・ストリームで返すモック。
- **確認コマンド**: `python -m py_compile src/core/llm/adapters/mock_unified_client.py`
- **完了条件**: モッククライアントが実装されていること。

### Step 7: 旧 `BaseLLMAdapter` 向けの後方互換ラッパーの実装
- **目的**: 既存の `generate_text()` や `stream_text()` 呼び出しを内部で `IUnifiedLLMClient` へ橋渡しするアダプタ。
- **対象ファイル**: `src/services/llm/legacy_adapter_bridge.py` (新規作成)
- **変更内容**: `LegacyLLMBridge(BaseLLMAdapter)` クラスを実装し、旧インターフェースを新クライアントへフォワード。
- **確認コマンド**: `python -m py_compile src/services/llm/legacy_adapter_bridge.py`
- **完了条件**: 互換ブリッジが実装されていること。

### Step 8: 旧 `ILLMProvider` 向けの後方互換ラッパーの実装
- **目的**: `src/core/spi/llm/interface.py` の `generate()` / `agenerate()` を新クライアントへブリッジ。
- **対象ファイル**: `src/core/spi/llm/legacy_spi_bridge.py` (新規作成)
- **変更内容**: `LegacySPIBridge(ILLMProvider)` クラスを実装。
- **確認コマンド**: `python -m py_compile src/core/spi/llm/legacy_spi_bridge.py`
- **完了条件**: 互換ブリッジが実装されていること。

### Step 9: 統一ファクトリ関数 `create_unified_llm_client` の設計
- **目的**: 環境変数や設定ファイルに応じて、適切な統一プロバイダインスタンスを生成。
- **対象ファイル**: `src/core/llm/factory.py` (新規作成)
- **変更内容**: `create_unified_llm_client(provider_name: str, **kwargs) -> IUnifiedLLMClient` を実装。
- **確認コマンド**: `python -c "from src.core.llm.factory import create_unified_llm_client; print('OK')"`
- **完了条件**: ファクトリが定義されること。

### Step 10: トークン計測・レイテンシ測定共通デコレータの実装
- **目的**: 各プロバイダが個別に計測せず、共通で `latency_ms` や `usage` をログ・メトリクスに出力。
- **対象ファイル**: `src/core/llm/unified_interface.py`
- **変更内容**: `@with_metrics` ラッパーを導入。
- **確認コマンド**: `python -m py_compile src/core/llm/unified_interface.py`
- **完了条件**: メトリクス計測処理が追加されていること。

### Step 11: 統一LLMインターフェース基盤の単体テスト作成
- **目的**: `UnifiedMockLLMClient` を用いて、同期・非同期・ストリーム・JSON出力の契約を検証。
- **対象ファイル**: `tests/unit/test_unified_llm_interface.py` (新規作成)
- **変更内容**: 全抽象メソッドのテストケースを網羅。
- **確認コマンド**: `pytest tests/unit/test_unified_llm_interface.py -v`
- **完了条件**: 単体テストがパスすること。

### Step 12: Checkpoint 1 - 統一LLMインターフェース規格の検証
- **目的**: Part 1 の型定義、インターフェース、互換ブリッジ、モックの全テストを実行。
- **確認コマンド**: `pytest tests/unit/test_unified_llm_interface.py`
- **完了条件**: Checkpoint 1 が ALL GREEN であること。

---

## 🔌 Part 2: プロバイダアダプタ移行 & `SpecialistAuditor` / `WriterAgent` のリフレクション撤廃 (Step 13〜24)

### Step 13: OpenAI統一アダプタ `OpenAIUnifiedClient` の実装
- **目的**: OpenAI API（v1.x互換）を `IUnifiedLLMClient` に適合させる。
- **対象ファイル**: `src/core/llm/adapters/openai_unified_client.py` (新規作成)
- **変更内容**: `generate`, `agenerate`, `stream`, `astream` を実装。
- **確認コマンド**: `python -m py_compile src/core/llm/adapters/openai_unified_client.py`
- **完了条件**: コンパイルエラーがないこと。

### Step 14: Gemini統一アダプタ `GeminiUnifiedClient` の実装
- **目的**: Google GenAI SDK（google-genai / google-generativeai）を統一インターフェースに適合。
- **対象ファイル**: `src/core/llm/adapters/gemini_unified_client.py` (新規作成)
- **変更内容**: Gemini向け実装。
- **確認コマンド**: `python -m py_compile src/core/llm/adapters/gemini_unified_client.py`
- **完了条件**: コンパイルエラーがないこと。

### Step 15: Claude / Anthropic統一アダプタ `ClaudeUnifiedClient` の実装
- **目的**: Anthropic SDKを統一インターフェースに適合。
- **対象ファイル**: `src/core/llm/adapters/claude_unified_client.py` (新規作成)
- **変更内容**: Claude向け実装。
- **確認コマンド**: `python -m py_compile src/core/llm/adapters/claude_unified_client.py`
- **完了条件**: コンパイルエラーがないこと。

### Step 16: ローカルOllama / vLLM統一アダプタ `OllamaUnifiedClient` の実装
- **目的**: ローカルLLM環境向けのHTTPクライアントを統一インターフェースに適合。
- **対象ファイル**: `src/core/llm/adapters/ollama_unified_client.py` (新規作成)
- **変更内容**: Ollama向け実装。
- **確認コマンド**: `python -m py_compile src/core/llm/adapters/ollama_unified_client.py`
- **完了条件**: コンパイルエラーがないこと。

### Step 17: `SpecialistAuditor` 基底クラスのリフレクション呼び出しの廃止
- **目的**: `specialist_auditor_base.py` の `ainvoke` / `generate` / `invoke` 等の動的判定を全廃し、`client.agenerate()` / `client.generate()` に一本化。
- **対象ファイル**: `src/agents/specialist_auditor_base.py`
- **変更内容**:
  ```python
  # 変更前: getattr(client, "ainvoke", None) ... によるリフレクション
  # 変更後:
  if isinstance(self.llm_client, IUnifiedLLMClient):
      resp = await self.llm_client.agenerate(LLMRequest(prompt=prompt, system_prompt=sys_prompt))
      text = resp.content
  ```
- **確認コマンド**: `python -m py_compile src/agents/specialist_auditor_base.py`
- **完了条件**: リフレクションコードが削除されていること。

### Step 18: `WriterAgent` の統一クライアント移行
- **目的**: `WriterAgent` 内のLLM呼び出しを `IUnifiedLLMClient` の `agenerate` / `astream` に統一。
- **対象ファイル**: `src/agents/writer_agent.py`
- **変更内容**: 統一リクエストモデルを用いた呼び出しへの変更。
- **確認コマンド**: `python -m py_compile src/agents/writer_agent.py`
- **完了条件**: 執筆エージェントが統一クライアントに対応すること。

### Step 19: ホットパスでの `import re` のモジュールトップへの移動
- **目的**: `code_review_2026-09-07.md` で指摘されたP0項目（各auditor関数内で `import re` が散在していた問題）の解消。
- **対象ファイル**:
  - `src/agents/specialists/consistency_auditor.py`
  - `src/agents/specialists/factual_auditor.py`
  - `src/agents/specialists/multimodal_auditor.py`
  - `src/agents/specialist_auditor_base.py`
- **変更内容**: 全ての `import re` を各ファイルの先頭へ移動。
- **確認コマンド**: `grep -n "def.*:" -A 5 src/agents/specialists/*.py | grep "import re"` で0件ヒット。
- **完了条件**: ホットパスの `import re` が完全排除されていること。

### Step 20: `safe_audit = _safe_audit` エイリアスの整理と意図の明文化
- **目的**: P1指摘事項であった命名規約の曖昧さを解消し、パブリックメソッド `safe_audit` に正式リファクタリング。
- **対象ファイル**: `src/agents/specialist_auditor_base.py`
- **変更内容**: `safe_audit` を本体とし、`_safe_audit` を後方互換用エイリアス（またはその逆）としてdocstringで明確化。
- **確認コマンド**: `python -m py_compile src/agents/specialist_auditor_base.py`
- **完了条件**: コード可読性が向上していること。

### Step 21: 型ヒントの徹底と `# type: ignore` の解消
- **目的**: `specialist_auditor_base.py` および `openai_unified_client.py` の戻り値・変数の厳格な型付け。
- **対象ファイル**: `src/agents/specialist_auditor_base.py`
- **変更内容**: `text_resp: str` 等の明示的な型指定。
- **確認コマンド**: `python -m py_compile src/agents/specialist_auditor_base.py`
- **完了条件**: 型安全性が向上していること。

### Step 22: 統一クライアントを用いた8専門家オーディターの単体テスト
- **目的**: `UnifiedMockLLMClient` を注入し、8つのオーディター（consistency, reader_hook, style 等）が正常に審査を実行できることをテスト。
- **対象ファイル**: `tests/unit/test_unified_auditor_llm.py` (新規作成)
- **変更内容**: 8オーディターの網羅テスト。
- **確認コマンド**: `pytest tests/unit/test_unified_auditor_llm.py -v`
- **完了条件**: 全オーディターの単体テストが成功すること。

### Step 23: 旧アダプタ使用時の非推奨警告（DeprecationWarning）発行
- **目的**: レガシーな `BaseLLMAdapter` を直接使っている古いコードに対し、段階的移行を促す警告を発行。
- **対象ファイル**: `src/services/llm/base.py`
- **変更内容**: `warnings.warn("BaseLLMAdapter is deprecated, use IUnifiedLLMClient", DeprecationWarning)` を追加。
- **確認コマンド**: `python -m py_compile src/services/llm/base.py`
- **完了条件**: 警告が適切に設定されていること。

### Step 24: Checkpoint 2 - 統一アダプタ移行完了の検証
- **目的**: Part 1 および Part 2 の全テストを実行し、リフレクション撤廃とアダプタ統合を確認。
- **確認コマンド**: `pytest tests/unit/test_unified_llm_interface.py tests/unit/test_unified_auditor_llm.py`
- **完了条件**: Checkpoint 2 が ALL GREEN であること。

---

## 📡 Part 3: 執筆・PDCA・DAGイベントバス & WebSocket/SSEストリーミングサーバー (Step 25〜36)

### Step 25: パイプラインリアルタイムイベントモデル `PipelineEvent` の定義
- **目的**: 執筆中・監査中・PDCA再執筆中・DAGタスク進行中にクライアントへ送信するメッセージの共通スキーマ。
- **対象ファイル**: `src/backend/schemas/pipeline_events.py` (新規作成)
- **変更内容**:
  ```python
  from dataclasses import dataclass
  from datetime import datetime
  from typing import Any

  @dataclass
  class PipelineEvent:
      event_type: str  # task_started | pdca_cycle | audit_diff | score_updated | completed | error
      book_id: int
      task_id: str
      timestamp: str = field(default_factory=lambda: datetime.now().isoformat())
      payload: dict[str, Any] = field(default_factory=dict)
  ```
- **確認コマンド**: `python -c "from src.backend.schemas.pipeline_events import PipelineEvent; print('OK')"`
- **完了条件**: クラスが定義されること。

### Step 26: イベント購読・配信ハブ `PipelineEventHub` の実装
- **目的**: インメモリの Pub/Sub ハブを作成し、エージェント側からの `publish()` を接続中WebSocketクライアントへブロードキャスト。
- **対象ファイル**: `src/backend/websocket/pipeline_hub.py` (新規作成)
- **変更内容**: `PipelineEventHub` クラス（`subscribe(book_id, queue)`, `unsubscribe(book_id, queue)`, `broadcast(event)`）を実装。
- **確認コマンド**: `python -m py_compile src/backend/websocket/pipeline_hub.py`
- **完了条件**: ハブクラスがコンパイルできること。

### Step 27: DAGスケジューラへのイベント発火フック追加
- **目的**: `dag_scheduler.py` でタスク開始、完了、リトライ、キャンセル時に `PipelineEventHub.broadcast()` を呼ぶ。
- **対象ファイル**: `src/backend/tasks/dag_scheduler.py`
- **変更内容**: イベント送出コードの統合。
- **確認コマンド**: `python -m py_compile src/backend/tasks/dag_scheduler.py`
- **完了条件**: DAGタスクイベントが発火されること。

### Step 28: クローズドループPDCAへのスコア改善イベント発火フック追加
- **目的**: `pdca_cycle.py` でBefore/Afterスコア算出時、改善率およびActionable Diffをリアルタイム送信。
- **対象ファイル**: `src/services/pdca_cycle.py`
- **変更内容**: `pdca_cycle_result` 確定時のイベント送出。
- **確認コマンド**: `python -m py_compile src/services/pdca_cycle.py`
- **完了条件**: PDCAイベントが発火されること。

### Step 29: FastAPI WebSocket エンドポイント `/api/ws/pipeline/{book_id}` の作成
- **目的**: フロントエンドが書籍IDを指定してパイプライン進捗をリアルタイム購読できるエンドポイント。
- **対象ファイル**: `src/backend/routers/pipeline_stream.py` (新規作成)
- **変更内容**:
  ```python
  @router.websocket("/api/ws/pipeline/{book_id}")
  async def websocket_pipeline_stream(websocket: WebSocket, book_id: int):
      await websocket.accept()
      # キューを登録し、イベントをループ受信して JSON 送信
  ```
- **確認コマンド**: `python -m py_compile src/backend/routers/pipeline_stream.py`
- **完了条件**: WebSocketエンドポイントが実装されること。

### Step 30: WebSocketハートビート（Ping/Pong）と切断ハンドリング
- **目的**: ネットワーク瞬断やブラウザリロード時のゴースト接続を自動検知し、安全にクリーンアップ。
- **対象ファイル**: `src/backend/websocket/pipeline_hub.py`
- **変更内容**: 定期的な Ping 送信と `WebSocketDisconnect` 例外ハンドリング。
- **確認コマンド**: `python -m py_compile src/backend/websocket/pipeline_hub.py`
- **完了条件**: 切断ハンドリングが実装されていること。

### Step 31: SSE（Server-Sent Events）代替エンドポイントの提供
- **目的**: プロキシや企業ファイアウォールでWebSocketが遮断されている環境向けのフォールバック（`GET /api/stream/pipeline/{book_id}`）。
- **対象ファイル**: `src/backend/routers/pipeline_stream.py`
- **変更内容**: `EventSourceResponse` を用いたストリーミング実装。
- **確認コマンド**: `python -m py_compile src/backend/routers/pipeline_stream.py`
- **完了条件**: SSEエンドポイントが実装されること。

### Step 32: イベントバッファリング（接続前の最新状態スナップショット）
- **目的**: 接続が遅れたクライアントにも、現在の最新タスク進捗とスコアを接続直後に即座に初期イベントとして配信。
- **対象ファイル**: `src/backend/websocket/pipeline_hub.py`
- **変更内容**: `latest_snapshots: dict[int, dict]` のキャッシュと初期送信。
- **確認コマンド**: `python -m py_compile src/backend/websocket/pipeline_hub.py`
- **完了条件**: 初期スナップショット配信が実装されていること。

### Step 33: WebSocketストリーミングの単体・統合テスト作成
- **目的**: `TestClient.websocket_connect` を用いて、イベント送出 ➔ クライアント側受信を検証。
- **対象ファイル**: `tests/unit/test_pipeline_websocket.py` (新規作成)
- **変更内容**: 接続、イベント受信、切断のテストケースを作成。
- **確認コマンド**: `pytest tests/unit/test_pipeline_websocket.py -v`
- **完了条件**: WebSocketテストが成功すること。

### Step 34: 認証トークン（API Key）のWebSocketクエリパラメータ検証
- **目的**: セキュリティ保護のため、接続時（URLクエリ `?token=...`）にAPIキーを検証。
- **対象ファイル**: `src/backend/routers/pipeline_stream.py`
- **変更内容**: 不正トークン時の接続拒否（コード 1008 Policy Violation）。
- **確認コマンド**: `python -m py_compile src/backend/routers/pipeline_stream.py`
- **完了条件**: 認証チェックが実装されていること。

### Step 35: サーバーメインアプリケーション（`server.py`）へのルータ登録
- **目的**: `pipeline_stream` ルータを FastAPI `app` に登録。
- **対象ファイル**: `src/backend/server.py`
- **変更内容**: `app.include_router(pipeline_stream.router)` の追加。
- **確認コマンド**: `python -m py_compile src/backend/server.py`
- **完了条件**: アプリケーション起動時にルータが有効化されること。

### Step 36: Checkpoint 3 - リアルタイム配信サーバー基盤の検証
- **目的**: Part 3 の全テストを実行し、イベントハブおよびストリーミングの正常性を確認。
- **確認コマンド**: `pytest tests/unit/test_pipeline_websocket.py`
- **完了条件**: Checkpoint 3 が ALL GREEN であること。

---

## 💻 Part 4: フロントエンドリアルタイム進捗コンポーネント & スコア改善可視化 (Step 37〜48)

### Step 37: フロントエンドWebSocketカスタムフック `usePipelineWebSocket` の実装
- **目的**: 指定 `bookId` の WebSocket に自動接続し、イベントリスナーと再接続ロジック（指数バックオフ）を提供。
- **対象ファイル**: `frontend/src/hooks/usePipelineWebSocket.ts` (新規作成)
- **変更内容**:
  ```typescript
  export function usePipelineWebSocket(bookId: number | null) {
      // WebSocket接続、イベント受信時のステート更新、切断時の自動再接続
  }
  ```
- **確認コマンド**: `npx tsc --noEmit`
- **完了条件**: カスタムフックが実装されていること。

### Step 38: パイプライン進捗状態マネージャの定義
- **目的**: 受信したイベント（タスク進行度、現在のエージェント思考、最新スコア）をUIでバインドしやすいReactステートに整理。
- **対象ファイル**: `frontend/src/hooks/usePipelineWebSocket.ts`
- **変更内容**: `PipelineState` インターフェースの定義と `useReducer` による状態更新。
- **確認コマンド**: `npx tsc --noEmit`
- **完了条件**: 状態管理が定義されていること。

### Step 39: PDCA Before-After スコア改善アニメーションコンポーネント `PDCALiveMonitor.tsx`
- **目的**: 再執筆前と再執筆後の5次元レーダーチャートおよび総合スコア改善（例: 62点 ➔ 78点, +16点UP!）をリアルタイムアニメーション表示。
- **対象ファイル**: `frontend/src/components/studio/PDCALiveMonitor.tsx` (新規作成)
- **変更内容**:
  - スコア差分の数値カウントアップアニメーション。
  - 8専門家オーディターの合格／不合格バッジ表示。
- **確認コマンド**: `npx tsc --noEmit`
- **完了条件**: コンポーネントが作成されること。

### Step 40: リアルタイムActionable Diff表示ペインの実装
- **目的**: 監査員が指摘した改善ディレクティブ（「第3段落のテンポを上げよ」「伏線を提示せよ」）をタイムライン形式で流し込み表示。
- **対象ファイル**: `frontend/src/components/studio/PDCALiveMonitor.tsx`
- **変更内容**: `ActionableDiffStream` サブコンポーネントの実装。
- **確認コマンド**: `npx tsc --noEmit`
- **完了条件**: タイムライン表示が実装されること。

### Step 41: DAGタスクリアルタイム実行グラフコンポーネント `DAGLiveTracker.tsx`
- **目的**: DAGスケジューラの各ノード（プロット ➔ 執筆 ➔ 監査 ➔ リプランニング）の実行状態を視覚的にトラッキング。
- **対象ファイル**: `frontend/src/components/studio/DAGLiveTracker.tsx` (新規作成)
- **変更内容**:
  - ノードの色変化（未実行: 灰、実行中: 青点滅、完了: 緑、リトライ: 橙、失敗: 赤）。
  - リトライ回数バッジの表示。
- **確認コマンド**: `npx tsc --noEmit`
- **完了条件**: DAGトラッカーコンポーネントが作成されること。

### Step 42: `GeneratePanel.tsx` への `PDCALiveMonitor` の組み込み
- **目的**: 小説生成実行タブにライブモニターを配置し、ユーザーが生成中のPDCA思考プロセスをリアルタイムで閲覧可能に。
- **対象ファイル**: `frontend/src/components/GeneratePanel.tsx`
- **変更内容**: 生成ボタン押下後に `PDCALiveMonitor` をアクティブ表示。
- **確認コマンド**: `npx tsc --noEmit`
- **完了条件**: UIパネルに統合されていること。

### Step 43: 通信切断時の「再接続中...」インジケータの実装
- **目的**: 接続が切れた際にユーザーに不安を与えないよう、画面右上に自動再接続ステータスを表示。
- **対象ファイル**: `frontend/src/components/studio/PDCALiveMonitor.tsx`
- **変更内容**: `connectionStatus === 'reconnecting'` のトースト表示。
- **確認コマンド**: `npx tsc --noEmit`
- **完了条件**: 接続インジケータが実装されること。

### Step 44: 音声再生・効果音による完了通知（オプション）
- **目的**: バックグラウンドタブで生成を実行している際、PDCA完了時にチャイム音で通知。
- **対象ファイル**: `frontend/src/components/studio/PDCALiveMonitor.tsx`
- **変更内容**: Web Audio API による短い完了通知トーンの再生。
- **確認コマンド**: `npx tsc --noEmit`
- **完了条件**: 通知機能が実装されること。

### Step 45: フロントエンドUIコンポーネントの単体レンダリングテスト
- **目的**: Vitest / Jest を用いて、モックイベント受信時に `PDCALiveMonitor` が正しく点数を更新するかテスト。
- **対象ファイル**: `frontend/src/components/studio/__tests__/PDCALiveMonitor.test.tsx` (新規作成)
- **変更内容**: イベント注入時のコンポーネントテスト。
- **確認コマンド**: `npm test frontend/src/components/studio/__tests__/PDCALiveMonitor.test.tsx` (利用可能な場合)
- **完了条件**: コンポーネントテストが記述されていること。

### Step 46: レスポンシブ対応（モバイル・タブレット画面での崩れ防止）
- **目的**: スマートフォンや幅狭画面でもレーダーチャートとディレクティブが適切にスタック表示されるCSS調整。
- **対象ファイル**: `frontend/src/index.css`
- **変更内容**: `@media (max-width: 768px)` スタイルの追加。
- **確認コマンド**: `npx tsc --noEmit`
- **完了条件**: スタイルが調整されていること。

### Step 47: ダークモード・ライトモードのテーマ統一
- **目的**: 既存のAutoNovelスタジオデザインに合わせた洗練された配色（グラデーション・グラスモフィズム）。
- **対象ファイル**: `frontend/src/index.css`
- **変更内容**: CSS変数の適用。
- **確認コマンド**: `git diff frontend/src/index.css`
- **完了条件**: テーマが統一されていること。

### Step 48: Checkpoint 4 - フロントエンドリアルタイムUIの検証
- **目的**: TypeScript型チェックおよびビルド構文チェックを実行。
- **確認コマンド**: `npx tsc --noEmit`
- **完了条件**: Checkpoint 4 でTypeScriptコンパイルエラーが0件であること。

---

## 📝 Part 5: プラットフォーム別フォーマッター（なろう・カクヨム・KDP）設計 (Step 49〜60)

### Step 49: プラットフォーム種別列挙型 `PublishPlatform` の定義
- **目的**: 投稿先プラットフォーム（小説家になろう、カクヨム、アルファポリス、Kindle Direct Publishing (KDP)、青空文庫）を定義。
- **対象ファイル**: `src/services/formatters/platform_formatter.py` (新規作成)
- **変更内容**:
  ```python
  from enum import Enum

  class PublishPlatform(str, Enum):
      NAROU = "narou"             # 小説家になろう
      KAKUYOMU = "kakuyomu"       # カクヨム
      ALPHAPOLIS = "alphapolis"   # アルファポリス
      KDP_EPUB = "kdp_epub"       # Amazon KDP (縦書き電子書籍)
      AOZORA = "aozora"           # 青空文庫形式
  ```
- **確認コマンド**: `python -c "from src.services.formatters.platform_formatter import PublishPlatform; print('OK')"`
- **完了条件**: 列挙型が定義されること。

### Step 50: ルビ記法相互トランスパイラ `RubyTranspiler` の実装
- **目的**: 内部記法（`｜漢字《ルビ》`）を各プラットフォーム固有のルビ形式へ一括変換。
  - なろう形式: `|漢字《ルビ》`
  - カクヨム形式: `|漢字《ルビ》` または `《《傍点》》`
  - アルファポリス形式: `#漢字(ルビ)#`
- **対象ファイル**: `src/services/formatters/ruby_transpiler.py` (新規作成)
- **変更内容**: `transpile_ruby(text: str, target: PublishPlatform) -> str` を実装。
- **確認コマンド**: `python -m py_compile src/services/formatters/ruby_transpiler.py`
- **完了条件**: ルビ変換クラスが実装されていること。

### Step 51: 傍点（圏点）記法の各サイト変換
- **目的**: カクヨムの `《《強調》》` や、なろうのルビ置換（`|強《・》|調《・》`）への自動トランスパイル。
- **対象ファイル**: `src/services/formatters/ruby_transpiler.py`
- **変更内容**: `transpile_bouten(text: str, target: PublishPlatform) -> str` を実装。
- **確認コマンド**: `python -m py_compile src/services/formatters/ruby_transpiler.py`
- **完了条件**: 傍点変換が実装されていること。

### Step 52: 三拍子・字下げ・空行の自動整形ルールエンジン
- **目的**: Web小説特有の可読性ルール（段落頭の全角スペース、会話文「」の字下げ禁止、連続空行の上限2行への制限）を正規化。
- **対象ファイル**: `src/services/formatters/platform_formatter.py`
- **変更内容**: `normalize_typography(text: str, platform: PublishPlatform) -> str` を実装。
- **確認コマンド**: `python -m py_compile src/services/formatters/platform_formatter.py`
- **完了条件**: タイポグラフィ正規化が実装されていること。

### Step 53: プラットフォーム文字数・タイトル制限バリデータ
- **目的**: カクヨム（タイトル100字、あらすじ1万字以内）、なろう（タイトル100字、あらすじ数百字推奨）の制限超過警告。
- **対象ファイル**: `src/services/formatters/platform_formatter.py`
- **変更内容**: `validate_limits(title: str, synopsis: str, platform: PublishPlatform) -> list[str]` を実装。
- **確認コマンド**: `python -m py_compile src/services/formatters/platform_formatter.py`
- **完了条件**: バリデータが実装されていること。

### Step 54: 前書き・後書き（作者コメント）枠の自動生成
- **目的**: なろうやカクヨムに存在する「前書き」「後書き」フィールドに、適切な惹き文や次回予告を自動生成・配置。
- **対象ファイル**: `src/services/formatters/platform_formatter.py`
- **変更内容**: `format_episode_with_author_notes(ep: EpisodeResult, platform: PublishPlatform) -> dict` を実装。
- **確認コマンド**: `python -m py_compile src/services/formatters/platform_formatter.py`
- **完了条件**: 前後書き整形メソッドが実装されていること。

### Step 55: 一括エクスポート用ZIPパッケージング機能
- **目的**: 選択したプラットフォーム向けに、第1話〜最終話まで各話ごとの `.txt` ファイルをフォルダ分けしてZIP圧縮。
- **対象ファイル**: `src/services/formatters/platform_formatter.py`
- **変更内容**: `package_for_platform(series: SeriesResult, platform: PublishPlatform) -> bytes` を実装。
- **確認コマンド**: `python -m py_compile src/services/formatters/platform_formatter.py`
- **完了条件**: パッケージング関数が実装されていること。

### Step 56: ルビトランスパイラの単体テスト作成
- **目的**: なろう、カクヨム、アルファポリスへの各ルビ・傍点変換が仕様通り行われるかをテスト。
- **対象ファイル**: `tests/unit/test_platform_formatter.py` (新規作成)
- **変更内容**: ルビ置換の正規表現検証テストケースを網羅。
- **確認コマンド**: `pytest tests/unit/test_platform_formatter.py -k test_ruby -v`
- **完了条件**: ルビテストがパスすること。

### Step 57: タイポグラフィ正規化の単体テスト作成
- **目的**: かぎ括弧頭の全角スペース削除、通常段落の全角スペース付与、連続空行抑制をテスト。
- **対象ファイル**: `tests/unit/test_platform_formatter.py`
- **変更内容**: 文字整形アサーションテストを追加。
- **確認コマンド**: `pytest tests/unit/test_platform_formatter.py -k test_typography -v`
- **完了条件**: 整形テストがパスすること。

### Step 58: バリデータの単体テスト作成
- **目的**: 制限文字数超過時に正しい警告メッセージが返却されることをテスト。
- **対象ファイル**: `tests/unit/test_platform_formatter.py`
- **変更内容**: 制限超過ケースのテストを追加。
- **確認コマンド**: `pytest tests/unit/test_platform_formatter.py -k test_validation`
- **完了条件**: バリデータテストがパスすること。

### Step 59: プラットフォーム別フォーマットガイドドキュメントの作成
- **目的**: 各投稿サイトでの推奨設定とフォーマッターの変換仕様を解説。
- **対象ファイル**: `docs/publishing_guide.md` (新規作成)
- **変更内容**: 各サイトのルビ記法と注意事項をまとめたガイド。
- **確認コマンド**: `python -c "import os; assert os.path.exists('docs/publishing_guide.md'); print('OK')"`
- **完了条件**: ガイドファイルが作成されていること。

### Step 60: Checkpoint 5 - プラットフォームフォーマッターの検証
- **目的**: Part 5 の全テストを実行し、各種小説サイト向けテキスト整形の正確性を確認。
- **確認コマンド**: `pytest tests/unit/test_platform_formatter.py`
- **完了条件**: Checkpoint 5 が ALL GREEN であること。

---

## 🚀 Part 6: パブリッシングUI・ワンクリックエクスポート & P2総合E2Eテスト (Step 61〜72)

### Step 61: FastAPI エンドポイント `POST /api/export/publish/{platform}` の作成
- **目的**: フロントエンドからプラットフォーム指定で整形済みテキストまたはZIPアーカイブをダウンロードするAPI。
- **対象ファイル**: `src/backend/routers/publishing.py` (新規作成)
- **変更内容**:
  ```python
  @router.post("/publish/{platform}")
  async def export_for_publishing(book_id: int, platform: PublishPlatform):
      # SeriesDataLoader で書籍をロードし、package_for_platform で ZIP 生成して StreamingResponse 返却
  ```
- **確認コマンド**: `python -m py_compile src/backend/routers/publishing.py`
- **完了条件**: エンドポイントが実装されること。

### Step 62: 各話プレビューAPI `GET /api/export/publish/{platform}/preview` の作成
- **目的**: 指定した1章分の整形後テキストを画面上で即座に確認・ワンクリックコピーできるAPI。
- **対象ファイル**: `src/backend/routers/publishing.py`
- **変更内容**: `{ "formatted_content": "...", "warnings": [...] }` を返却。
- **確認コマンド**: `python -m py_compile src/backend/routers/publishing.py`
- **完了条件**: プレビューAPIが実装されること。

### Step 63: サーバーメインアプリケーションへのパブリッシングAPI登録
- **目的**: `server.py` に `publishing.router` をマウント。
- **対象ファイル**: `src/backend/server.py`
- **変更内容**: `app.include_router(publishing.router, prefix="/api/export")`。
- **確認コマンド**: `python -m py_compile src/backend/server.py`
- **完了条件**: APIが利用可能になること。

### Step 64: フロントエンドAPIクライアント `publishingApi.ts` の作成
- **目的**: フロントからプレビュー取得およびダウンロードを呼び出す通信モジュール。
- **対象ファイル**: `frontend/src/api/publishing.ts` (新規作成)
- **変更内容**: `fetchPublishPreview`, `downloadPublishZip` 関数を実装。
- **確認コマンド**: `npx tsc --noEmit`
- **完了条件**: クライアント関数が追加されていること。

### Step 65: パブリッシングモーダル `PublishExportModal.tsx` の作成
- **目的**: なろう／カクヨム／KDPのタブ切替、整形後本文のシンタックスハイライト表示、ワンクリック「クリップボードにコピー」ボタンの実装。
- **対象ファイル**: `frontend/src/components/common/PublishExportModal.tsx` (新規作成)
- **変更内容**: モーダルUIコンポーネントの実装。
- **確認コマンド**: `npx tsc --noEmit`
- **完了条件**: モーダルが作成されていること。

### Step 66: `ExportPanel.tsx` への「投稿サイト向け出力」ボタン追加
- **目的**: エクスポート画面に「Web小説投稿フォーマットで出力」導線を追加し、モーダルを開く。
- **対象ファイル**: `frontend/src/components/ExportPanel.tsx`
- **変更内容**: ボタンとモーダルの配線。
- **確認コマンド**: `npx tsc --noEmit`
- **完了条件**: UIが統合されていること。

### Step 67: P2総合結合テスト用ハーネスの整備
- **目的**: 統一LLMインターフェース、WebSocketイベント配信、パブリッシングエクスポートを結合検証するテストスイート。
- **対象ファイル**: `tests/integration/test_p2_architecture_ux_e2e.py` (新規作成)
- **変更内容**: 統合テスト用環境の構築。
- **確認コマンド**: `pytest tests/integration/test_p2_architecture_ux_e2e.py -v`
- **完了条件**: テストハーネスが動作すること。

### Step 68: E2Eテスト 1: 統一LLMクライアント経由での執筆 & 8専門家監査
- **目的**: `UnifiedMockLLMClient` を使用し、リフレクションを一切介さずに執筆から監査・PDCAまで完走することを検証。
- **対象ファイル**: `tests/integration/test_p2_architecture_ux_e2e.py`
- **変更内容**: `test_unified_llm_writing_audit_flow_e2e` を記述。
- **確認コマンド**: `pytest tests/integration/test_p2_architecture_ux_e2e.py -k test_unified_llm`
- **完了条件**: テストが合格すること。

### Step 69: E2Eテスト 2: WebSocket経由でのリアルタイム進捗配信 & スコア更新
- **目的**: WebSocketクライアントを接続した状態でPDCAサイクルを実行し、スコア改善イベントが正確に受信できるかを検証。
- **対象ファイル**: `tests/integration/test_p2_architecture_ux_e2e.py`
- **変更内容**: `test_websocket_live_progress_stream_e2e` を記述。
- **確認コマンド**: `pytest tests/integration/test_p2_architecture_ux_e2e.py -k test_websocket_live`
- **完了条件**: テストが合格すること。

### Step 70: E2Eテスト 3: 書籍データからのカクヨム・なろう向け一括エクスポート
- **目的**: 実DBから読み込んだ小説をカクヨム形式およびなろう形式でエクスポートし、ルビやタイポグラフィが完全に出力されることを検証。
- **対象ファイル**: `tests/integration/test_p2_architecture_ux_e2e.py`
- **変更内容**: `test_publishing_export_zip_e2e` を記述。
- **確認コマンド**: `pytest tests/integration/test_p2_architecture_ux_e2e.py -k test_publishing_export`
- **完了条件**: テストが合格すること。

### Step 71: コードクリーンアップと型チェック（mypy/ruff）
- **目的**: Part 1〜6で作成・改変した全モジュールの型安全性・構文整合性を検証。
- **対象ファイル**: `src/core/llm/`, `src/backend/websocket/`, `src/services/formatters/`
- **確認コマンド**: `python -m py_compile src/core/llm/unified_interface.py src/backend/websocket/pipeline_hub.py src/services/formatters/platform_formatter.py`
- **完了条件**: コンパイルエラーおよび重大な警告がないこと。

### Step 72: Final Gate (Checkpoint 6) - P2 全テストスイート一括実行 (100% ALL GREEN)
- **目的**: P2 に関連する全ての単体テスト・結合テストスイートを一括実行し、完全合格を確認。
- **確認コマンド**: `pytest tests/unit/test_unified_llm_interface.py tests/unit/test_unified_auditor_llm.py tests/unit/test_pipeline_websocket.py tests/unit/test_platform_formatter.py tests/integration/test_p2_architecture_ux_e2e.py -v`
- **完了条件**: 全テストが 100% GREEN でパスすること。
