# P0 実装計画書: 即効性が高い実用化 72ステップ詳細設計
## （マルチメディア実DB結合・商用縦書きEPUB挿絵統合・IFルートマージ確定コミット）

- **策定日**: 2026年9月11日
- **対象バージョン**: AutoNovel v4.5.0+
- **設計方針**: 低性能なLLMでも迷わず1ステップずつ確実に実装・検証できるよう、最小単位の作業（1コミット＝1テスト緑）に分割。6パート構成、各パート12ステップ、12ステップごとに動作検証ゲート（Checkpoint）を設置。
- **総ステップ数**: 全72ステップ（12ステップ × 6パート）

---

## 📋 パート別構成概要

| パート | ステップ | テーマ | 主な対象ファイル | ステータス |
|---|---|---|---|---|
| **Part 1** | Step 1〜12 | `SeriesResult` / `EpisodeResult` DBローダー & 復元基盤 | `src/backend/database/series_loader.py`<br>`src/services/series_serializer.py`<br>`tests/unit/test_series_loader.py` | ✅ 完了（4/4 Passed） |
| **Part 2** | Step 13〜24 | `MultimediaService` の実データ結合 & ダミー依存完全脱却 | `src/backend/multimedia_service.py`<br>`src/backend/routers/multimedia.py`<br>`tests/unit/test_multimedia_real_db.py` | ✅ 完了（7/7 Passed） |
| **Part 3** | Step 25〜36 | 商用縦書きEPUBの挿絵・口絵マニフェスト & XHTML統合 | `src/services/exporters/epub_commercial_builder.py`<br>`src/services/exporters/epub_content_builder.py`<br>`tests/unit/test_epub_illustrations.py` | ✅ 完了（2/2 Passed） |
| **Part 4** | Step 37〜48 | 挿絵付き商用EPUBのCSS・メタデータ・配信パック統合 | `src/services/exporters/vertical_css_templates.py`<br>`src/backend/multimedia_service.py`<br>`tests/unit/test_commercial_epub_bundle.py` | ✅ 完了（5/5 Passed） |
| **Part 5** | Step 49〜60 | IFルートマージ確定コミットAPI & 競合解決エンジン | `src/backend/schemas/branch.py`<br>`src/backend/services/branch_merge_service.py`<br>`src/backend/routers/branches.py`<br>`tests/unit/test_branch_merge_commit.py` | ✅ 完了（3/3 Passed） |
| **Part 6** | Step 61〜72 | フロントエンド連携・WebSocket通知・P0総合E2Eテスト | `frontend/src/components/branches/MergeConflictPreview.tsx`<br>`tests/integration/test_p0_multimedia_merge_e2e.py` | ✅ 完了（5/5 Passed） |


---

## 📦 Part 1: `SeriesResult` / `EpisodeResult` DBローダー & 復元基盤 (Step 1〜12)

### Step 1: `SeriesDataLoaderConfig` データクラスの定義
- **目的**: DBから `SeriesResult` を復元する際の読み込みオプション（特定ブランチ指定、最新ドラフト優先、メタデータ復元有無）を定義する。
- **対象ファイル**: `src/backend/database/series_loader.py` (新規作成)
- **変更内容**:
  ```python
  from dataclasses import dataclass

  @dataclass
  class SeriesDataLoaderConfig:
      book_id: int
      branch_id: int | None = None
      include_unpublished: bool = True
      fallback_to_minimal: bool = False
  ```
- **確認コマンド**: `python -c "from src.backend.database.series_loader import SeriesDataLoaderConfig; print('OK')"`
- **完了条件**: クラスが定義され、エラーなくインポートできること。

### Step 2: `EpisodeResult` 復元ヘルパー `_row_to_episode_result` の実装
- **目的**: DBの `chapters` テーブルの行（SQLAlchemy モデルまたは辞書）から `EpisodeResult` を正しくマッピングする。
- **対象ファイル**: `src/backend/database/series_loader.py`
- **変更内容**:
  - `chapter_number`, `title`, `content` から文字数、`audit_score`（デフォルト80.0または保存値）、`metadata` を展開して `EpisodeResult` を組み立てる関数を実装。
- **確認コマンド**: `python -m py_compile src/backend/database/series_loader.py`
- **完了条件**: 構文エラーなくコンパイルできること。

### Step 3: `SeriesDataLoader` クラスの初期化とDBセッション管理
- **目的**: 同期/非同期双方のDBセッションファクトリに対応するローダークラスを定義する。
- **対象ファイル**: `src/backend/database/series_loader.py`
- **変更内容**:
  ```python
  from sqlalchemy.orm import Session
  from src.backend.database import SessionLocal

  class SeriesDataLoader:
      def __init__(self, session_factory=None):
          self._session_factory = session_factory or SessionLocal
  ```
- **確認コマンド**: `python -m py_compile src/backend/database/series_loader.py`
- **完了条件**: クラスが初期化可能であること。

### Step 4: 書籍メタデータおよびバイブルの取得メソッド `_load_book_metadata`
- **目的**: `books` テーブルからタイトル、ジャンル、コンセプト、およびバイブル設定（JSON）を安全にロードする。
- **対象ファイル**: `src/backend/database/series_loader.py`
- **変更内容**: `_load_book_metadata(self, session: Session, book_id: int) -> dict` を実装。見つからない場合は `ValueError` を送出。
- **確認コマンド**: `python -m py_compile src/backend/database/series_loader.py`
- **完了条件**: 書籍メタデータ取得ロジックが実装されていること。

### Step 5: チャプター一覧の章番号順取得クエリ `_load_chapters`
- **目的**: 対象 `book_id` のチャプターを章番号（`episode_num`）順に取得する。ブランチIDが指定されている場合は該当ブランチの章を取得。
- **対象ファイル**: `src/backend/database/series_loader.py`
- **変更内容**: `SELECT * FROM chapters WHERE book_id = :book_id ORDER BY chapter_number ASC` を実行し、`_row_to_episode_result` でリスト化。
- **確認コマンド**: `python -m py_compile src/backend/database/series_loader.py`
- **完了条件**: チャプター一覧取得クエリが実装されていること。

### Step 6: 欠番チャプターおよび空文字チャプターの補正処理
- **目的**: 章番号の飛びやコンテンツが空のチャプターが存在する場合の警告ログ出力とインデックス正規化。
- **対象ファイル**: `src/backend/database/series_loader.py`
- **変更内容**: `_normalize_episodes(episodes: list[EpisodeResult]) -> list[EpisodeResult]` を実装。
- **確認コマンド**: `python -m py_compile src/backend/database/series_loader.py`
- **完了条件**: 欠番補正ロジックが追加されていること。

### Step 7: 完全な `SeriesResult` 構築メソッド `load_series` の実装
- **目的**: メタデータとチャプターリストを結合して `SeriesResult` オブジェクトを構築して返す。
- **対象ファイル**: `src/backend/database/series_loader.py`
- **変更内容**:
  ```python
  def load_series(self, config: SeriesDataLoaderConfig) -> SeriesResult:
      # session を開き、book メタデータと chapters を取得して SeriesResult を返す
  ```
- **確認コマンド**: `python -m py_compile src/backend/database/series_loader.py`
- **完了条件**: `load_series` メソッドが完成すること。

### Step 8: チャプターが存在しない場合の安全なエラーハンドリング
- **目的**: DBに本はあるが章が1つも書かれていない場合、明示的な例外 `NoChaptersFoundError` を送出する。
- **対象ファイル**: `src/backend/exceptions.py`, `src/backend/database/series_loader.py`
- **変更内容**: `NoChaptersFoundError(AutoNovelException)` を追加し、ローダー内で章数0の際に送出。
- **確認コマンド**: `python -c "from src.backend.exceptions import NoChaptersFoundError; print('OK')"`
- **完了条件**: 例外クラスが定義され、送出分岐が組み込まれていること。

### Step 9: `SeriesResult` から DBへの逆保存ヘルパー `save_series_to_db`
- **目的**: エクスポート前の一時編集やインポート時に `SeriesResult` をDBにアトミック保存できるメソッドを追加。
- **対象ファイル**: `src/backend/database/series_loader.py`
- **変更内容**: `save_series_to_db(self, session: Session, series: SeriesResult, book_id: int) -> None` を実装。
- **確認コマンド**: `python -m py_compile src/backend/database/series_loader.py`
- **完了条件**: 逆保存メソッドが実装されていること。

### Step 10: 依存性注入用ファクトリ関数 `get_series_loader` の作成
- **目的**: FastAPIルータやサービスクラスから注入しやすいようにプロバイダ関数を定義。
- **対象ファイル**: `src/backend/database/series_loader.py`
- **変更内容**: `def get_series_loader() -> SeriesDataLoader: return SeriesDataLoader()` を定義。
- **確認コマンド**: `python -c "from src.backend.database.series_loader import get_series_loader; print(get_series_loader())"`
- **完了条件**: ファクトリ関数が動作すること。

### Step 11: `SeriesDataLoader` の単体テスト作成
- **目的**: メモリ内SQLiteを用いて、書籍作成、章挿入、`load_series()` のデータ整合性を検証する。
- **対象ファイル**: `tests/unit/test_series_loader.py` (新規作成)
- **変更内容**: 正常系、チャプター0件時の例外発生、ブランチフィルタのテストケースを記述。
- **確認コマンド**: `pytest tests/unit/test_series_loader.py -v`
- **完了条件**: 単体テストが作成され、実行可能であること。

### Step 12: Checkpoint 1 - ローダー基盤の検証とゲート通過
- **目的**: Part 1 の全モジュールの構文・テスト・インポートを検証。
- **確認コマンド**: `pytest tests/unit/test_series_loader.py`
- **完了条件**: Checkpoint 1 が ALL GREEN であること。

---

## 🔗 Part 2: `MultimediaService` の実データ結合 & ダミー依存完全脱却 (Step 13〜24)

### Step 13: `MultimediaService` への `SeriesDataLoader` 依存性注入
- **目的**: `MultimediaService` のコンストラクタで `SeriesDataLoader` を受け取れるようにする。
- **対象ファイル**: `src/backend/multimedia_service.py`
- **変更内容**:
  ```python
  def __init__(self, session_factory=None, output_dir=None, series_loader=None):
      self._series_loader = series_loader or SeriesDataLoader(self._session_factory)
  ```
- **確認コマンド**: `python -m py_compile src/backend/multimedia_service.py`
- **完了条件**: コンストラクタ引数が追加され、デフォルトでローダーが初期化されること。

### Step 14: 実DB取得ヘルパー `_resolve_series` の実装
- **目的**: 外部から `series` が渡されていない場合、DBから最新の章データを取得して自動解決する。
- **対象ファイル**: `src/backend/multimedia_service.py`
- **変更内容**:
  - `_resolve_series(self, book_id: int, series: SeriesResult | None) -> SeriesResult` を実装。
  - `series` が None の場合、`self._series_loader.load_series(...)` を呼び出す。
- **確認コマンド**: `python -m py_compile src/backend/multimedia_service.py`
- **完了条件**: 自動解決メソッドが追加されていること。

### Step 15: `generate_media_mix` の実DB連携化
- **目的**: `generate_media_mix` 内の `make_minimal_series` 呼び出しを廃止し、`_resolve_series` に置き換える。
- **対象ファイル**: `src/backend/multimedia_service.py`
- **変更内容**:
  - `series = self._resolve_series(book_id, series)` を呼ぶように修正。
  - チャプターが存在しない場合は適切なエラーレスポンスを返せるようにする。
- **確認コマンド**: `python -m py_compile src/backend/multimedia_service.py`
- **完了条件**: `generate_media_mix` が実DBデータを参照するようになること。

### Step 16: `export_ebook` の実DB連携化
- **目的**: `export_ebook` 内の `make_minimal_series` 呼び出しを廃止し、DB上の全チャプターを取得してEPUB/PDF化する。
- **対象ファイル**: `src/backend/multimedia_service.py`
- **変更内容**: `series = self._resolve_series(book_id, series)` に置き換え。
- **確認コマンド**: `python -m py_compile src/backend/multimedia_service.py`
- **完了条件**: `export_ebook` が実DBチャプターを対象に出力すること。

### Step 17: `generate_if_routes` の実DB連携化
- **目的**: `generate_if_routes` 内のダミーデータ呼び出しを実章データに置き換える。
- **対象ファイル**: `src/backend/multimedia_service.py`
- **変更内容**: `series = self._resolve_series(book_id, series)` に置き換え。
- **確認コマンド**: `python -m py_compile src/backend/multimedia_service.py`
- **完了条件**: IFルート生成が実書籍データからグラフを構築すること。

### Step 18: `generate_asset_pack` の実DB連携化
- **目的**: 資産化パック生成時に、書籍の全章・全設定をDBから忠実に引き継いでバンドルする。
- **対象ファイル**: `src/backend/multimedia_service.py`
- **変更内容**: `series = self._resolve_series(book_id, series)` に置き換え、下流の各生成器に引き渡す。
- **確認コマンド**: `python -m py_compile src/backend/multimedia_service.py`
- **完了条件**: アセットパックが実データから生成されること。

### Step 19: 書籍プリセット（キャラクター・文体）の実DB引き当て
- **目的**: `_make_minimal_preset(series.genre)` で固定の空辞書を渡していた箇所を、書籍に保存された `characters` / `style` 設定から取得するように改善。
- **対象ファイル**: `src/backend/multimedia_service.py`
- **変更内容**: `series.bible` から `characters` と `style` を安全に抽出しプリセット辞書を組み立てる `_extract_preset(series)` を実装。
- **確認コマンド**: `python -m py_compile src/backend/multimedia_service.py`
- **完了条件**: キャラクター設定が正しくプリセットに反映されること。

### Step 20: FastAPI ルータ `routers/multimedia.py` のエラーハンドリング更新
- **目的**: `NoChaptersFoundError` が発生した際に、HTTP 404 または 422（Unprocessable Entity）の明瞭なJSONエラーメッセージを返す。
- **対象ファイル**: `src/backend/routers/multimedia.py`
- **変更内容**: 各エンドポイントに `try ... except NoChaptersFoundError as e: raise HTTPException(status_code=422, detail=str(e))` を追加。
- **確認コマンド**: `python -m py_compile src/backend/routers/multimedia.py`
- **完了条件**: ルータのエラーハンドリングが拡充されること。

### Step 21: `make_minimal_series` のテスト専用モジュールへの退避・分離
- **目的**: プロダクションコード `multimedia_service.py` からテスト用ダミー生成ヘルパーを分離し、混入を防止する。
- **対象ファイル**: `src/backend/multimedia_service.py`, `tests/helpers/multimedia_fixtures.py` (新規作成)
- **変更内容**: `make_minimal_series`, `_make_minimal_episode` を `tests/helpers/multimedia_fixtures.py` に移動し、テスト側からインポート。
- **確認コマンド**: `python -m py_compile src/backend/multimedia_service.py`
- **完了条件**: 本番サービスファイルからダミーデータ生成関数が排除されていること。

### Step 22: DBデータを用いたマルチメディア生成の結合テスト作成
- **目的**: テスト用DBにBookと3つの章を保存し、`generate_media_mix` と `export_ebook` を実行して生成物に対象チャプターの文言が含まれているか検証。
- **対象ファイル**: `tests/unit/test_multimedia_real_db.py` (新規作成)
- **変更内容**: 実データ注入テストを作成。
- **確認コマンド**: `pytest tests/unit/test_multimedia_real_db.py -v`
- **完了条件**: 実データからの生成テストが合格すること。

### Step 23: 章なし書籍に対する 422 レスポンステストの作成
- **目的**: 章が1件も存在しない Book ID でマルチメディアAPIを呼び出し、422 エラーが返ることを確認。
- **対象ファイル**: `tests/unit/test_multimedia_real_db.py`
- **変更内容**: 空書籍のエラーテストケースを追加。
- **確認コマンド**: `pytest tests/unit/test_multimedia_real_db.py -k test_empty_book`
- **完了条件**: エラー系テストがパスすること。

### Step 24: Checkpoint 2 - 実DB結合機能の全テスト合格
- **目的**: Part 1 および Part 2 の全テストを実行し、ダミー依存からの完全脱却を確認。
- **確認コマンド**: `pytest tests/unit/test_series_loader.py tests/unit/test_multimedia_real_db.py`
- **完了条件**: Checkpoint 2 が ALL GREEN であること。

---

## 🎨 Part 3: 商用縦書きEPUBの挿絵・口絵マニフェスト & XHTML統合 (Step 25〜36)

### Step 25: 挿絵メタデータモデル `EpubIllustrationItem` の定義
- **目的**: 表紙以外のカラー口絵、章間挿絵、挿絵位置（章の前/後/文中）を管理する型定義。
- **対象ファイル**: `src/services/exporters/epub_manifest_builder.py`
- **変更内容**:
  ```python
  from dataclasses import dataclass
  from typing import Literal

  @dataclass
  class EpubIllustrationItem:
      image_id: str
      image_bytes: bytes
      file_name: str
      media_type: str = "image/jpeg"
      position: Literal["frontmatter", "chapter_start", "chapter_end"] = "chapter_start"
      chapter_index: int | None = None
      caption: str = ""
  ```
- **確認コマンド**: `python -c "from src.services.exporters.epub_manifest_builder import EpubIllustrationItem; print('OK')"`
- **完了条件**: クラスが正しくインポートできること。

### Step 26: `EpubContentBuilder.build_illustration_xhtml` のスタイル・マークアップ拡張
- **目的**: 縦書きEPUB端末で美しく全画面／中央表示される挿絵専用XHTMLの構築。
- **対象ファイル**: `src/services/exporters/epub_content_builder.py`
- **変更内容**:
  - キャプション表示用の `<p class="illustration-caption">` 追加。
  - SVGラッパーまたはCSS `object-fit: contain; max-height: 100vh;` による画面アスペクト比対応。
- **確認コマンド**: `python -m py_compile src/services/exporters/epub_content_builder.py`
- **完了条件**: 挿絵XHTML生成メソッドが拡張されていること。

### Step 27: `EpubManifestBuilder.build_standard_opf` への動的画像マニフェスト追加
- **目的**: 渡された挿絵リストの各アイテムを `standard.opf` の `<manifest>` に自動追加する。
- **対象ファイル**: `src/services/exporters/epub_manifest_builder.py`
- **変更内容**: `manifest_items` に `{"id": f"img-{item.image_id}", "href": f"images/{item.file_name}", "media-type": item.media_type}` を追加。
- **確認コマンド**: `python -m py_compile src/services/exporters/epub_manifest_builder.py`
- **完了条件**: マニフェストに挿絵画像項目が出力されること。

### Step 28: `build_standard_opf` への挿絵XHTMLページのSpine登録
- **目的**: 挿絵ページをEPUBの読み進め順（`<spine>`）の適切な位置（口絵なら表紙の直後、章間なら該当章の前）に挿入する。
- **対象ファイル**: `src/services/exporters/epub_manifest_builder.py`
- **変更内容**: `spine_items` リストに挿絵ページのID（`p-ill-XXX`）を挟み込むロジックを実装。
- **確認コマンド**: `python -m py_compile src/services/exporters/epub_manifest_builder.py`
- **完了条件**: Spineに挿絵ページが正しく配置されること。

### Step 29: ナビゲーション文書（`nav.xhtml`）および `toc.ncx` への挿絵除外設定
- **目的**: 挿絵ページが目次一覧に余計な項目として現れないよう、TOCの抽出条件を整理（または「口絵・挿絵」としてオプション表示）。
- **対象ファイル**: `src/services/exporters/epub_manifest_builder.py`
- **変更内容**: `nav.xhtml` の `<nav epub:type="toc">` に挿絵ページが混入しないようフィルタを適用。
- **確認コマンド**: `python -m py_compile src/services/exporters/epub_manifest_builder.py`
- **完了条件**: 目次が本文チャプターのみで整然と出力されること。

### Step 30: `CommercialEpubBuilder.build_commercial_epub` での `images` 引数の受領・パース
- **目的**: 無視されていた `images` 引数を `EpubIllustrationItem` のリストへ正規化。
- **対象ファイル**: `src/services/exporters/epub_commercial_builder.py`
- **変更内容**:
  ```python
  parsed_images = [
      img if isinstance(img, EpubIllustrationItem) else EpubIllustrationItem(**img)
      for img in (images or [])
  ]
  ```
- **確認コマンド**: `python -m py_compile src/services/exporters/epub_commercial_builder.py`
- **完了条件**: 画像引数のパース処理が実装されていること。

### Step 31: 画像バイナリのZIPパッケージ追加処理
- **目的**: 全挿絵の画像バイナリを `PureEpubPacker` を通じて `item/images/{file_name}` に追加する。
- **対象ファイル**: `src/services/exporters/epub_commercial_builder.py`
- **変更内容**: `packer.add_file(f"item/images/{item.file_name}", item.image_bytes)` をループ実行。
- **確認コマンド**: `python -m py_compile src/services/exporters/epub_commercial_builder.py`
- **完了条件**: 画像ファイルがZIPアーカイブに正しく追加されること。

### Step 32: 口絵（カラー巻頭イラスト）ページの生成とSpine配置
- **目的**: `position == "frontmatter"` のイラストを表紙の直後、第1章の前に配置するXHTML生成とSpine挿入。
- **対象ファイル**: `src/services/exporters/epub_commercial_builder.py`
- **変更内容**: 口絵ページ `item/xhtml/ill-front-{idx}.xhtml` を生成し、Spineの先頭側に追加。
- **確認コマンド**: `python -m py_compile src/services/exporters/epub_commercial_builder.py`
- **完了条件**: 口絵ページが正しく組み込まれること。

### Step 33: 章間挿絵（チャプター連動イラスト）ページの生成とSpine配置
- **目的**: `position == "chapter_start"` のイラストを、該当 `chapter_index` の章本文の直前に挟み込む。
- **対象ファイル**: `src/services/exporters/epub_commercial_builder.py`
- **変更内容**: チャプターのループ内で該当するイラストを検索し、本文ページの直前に挿絵XHTMLを挟み込む。
- **確認コマンド**: `python -m py_compile src/services/exporters/epub_commercial_builder.py`
- **完了条件**: 指定章の直前に挿絵ページが配置されること。

### Step 34: 複数挿絵が存在する場合の連番ID衝突防止
- **目的**: 同一章に複数の挿絵が存在したり、同一ファイル名がある場合のID一意化（`ill-c1-1`, `ill-c1-2`）。
- **対象ファイル**: `src/services/exporters/epub_commercial_builder.py`
- **変更内容**: 一意なIDとファイルパスを保証する命名サニタイズ処理を追加。
- **確認コマンド**: `python -m py_compile src/services/exporters/epub_commercial_builder.py`
- **完了条件**: 命名衝突が発生しないこと。

### Step 35: 挿絵統合機能の単体テスト作成
- **目的**: ダミーJPEG画像バイナリ（1x1pxまたはテスト画像）を2枚用意し、口絵と章間挿絵がEPUB内に生成されるか検証。
- **対象ファイル**: `tests/unit/test_epub_illustrations.py` (新規作成)
- **変更内容**: 生成されたEPUBバイナリを `zipfile.ZipFile` で開き、`item/images/` および `item/xhtml/ill-*.xhtml` の存在を確認するテストを作成。
- **確認コマンド**: `pytest tests/unit/test_epub_illustrations.py -v`
- **完了条件**: 単体テストが成功すること。

### Step 36: Checkpoint 3 - EPUB挿絵統合機能の検証
- **目的**: Part 3 の実装が既存のテキストのみのEPUB生成を破壊していないことを含めて検証。
- **確認コマンド**: `pytest tests/unit/test_epub_illustrations.py`
- **完了条件**: Checkpoint 3 が ALL GREEN であること。

---

## 📖 Part 4: 挿絵付き商用EPUBのCSS・メタデータ・配信パック統合 (Step 37〜48)

### Step 37: 縦書きCSS `vertical.css` の挿絵スタイル整備
- **目的**: EPUBリーダーでの挿絵表示用CSS（中央揃え、枠線なし、キャプション縦中横／横書き切り替え）を追加。
- **対象ファイル**: `src/services/exporters/vertical_css_templates.py`
- **変更内容**:
  ```css
  /* 挿絵ページスタイル */
  body.p-illustration {
      margin: 0;
      padding: 0;
      text-align: center;
  }
  div.illustration-wrap {
      width: 100%;
      height: 100%;
      display: flex;
      justify-content: center;
      align-items: center;
  }
  img.illustration-img {
      max-width: 100%;
      max-height: 100%;
      object-fit: contain;
  }
  ```
- **確認コマンド**: `python -m py_compile src/services/exporters/vertical_css_templates.py`
- **完了条件**: CSSテンプレートが構文エラーなく読み込めること。

### Step 38: 挿絵XHTMLへの専用クラス `class="p-illustration"` 付与
- **目的**: 挿絵ページの `<body>` に `class="p-illustration"` を付与し、本文の縦書き段落スタイルと干渉しないように分離。
- **対象ファイル**: `src/services/exporters/epub_content_builder.py`
- **変更内容**: `build_illustration_xhtml` のテンプレート更新。
- **確認コマンド**: `python -m py_compile src/services/exporters/epub_content_builder.py`
- **完了条件**: 挿絵専用クラスが付与されていること。

### Step 39: DBの `book_illustrations` / アセットテーブルからの挿絵自動引き当て
- **目的**: `multimedia_service.py` の `export_ebook` で、対象 `book_id` に紐づく画像アセットをDBから検索して自動取得。
- **対象ファイル**: `src/backend/multimedia_service.py`
- **変更内容**:
  - `_load_book_illustrations(self, session: Session, book_id: int) -> list[dict]` を実装。
  - `multimedia_artifacts` または画像保存テーブルから `asset_type="illustration"` を抽出。
- **確認コマンド**: `python -m py_compile src/backend/multimedia_service.py`
- **完了条件**: DBから画像アセットを検索する処理が追加されること。

### Step 40: 画像ファイルの実体読み込みとフォールバック
- **目的**: 保存されたパスから画像バイナリを読み取り、ファイルが存在しない場合は警告ログを出してスキップする堅牢化。
- **対象ファイル**: `src/backend/multimedia_service.py`
- **変更内容**: 画像読み込みループ内で `try ... open(path, "rb")` 例外処理を追加。
- **確認コマンド**: `python -m py_compile src/backend/multimedia_service.py`
- **完了条件**: ファイル欠落時にもEPUB生成がクラッシュしないこと。

### Step 41: `export_ebook` での `CommercialEpubBuilder` への画像受け渡し
- **目的**: `MultimediaService.export_ebook` 内で取得したイラスト一覧を `builder.build_commercial_epub(..., images=illustrations)` に渡す。
- **対象ファイル**: `src/backend/multimedia_service.py`
- **変更内容**: `build_commercial_epub` 呼び出し引数を更新。
- **確認コマンド**: `python -m py_compile src/backend/multimedia_service.py`
- **完了条件**: 引数が正しく受け渡されること。

### Step 42: `AssetPack` への挿絵付きEPUBの同梱確認
- **目的**: `generate_asset_pack` を実行した際、生成されるZIP内に挿絵が正しく埋め込まれたEPUBが格納されることを保証。
- **対象ファイル**: `src/backend/multimedia_service.py`
- **変更内容**: バンドル生成時のログに「挿絵N枚を含むEPUB」とメタデータを記録。
- **確認コマンド**: `python -m py_compile src/backend/multimedia_service.py`
- **完了条件**: アセットパックのメタデータに挿絵数が反映されること。

### Step 43: EPUB 3 規格適合性チェック（mimetype無圧縮・META-INF順序）
- **目的**: 挿絵を追加しても、EPUB規格必須の「第1エントリが非圧縮のmimetype」である順序が維持されていることを保証。
- **対象ファイル**: `src/services/exporters/pure_epub_packer.py`
- **変更内容**: `build_epub_bytes()` のアサーション強化。
- **確認コマンド**: `python -m py_compile src/services/exporters/pure_epub_packer.py`
- **完了条件**: アーカイブ生成ロジックの厳格性が保証されること。

### Step 44: 各種画像フォーマット（PNG, JPEG, WebP）の自動メディアタイプ判定
- **目的**: PNGやJPEG、WebPが混在した場合でも、正しい `media-type`（`image/png`, `image/jpeg` 等）をOPFに記載。
- **対象ファイル**: `src/services/exporters/epub_manifest_builder.py`
- **変更内容**: 拡張子およびバイナリマジックバイト（`\xFF\xD8`, `\x89PNG`）による MIME 自動判定ヘルパーを実装。
- **確認コマンド**: `python -m py_compile src/services/exporters/epub_manifest_builder.py`
- **完了条件**: MIMEタイプが正確に解決されること。

### Step 45: 画像埋め込みEPUBの統合テスト作成
- **目的**: `MultimediaService.export_ebook` を通して実際にディスクに出力された `.epub` ファイルを解凍し、画像とXHTMLが完全に出力されているかテスト。
- **対象ファイル**: `tests/unit/test_commercial_epub_bundle.py` (新規作成)
- **変更内容**: 実サービス経由での画像付きEPUB出力テスト。
- **確認コマンド**: `pytest tests/unit/test_commercial_epub_bundle.py -v`
- **完了条件**: 統合テストが作成され成功すること。

### Step 46: メモリ消費量の最適化（大容量画像挿入時のストリーミング）
- **目的**: 多数の高解像度画像（10枚以上）が指定された場合でも、極端なメモリ肥大化を抑えるバッファ管理。
- **対象ファイル**: `src/services/exporters/pure_epub_packer.py`
- **変更内容**: 一時ファイルまたはチャンク書き込みの活用（必要に応じてガーベジコレクションを促進）。
- **確認コマンド**: `python -m py_compile src/services/exporters/pure_epub_packer.py`
- **完了条件**: 最適化処理が組み込まれていること。

### Step 47: ドキュメントの更新
- **目的**: 商用EPUBビルダーの挿絵仕様、対応画像形式、推奨アスペクト比（縦横比）を開発者ドキュメントに明記。
- **対象ファイル**: `docs/multimedia.md`
- **変更内容**: 「商用EPUB 3 挿絵・口絵の仕様と登録API」のセクションを追記。
- **確認コマンド**: `git diff docs/multimedia.md`
- **完了条件**: ドキュメントが更新されていること。

### Step 48: Checkpoint 4 - EPUB挿絵配信パッケージの検証
- **目的**: Part 3 および Part 4 の全テストスイートを実行し、画像付き商用EPUB機能の完全性を確認。
- **確認コマンド**: `pytest tests/unit/test_epub_illustrations.py tests/unit/test_commercial_epub_bundle.py`
- **完了条件**: Checkpoint 4 が ALL GREEN であること。

---

## 🌿 Part 5: IFルートマージ確定コミットAPI & 競合解決エンジン (Step 49〜60)

### Step 49: マージコミット用リクエスト・レスポンススキーマの定義
- **目的**: ユーザーがコンフリクトを解決（Base/Source/Target/手動編集）した最終テキスト群を受け取り、確定コミットを行うためのPydanticモデルを定義。
- **対象ファイル**: `src/backend/schemas/branch.py`
- **変更内容**:
  ```python
  from pydantic import BaseModel, Field

  class ResolvedChapterPayload(BaseModel):
      chapter_number: int
      resolved_content: str
      resolution_strategy: str = Field("manual", description="base | source | target | manual")

  class BranchMergeCommitRequest(BaseModel):
      source_branch_id: int
      target_branch_id: int
      merge_ep_num: int
      resolved_chapters: list[ResolvedChapterPayload]
      commit_message: str = "Merged branch with resolved conflicts"

  class BranchMergeCommitResponse(BaseModel):
      success: bool
      target_branch_id: int
      updated_chapters_count: int
      committed_at: str
  ```
- **確認コマンド**: `python -c "from src.backend.schemas.branch import BranchMergeCommitRequest; print('OK')"`
- **完了条件**: スキーマが正しくインポートできること。

### Step 50: `BranchMergeService` の新規作成と責務定義
- **目的**: マージプレビュー（シミュレーション）とマージコミット（実データ更新）のビジネスロジックをルータから分離するサービスクラスを作成。
- **対象ファイル**: `src/backend/services/branch_merge_service.py` (新規作成)
- **変更内容**: `BranchMergeService` クラスを定義し、コンストラクタで `AsyncSession` を受領。
- **確認コマンド**: `python -m py_compile src/backend/services/branch_merge_service.py`
- **完了条件**: サービスクラスがコンパイルできること。

### Step 51: アトミックトランザクションによる章コンテンツ確定更新メソッド
- **目的**: 解決されたチャプター群を対象ブランチの `chapters` テーブルにアトミック（全件成功またはロールバック）に反映する。
- **対象ファイル**: `src/backend/services/branch_merge_service.py`
- **変更内容**:
  - `commit_merge(self, request: BranchMergeCommitRequest) -> BranchMergeCommitResponse` を実装。
  - 各 `resolved_chapters` について、対象ブランチの該当章を `UPDATE` または `INSERT`。
- **確認コマンド**: `python -m py_compile src/backend/services/branch_merge_service.py`
- **完了条件**: DB更新ロジックが実装されていること。

### Step 52: IFグラフ（`BranchGraph`）へのMERGEノード確定反映
- **目的**: マージコミット成功時、ターゲットブランチのIFグラフJSONに正式な `merge` ノードを書き込む。
- **対象ファイル**: `src/backend/services/branch_merge_service.py`
- **変更内容**: `BranchRepository.load_branch_graph` でグラフをロードし、`merge_node_id` を確定追加して再保存。
- **確認コマンド**: `python -m py_compile src/backend/services/branch_merge_service.py`
- **完了条件**: グラフ構造が正しく更新されること。

### Step 53: マージ履歴（リビジョンログ）の保存
- **目的**: いつ、どのブランチからどのブランチへ、どの戦略でマージされたかの監査ログを記録。
- **対象ファイル**: `src/backend/services/branch_merge_service.py`
- **変更内容**: `branch_history` またはメタデータフィールドにコミットログを追記。
- **確認コマンド**: `python -m py_compile src/backend/services/branch_merge_service.py`
- **完了条件**: 履歴保存処理が追加されていること。

### Step 54: 不正な章番号や空テキスト送信のバリデーション強化
- **目的**: 解決済みテキストが空文字である場合や、存在しない章番号が含まれている場合に `ValueError` を発生させる事前チェック。
- **対象ファイル**: `src/backend/services/branch_merge_service.py`
- **変更内容**: `_validate_merge_payload(request)` メソッドを実装。
- **確認コマンド**: `python -m py_compile src/backend/services/branch_merge_service.py`
- **完了条件**: バリデーションが実装されていること。

### Step 55: FastAPI エンドポイント `POST /api/branches/{book_id}/merge/commit` の実装
- **目的**: フロントエンドから解決済みペイロードを受け取る確定APIを公開する。
- **対象ファイル**: `src/backend/routers/branches.py`
- **変更内容**:
  - ルータに `@router.post("/{book_id}/merge/commit", response_model=BranchMergeCommitResponse)` を追加。
  - `BranchMergeService` を呼び出してコミットを実行。
- **確認コマンド**: `python -m py_compile src/backend/routers/branches.py`
- **完了条件**: エンドポイントが追加され、ルーティングが定義されていること。

### Step 56: マージ確定時の EventBus イベント発行 `branch.merged`
- **目的**: マージが確定したことをシステム全体（WebSocket通知やバックグラウンドタスク）に知らせるイベントを発行。
- **対象ファイル**: `src/backend/routers/branches.py`
- **変更内容**: `EventBus.publish("branch.merged", {...})` を呼び出す。
- **確認コマンド**: `python -m py_compile src/backend/routers/branches.py`
- **完了条件**: イベント発行処理が組み込まれていること。

### Step 57: ロールバック機構のテスト（途中で失敗した場合の整合性）
- **目的**: DB更新中に例外が発生した場合、ターゲットブランチの章データやグラフが中途半端に変更されずロールバックされることを確認。
- **対象ファイル**: `tests/unit/test_branch_merge_commit.py` (新規作成)
- **変更内容**: 意図的なエラーを発生させ、トランザクションがロールバックされる単体テストを作成。
- **確認コマンド**: `pytest tests/unit/test_branch_merge_commit.py -k test_rollback -v`
- **完了条件**: ロールバックテストがパスすること。

### Step 58: 正常系マージコミットの単体テスト作成
- **目的**: Source ブランチの変更と Target ブランチの変更をマージコミットし、Target ブランチの章本文が正しく解決後の内容に更新されることを確認。
- **対象ファイル**: `tests/unit/test_branch_merge_commit.py`
- **変更内容**: 正常系のコミットテストケースを作成。
- **確認コマンド**: `pytest tests/unit/test_branch_merge_commit.py -k test_commit_success -v`
- **完了条件**: 正常系テストがパスすること。

### Step 59: `BRANCH_FEATURE_SUMMARY.md` の注意事項更新
- **目的**: 「マージプレビューは現在シミュレーションです」となっていた記述を、実コミット対応完了に更新。
- **対象ファイル**: `BRANCH_FEATURE_SUMMARY.md`
- **変更内容**: 実装完了ステータスおよび `POST /api/branches/{book_id}/merge/commit` の説明を追記。
- **確認コマンド**: `git diff BRANCH_FEATURE_SUMMARY.md`
- **完了条件**: ドキュメントが最新化されていること。

### Step 60: Checkpoint 5 - マージコミットエンジンの検証
- **目的**: Part 5 のバックエンドAPIおよびサービス単体テストを実行し、全テスト合格を確認。
- **確認コマンド**: `pytest tests/unit/test_branch_merge_commit.py`
- **完了条件**: Checkpoint 5 が ALL GREEN であること。

---

## 🚀 Part 6: フロントエンド連携・WebSocket通知・P0総合E2Eテスト (Step 61〜72)

### Step 61: フロントエンドAPIクライアント `commitBranchMerge` の実装
- **目的**: フロントエンドから `POST /api/branches/{book_id}/merge/commit` を呼び出す関数を追加。
- **対象ファイル**: `frontend/src/api/branches.ts`
- **変更内容**:
  ```typescript
  export async function commitBranchMerge(
    bookId: number,
    payload: BranchMergeCommitRequest
  ): Promise<BranchMergeCommitResponse> {
    const res = await fetch(`/api/branches/${bookId}/merge/commit`, {
      method: "POST",
      headers: { "Content-Type": "application/json" },
      body: JSON.stringify(payload),
    });
    if (!res.ok) throw new Error("Merge commit failed");
    return res.json();
  }
  ```
- **確認コマンド**: `npx tsc --noEmit` または構文チェック
- **完了条件**: クライアント関数が追加されていること。

### Step 62: `useMergePreview` カスタムフックへの `commitMutation` 追加
- **目的**: React Query の `useMutation` を用いて、コミット実行と成功時のキャッシュ無効化を行う。
- **対象ファイル**: `frontend/src/hooks/useMergePreview.ts`
- **変更内容**: `useMutation({ mutationFn: commitBranchMerge, onSuccess: () => { queryClient.invalidateQueries(...) } })` を追加。
- **確認コマンド**: `npx tsc --noEmit`
- **完了条件**: カスタムフックにコミット機能が追加されること。

### Step 63: `MergeConflictPreview.tsx` の「マージを実行」ボタンの配線
- **目的**: シミュレーション表示のみだった「マージを実行」ボタンに実際の `commitMutation` を接続。
- **対象ファイル**: `frontend/src/components/branches/MergeConflictPreview.tsx`
- **変更内容**:
  - 全コンフリクト解決状態を検証。
  - クリック時に `commitMutation.mutate(...)` を呼び出し、ローディングスピナーを表示。
- **確認コマンド**: `npx tsc --noEmit`
- **完了条件**: ボタンがAPIと接続されていること。

### Step 64: マージ完了トースト通知およびツリー再描画トリガー
- **目的**: コミット成功時に「ブランチのマージが正常に完了しました」と通知し、ブランチツリー画面を最新状態に自動更新。
- **対象ファイル**: `frontend/src/components/branches/MergeConflictPreview.tsx`
- **変更内容**: 成功コールバックとモーダル閉じる処理の実装。
- **確認コマンド**: `npx tsc --noEmit`
- **完了条件**: 完了フィードバック処理が実装されること。

### Step 65: WebSocket経由でのブランチ更新通知リスナーの実装
- **目的**: 共同編集や複数タブ利用時に、他所でマージが実行されたら自動でブランチ一覧をリフレッシュする。
- **対象ファイル**: `frontend/src/hooks/useBranchTree.ts`
- **変更内容**: WebSocketの `branch.merged` メッセージ受信時に `refetch()` を実行。
- **確認コマンド**: `npx tsc --noEmit`
- **完了条件**: リアルタイム同期が有効化されること。

### Step 66: E2E結合テスト用フィクスチャのセットアップ
- **目的**: P0の3大機能を一気通貫で検証するための統合テストハーネスを整備。
- **対象ファイル**: `tests/integration/test_p0_multimedia_merge_e2e.py` (新規作成)
- **変更内容**: テスト用DB、FastAPI TestClient、テスト画像データを用意。
- **確認コマンド**: `pytest tests/integration/test_p0_multimedia_merge_e2e.py -v`
- **完了条件**: テストフィクスチャが動作すること。

### Step 67: E2Eテスト 1: 実DB小説からのマルチメディア生成フロー
- **目的**: 本文を複数章作成 ➔ `MultimediaService` で漫画台本・音声ドラマ・EPUBを出力 ➔ 生成物に本文が正しく反映されているかを検証。
- **対象ファイル**: `tests/integration/test_p0_multimedia_merge_e2e.py`
- **変更内容**: `test_real_db_multimedia_generation_e2e` を記述。
- **確認コマンド**: `pytest tests/integration/test_p0_multimedia_merge_e2e.py -k test_real_db_multimedia`
- **完了条件**: テストが合格すること。

### Step 68: E2Eテスト 2: 挿絵付き商用EPUB出力フロー
- **目的**: 画像アセットを登録 ➔ EPUBエクスポート実行 ➔ 生成バイナリ内のXHTMLおよび画像配置、Spine順序が仕様通りであることを完全検証。
- **対象ファイル**: `tests/integration/test_p0_multimedia_merge_e2e.py`
- **変更内容**: `test_illustrated_commercial_epub_e2e` を記述。
- **確認コマンド**: `pytest tests/integration/test_p0_multimedia_merge_e2e.py -k test_illustrated_commercial_epub`
- **完了条件**: テストが合格すること。

### Step 69: E2Eテスト 3: IFルート分岐 ➔ コンフリクト発生 ➔ 確定コミットフロー
- **目的**: ブランチフォーク ➔ 各ブランチで章を編集 ➔ マージプレビューでコンフリクト検知 ➔ 解決ペイロードでコミット ➔ マージ後の章内容が確定されることを検証。
- **対象ファイル**: `tests/integration/test_p0_multimedia_merge_e2e.py`
- **変更内容**: `test_branch_fork_merge_commit_e2e` を記述。
- **確認コマンド**: `pytest tests/integration/test_p0_multimedia_merge_e2e.py -k test_branch_fork_merge_commit`
- **完了条件**: テストが合格すること。

### Step 70: APIエラーレスポンスおよびエッジケース総合検証
- **目的**: 異常なフォーマット名、存在しない画像ID、不正なブランチID送信時の適切なHTTPステータスコードを検証。
- **対象ファイル**: `tests/integration/test_p0_multimedia_merge_e2e.py`
- **変更内容**: 異常系テストケースの追加。
- **確認コマンド**: `pytest tests/integration/test_p0_multimedia_merge_e2e.py -k test_edge_cases`
- **完了条件**: エッジケーステストが合格すること。

### Step 71: コードクリーンアップと型チェック（mypy/ruff）
- **目的**: 新規作成・変更した全ファイルの型アノテーション整合性とコードスタイルを検証。
- **対象ファイル**: `src/backend/`, `src/services/`
- **確認コマンド**: `python -m py_compile src/backend/multimedia_service.py src/backend/database/series_loader.py src/services/exporters/epub_commercial_builder.py src/backend/routers/branches.py`
- **完了条件**: コンパイルエラーおよび重大な警告がないこと。

### Step 72: Final Gate (Checkpoint 6) - P0 全テスト一括実行 (100% ALL GREEN)
- **目的**: P0 に関連する全ての単体テスト・結合テストスイートを一括実行し、完全合格を確認。
- **確認コマンド**: `pytest tests/unit/test_series_loader.py tests/unit/test_multimedia_real_db.py tests/unit/test_epub_illustrations.py tests/unit/test_commercial_epub_bundle.py tests/unit/test_branch_merge_commit.py tests/integration/test_p0_multimedia_merge_e2e.py -v`
- **完了条件**: 全テストが 100% GREEN でパスすること。
